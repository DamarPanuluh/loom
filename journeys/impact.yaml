schema: loom.journey/v1
id: impact
name: Inspect what a change could reach
actor: operator
goal: An operator sees callers and behaviors at risk without losing resolution confidence
  or unresolved uncertainty.
inputs: {}
preconditions:
- The repository has extracted call relationships for the target symbol.
steps:
- id: inspect-change-impact
  name: Inspect Change Impact
  action: Ask what changing the target symbol could reach.
  expects:
  - Loom reports callers and behaviors at risk, separates exact from heuristic resolution,
    and counts unresolved calls.
  produces: {}
profiles:
  proof:
    inputs: {}
    workspace: {}
