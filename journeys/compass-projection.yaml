schema: loom.journey/v1
id: compass-projection
name: The compass projects the next unmet rung
actor: operator
goal: An operator sees one work direction derived from the maturity ladder rather
  than a competing decision system.
inputs: {}
preconditions:
- The graph contains a named behavior that has not yet been realized.
steps:
- id: inspect-work-direction
  name: Inspect Work Direction
  action: Inspect the graph's current work direction.
  expects:
  - The reported compass points to build because grounding is the lowest unmet rung.
  produces: {}
profiles:
  proof:
    inputs: {}
    workspace: {}
