journey: divergences reach the human, not the machine
steps:
  - name: meaning drift reaches the human ratify lane
    fixture: true
    intent: the human is asked only where judgment and evidence disagree
    run: "cargo test --test ring14_ratify redefinition_stales_ratification"
    expect:
      exit_code: 0
      stdout_contains: ["redefinition_stales_ratification", "1 passed", "0 failed"]
