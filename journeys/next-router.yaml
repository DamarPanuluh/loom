schema: loom.journey/v1
id: next-router
name: Receive the highest-priority actionable work
actor: operator
goal: The next-work router returns one self-contained packet for the most important
  asserted residue.
inputs: {}
preconditions:
- At least one enabled work lane contains actionable asserted residue.
steps:
- id: request-next-work
  name: Request Next Work
  action: Ask Loom for the next work item.
  expects:
  - The response identifies the lane, owner, target, allowed and forbidden actions,
    required evidence, write-back, and stop condition.
  produces: {}
profiles:
  proof:
    inputs: {}
    workspace: {}
