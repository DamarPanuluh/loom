schema: loom.journey/v1
id: find-grounding
name: Find a behavior with its grounding
actor: operator
goal: Search results explain where matched behavior lives and how confidently that
  location is known.
inputs: {}
preconditions:
- A behavior has a realizing file, a symbol locator, and recorded grounding evidence.
steps:
- id: find-exact-behavior
  name: Find Exact Behavior
  action: Find the behavior by its exact name.
  expects:
  - The result includes the realizing path, locator, grounding role, inspection state,
    and evidence.
  produces: {}
profiles:
  proof:
    inputs: {}
    workspace: {}
