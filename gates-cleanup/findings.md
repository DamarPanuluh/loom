[P1] src/artifact.rs:18 | simplification | `fingerprint(&str)` and `fingerprint_bytes(&[u8])` are byte-for-byte the same FNV-1a loop, with the two magic constants written twice | COST: the doc at :28 asserts "same FNV-1a schedule ... so text and byte fingerprints agree" in prose; nothing in code holds it, so editing one schedule silently forks every stored content_hash from every byte-hashed one | FIX: `pub fn fingerprint(content: &str) -> String { fingerprint_bytes(content.as_bytes()) }`
[P1] src/artifact.rs:77 | efficiency | `class_for` calls `classes()`, which heap-allocates a `Vec<Box<dyn ArtifactClass>>`, to answer one type comparison | COST: one Vec + one Box allocated and dropped per lookup, on a seam meant to be called per artifact | FIX: match on `node_type` directly, or have `classes()` return `&'static [&'static dyn ArtifactClass]`
[P1] src/statistics.rs:12 | efficiency | `quantile` sorts `samples` in place on every call | COST: a caller wanting p50 and p95 from one sample sorts it twice; O(n log n) paid per quantile rather than per sample | FIX: verify callers (thresholds.rs, proofstrength/) — if any asks for 2+ quantiles, split into `sort_samples` + `quantile_of_sorted`
[P1] src/identity.rs:45 | reuse+altitude | `Agent::parse` hand-rolls a `&str -> OwnerRole` match, and the same lane list is spelled a THIRD and FOURTH time as prose inside the two bail! messages at :42 and :54 (which are byte-identical to each other) | COST: the lane vocabulary now exists in four places — `registry.rs:17` (enum), `registry.rs:31` (as_str), `identity.rs:45` (parse), `policy.rs:67` (GATEABLE_ROLES) — plus twice more in prose; adding a lane means editing six sites and the compiler catches only one | FIX: give `OwnerRole` an `ALL` const and a `FromStr` beside its `as_str` in registry.rs (or route it through model.rs's `str_enum!`, which already generates exactly ALL/as_str/FromStr/Display/serde for 15 other enums); then `identity.rs` parses via `lane.parse()` and builds its message from `ALL`, and `GATEABLE_ROLES` derives from `ALL` minus Sync/Rectify
[P1] src/registry.rs:29 | reuse | `OwnerRole` is the only string-backed enum in the crate that does NOT use `model.rs:45 str_enum!`; it hand-writes `as_str` and has no `ALL`, `FromStr`, `Display`, or serde | COST: every consumer that needs parse-or-enumerate re-implements it (see the identity.rs finding); the macro exists precisely to stop this and covers NodeType, EdgeKind, TruthClass, and 12 others | FIX: `str_enum!` is private to model.rs — either `pub(crate) use str_enum;` it and apply, or add `ALL` + `FromStr` by hand in registry.rs
[P1] src/policy.rs:34 | altitude | `MAX_ADVERSARIAL_REVIEW_FRONTIER` is enforced (bails in `validate()` at :87) but is absent from `limits::all()` | COST: limits.rs:5 states its registry is "the single list `loom limits` renders ... so the list cannot drift from enforcement"; this cap already drifted, so a caller hitting it cannot discover it from `loom limits` | FIX: add a `Limit` entry referencing `crate::policy::MAX_ADVERSARIAL_REVIEW_FRONTIER`. Same question for the 128-char `WorkerProfile` cap at identity.rs:78
[P1] src/truth.rs:22 | reuse | `TruthAxis` is the second string-backed enum that opts out of `model.rs:45 str_enum!`: it hand-writes `as_str` (:76), hand-writes a separate `TRUTH_AXES` const (:46) where the macro generates `ALL`, and adds `#[serde(rename_all = "snake_case")]` (:23) as a THIRD independent way to produce the same strings | COST: three mechanisms must agree on every variant name, and only the serde one is checked by anything; the const is also named `TRUTH_AXES` rather than `TruthAxis::ALL`, so callers cannot discover it the way they discover `NodeType::ALL` | FIX: same cluster as the `OwnerRole` finding — route both through `str_enum!`, which yields as_str + ALL + FromStr + Display + serde from one variant list
[P1] src/mcp.rs:269 | simplification | `loom_journal` writes its default twice — "(default 20)" as prose at :269 and `unwrap_or(20)` at :282 | COST: exactly the prose/code drift the 4-commit diff just closed for `loom_impact` (:163) and `loom_observe` (:208) by building the description with `format!` from the constant; this third tool in the same file was left on the old shape | FIX: name the constant beside the reader and `format!` the description from it, matching the two tools above it
[P1] src/cli.rs:445 | altitude | `ModeArg` (16 variants + hand-written `as_str`), `lane::Lane::LADDER`, `RoleArg` (7 + `as_str`), `registry::OwnerRole` (7), `policy::GATEABLE_ROLES` (5), and the match in `identity.rs:45` are six parallel spellings of overlapping lane/role vocabularies | COST: the test at cli.rs:591 exists solely to assert two of these hand-written lists still agree — a test guarding a duplication instead of a construction that cannot drift; the other four pairs have no such guard | FIX: needs a design call at fix time. `ModeArg`/`RoleArg` must stay clap types, so the honest fix is deriving their string and their ALL from `Lane`/`OwnerRole` (one direction, one list) rather than adding more agreement tests. Lower-risk subset: give `OwnerRole` `ALL`+`FromStr` and derive `GATEABLE_ROLES` and the `identity.rs` parse from it.
[P1] src/commands.rs:372 | reuse | `parse_version` and `store/schema.rs:353 parse_crate_version` have byte-identical bodies (`value.split('.').map(|p| p.parse::<u64>().ok())` then `Some((n??,n??,n??))`), under two names, both private | COST: both answer the same question — is this binary's crate older than the graph's last writer — one to warn (commands.rs:381) and one to gate migration consent (schema.rs:372); they can silently disagree on what parses, and the migration-consent path is the one that matters | FIX: `src/text.rs` is the precedent the diff just set for exactly this shape (a pure low module so a low ring need not import the command plane). Add `parse_semver` there or in a sibling pure module and have both call it.
[P1] src/commands.rs:70 | simplification | the dispatch match repeats `cli.graph.as_deref()` 62 times and `cli.json` 65 times across ~175 lines | COST: 127 repeated field accesses where two bindings would do; every new arm copies the pair, and the width they add is why several arms wrap onto three lines | FIX: after the `let Some(command) = cli.command else` partial move, bind `let graph = cli.graph.as_deref(); let json = cli.json;` once and use them — disjoint-field borrow, so it compiles unchanged
[P1] src/commands.rs:643 | altitude | `fn layer` is the only command handler that lives in `commands.rs` itself; every other arm of the dispatch match routes to a `*_cmd` submodule | COST: the module doc at :3 says this file resolves the graph, calls the store, renders output — `layer` does full command work here, and it already reaches into `domain_cmd::layer_detector_state` (:660) for half its body, so its home is unambiguous | FIX: move it to `commands/domain_cmd/layers.rs` beside `layer_detector_state` and dispatch to it like every sibling
[P1] src/commands.rs:650 | reuse | the meta key `"layer_order"` is a bare string literal in 8 places across 5 files: commands.rs x4, travel.rs:388, domain_cmd/layers.rs:21, signal/smells.rs:891, and the portability allowlist at store/mod.rs:92 | COST: the allowlist entry is the one that must match exactly or the key silently stops travelling in the export, and nothing ties it to the writers; `policy.rs:23` already names `EVIDENCE_POLICY_META_KEY` as a const for precisely this reason | FIX: one `pub const LAYER_ORDER_META_KEY` (beside the allowlist, or in domain_cmd) referenced by all 8 sites
[P1] src/commands.rs:624 | simplification | `query_terms` writes the collect/sort/dedup pipeline twice, identical except that the fallback drops the stopword filter and lowers the length floor from 3 to 2 | COST: 14 lines doing one job; a change to normalization or dedup has to be made in both halves or the fallback quietly diverges from the primary path | FIX: one closure `gather(min_len, stop)` called as `gather(3, STOP)` then `gather(2, &[])` when the first is empty
[P2] src/prescan.rs:156 | reuse | `truncate_excerpt` caps text at N characters with a manual `char_indices` walk — the exact job `src/text.rs:15 ellipsize` was created for in this very diff, minus the `…` marker | COST: the diff's stated goal was "one rule for capping rendered text" and it consolidated `commands::truncate` + `scan::truncate_chars`; this third copy was left behind, so the module doc now overstates what it covers | FIX: `ellipsize` with an explicit no-marker mode, or keep prescan's and have it call a shared char-boundary prefix helper. Its two `160` literals (:70, :153) should also be one const — adjudication hashing depends on both producing the identical canonical form (store/adjudications.rs:128)
[P2] src/commands/capture_cmd.rs:612 | reuse | `id.chars().take(8).collect()` re-implements `model::short` (model.rs:22), and `workitem/mod.rs:491` does the same thing again | COST: `model::short` exists specifically because `&id[..8]` panics on short or multibyte ids — its doc says so; both inline copies also allocate a `String` where `short` returns a `&str`, and both hardcode the 8 that `short` owns | FIX: `crate::model::short(id)` at both sites
[P2] src/commands/proposal_cmd.rs:559 | simplification | caps a string at 100 chars by `take(97)` plus a literal `"..."`, while `text::ellipsize` caps at N and appends `…` | COST: two cap-and-mark conventions in one crate that produce different lengths (100 vs 101 chars) and different markers for the same intent; a reader cannot tell which one a given surface used | FIX: pick one. If the 100-char total matters, give `ellipsize` that contract; otherwise call it directly
[P2] src/pattern.rs:179 | simplification | `pattern::truncate_excerpt` and `prescan::truncate_excerpt` share a name but have different contracts — byte budget + omitted-byte marker + `Option` return, versus char cap + no marker + `String` | COST: not a duplicate, but the shared name means a reader who has seen one will misread the other; the byte-vs-char difference is exactly the bug class `text.rs:10` documents | FIX: rename pattern's to `fit_within_byte_budget` (what it actually does); leave the logic alone
[P2] src/journal.rs:414 | altitude | `now_iso()` returns epoch MILLISECONDS as a string (`timestamp()` at :569 formats `as_millis()`), not an ISO timestamp; callers who actually want ISO must write `millis_to_iso(now_iso().parse::<i64>().unwrap_or(0))` | COST: the name lies to all 14 call sites, and the two that want a real ISO string (harness.rs:175, store/lock.rs:108) each carry a silent `unwrap_or(0)` that would stamp 1970-01-01 rather than fail if the format ever changed | FIX: rename to `now_millis()` (what it returns) and add `now_iso()` that does the conversion once, so neither caller re-derives it or swallows the parse
[P2] src/harness.rs:96 | simplification | `acquire_for_artifact` (:96-104) and `lock_path` (:208-216) contain the same four-step temp-lock-path construction — canonicalize, fall back to the original on failure, `to_string_lossy`, fingerprint into `temp_dir().join(format!("loom-harness…-{}.lock"))` — differing only in the filename prefix | COST: two copies of the fallback-on-canonicalize rule, which is the part that decides whether two processes agree they are contending for the same resource; they can drift apart silently | FIX: one `fn temp_lock_path(prefix: &str, target: &Path) -> PathBuf`, called with `"loom-harness-spec"` and `"loom-harness"`
[P2] src/runner.rs:181 | simplification | `covered_hashes` (:185-187) and `covered_intact` (:202-204) both inline `read_to_string(root.join(f)).map(|c| fingerprint(&c)).unwrap_or_default()` | COST: the `unwrap_or_default()` is load-bearing — the doc at :178 says an unreadable file must hash empty so its later appearance counts as a change — and that rule is stated once but implemented twice | FIX: `fn hash_file(root: &Path, rel: &str) -> String` called by both
[P2] src/anchor.rs:34 | altitude | the settling-state policy is driven by raw `&str` matching: `is_settling` (:34) lists 14 state strings and `required_for` (:99) lists 6 more that must not overlap, while those same states exist as typed enums (`InspectionStatus` in model.rs:175, adjudication states elsewhere) | COST: two hand-written string lists that must stay disjoint and jointly exhaustive, with no compiler check; a renamed or added state silently falls through to the wrong floor | FIX: take the typed state (or a `SettleState` enum) instead of `&str`, so the match is exhaustive and the partition is a property of the type
[SWEEP] src/locator.rs:699 | reuse | The edge facet key "locator" is a bare string literal at 62 sites across 27 files, and the read idiom `store.get_facet(&edge.id, TargetKind::Edge, "locator")?` is hand-inlined at 24 of them even though `commands/context_cmd.rs:505` already wraps it as `edge_locator`. | COST: This is the single most-spread key in the crate and the only facet key `set_facet` special-cases by name (`store/facets.rs:74`). A rename, or a decision to store it under a namespaced key, means editing 62 call sites the compiler cannot find; `signal/doctor.rs` reads it from a `Snapshot` instead of the store, so a typo there degrades silently to `None` (edge looks unlocated) rather than failing. `divergence.rs:160` already names `RECTIFY_FACET` and `evidence.rs:49` names `EVIDENCE_SPANS_KEY` for keys used in 2 and 3 places respectively — the crate's own precedent is the opposite of what the busiest key got. | FIX: `pub const LOCATOR_FACET: &str = "locator";` in `src/locator.rs` (its natural home), referenced by all 62 sites; promote `context_cmd::edge_locator` to `Store::edge_locator(&self, edge_id) -> Result<Option<String>>` beside `store/edges.rs:421` so the 24 inlined three-argument calls collapse to one. | SITES: src/locator.rs:731, src/locator.rs:847, src/pattern.rs:273, src/sync.rs:615, src/sync.rs:666, src/divergence.rs:457, src/divergence.rs:679, src/divergence.rs:750, src/divergence.rs:759, src/proofstrength/mod.rs:592, src/proofstrength/mod.rs:612, src/proofstrength/entries.rs:119
[SWEEP] src/sync.rs:429 | altitude | The "is this compiled Validation still current for its Journey?" predicate — the same four/five body keys `type` / `journey_hash` / `surface_hash` / `profile` / `compiler_version` compared against the same right-hand sides — is written out longhand in seven files, each with its own spelling of the `.body.get(K).and_then(as_str)` chain. | COST: Seven independent copies of one staleness rule, all keyed by bare strings. `sync.rs` and `completeness/readiness.rs` check all five; `signal/doctor.rs:540-566` checks only `compiler_version` and `journey_hash` and omits `surface_hash` and `profile`; `journey_exercises.rs:435-450` checks `type`/`profile`/`compiler_version` but not `surface_hash`. They already disagree about what "current" means, and each new consumer copies whichever neighbour it happened to read. A sixth field added to the Validation body is invisible to all seven until someone edits all seven. | FIX: Name the contract once — a `struct CompiledProofBody { journey_hash, surface_hash, profile, compiler_version }` with `#[serde]` field names (or a `journey::compile::proof_body_current(&Node, &JourneyFacts) -> bool`) in `src/journey/compile.rs`, which already owns `JOURNEY_COMPILER_VERSION` and writes the body at `commands/journey/runtime.rs:248-254`. The seven readers then call one predicate and the key strings exist once, as serde field names. | SITES: src/journey_exercises.rs:439, src/journey_exercises.rs:446, src/journey_exercises.rs:449, src/journey/compile.rs:287, src/journey/compile.rs:297, src/journey/compile.rs:303, src/journey/compile.rs:308, src/journey/compile.rs:607, src/journey/compile.rs:620, src/journey/compile.rs:649, src/journey/compile.rs:654, src/release/section_05.rs:22
[SWEEP] src/commands/journey/registry.rs:161 | reuse | The provenance-facet vocabulary on `Surfaces`/`Derives` edges — "journey_hash", "step_ids", "operation_bindings", "setup", "proposal_id", "manifest_hash" — is written as bare literals at 19, 19, 13, 10, 12 and 12 sites spanning 15, 7, 7, 5, 3 and 2 files, with no const anywhere; three of the readers are near-identical projection loops. | COST: These six keys ARE the compiled-Journey provenance contract: `sync.rs`, `proofstrength/entries.rs`, `completeness/mod.rs` and `signal/doctor.rs` stale a proof when they disagree. Every read is a bare string, so a mistyped key reads as `None` and the graph quietly declares the surface unbound rather than erroring. The three projection loops in `registry.rs:157-173`, `surface.rs:11-29` and `derive.rs:14-30` emit overlapping-but-different subsets of the same facet set, so "what a surface edge carries" has to be reconstructed by diffing them. | FIX: One `pub mod facet_keys` (or consts in `src/journey/mod.rs`) naming all six, referenced everywhere — the shape `policy.rs:23 EVIDENCE_POLICY_META_KEY` and `evidence.rs:49 EVIDENCE_SPANS_KEY` already use. Then collapse the three projection loops into one `surface_edge_json(&store, &edge)` / `derivation_edge_json(&store, &edge)` pair in `commands/journey/support.rs`, which already hosts the shared `edge_json_facet`. | SITES: src/sync.rs:481, src/proofstrength/entries.rs:426, src/journey/compile.rs:84, src/journey/compile.rs:164, src/journey/compile.rs:173, src/journey/compile.rs:182, src/journey/derivation.rs:364, src/journey_exercises.rs:261, src/release/section_02.rs:131, src/release/section_02.rs:136, src/release/section_02.rs:141, src/completeness/readiness.rs:42
[SWEEP] src/store/mod.rs:92 | altitude | `PORTABLE_META_KEYS` re-spells all eight meta keys as literals — including the three that already have consts (`evidence_policy`, `thresholds`, `scan_adapters`) — while the other five (`ignores`, `codefile_globs`, `observed_globs`, `upstream_graphs`, and the unlisted `upstream_hashes`) have no const and are bare at 21 sites across 9 files. | COST: The allowlist is the export gate: an entry that does not byte-match its writer silently drops that key from every exported graph, and nothing links the two. `policy.rs:23` names `EVIDENCE_POLICY_META_KEY` explicitly "allowlisted in `store::PORTABLE_META_KEYS`" — and then the allowlist writes `"evidence_policy"` by hand anyway, so the doc-comment link is the only thing holding them together. `federation.rs` writes two sibling keys, `upstream_graphs` (portable) and `upstream_hashes` (deliberately not), four lines apart as identical-looking literals; that distinction lives nowhere but this list. | FIX: Give the five unnamed keys consts beside their owners (`coverage.rs`, `commands/codefile_cmd.rs`, `federation.rs`) matching `thresholds.rs:18` / `scan.rs:17` / `policy.rs:23`, then build `PORTABLE_META_KEYS` from all eight consts so the allowlist cannot drift from the writer. | SITES: src/commands/diagnostics_cmd/coverage.rs:107, src/commands/diagnostics_cmd/coverage.rs:109, src/commands/diagnostics_cmd/coverage.rs:125, src/commands/diagnostics_cmd/coverage.rs:131, src/commands/diagnostics_cmd/coverage.rs:145, src/commands/codefile_cmd.rs:334, src/commands/codefile_cmd.rs:337, src/federation.rs:37, src/federation.rs:53, src/federation.rs:95, src/federation.rs:143, src/travel.rs:285
[SWEEP] src/store/open.rs:309 | reuse | `lib.rs` exports `LOOM_DIR = ".loom"` and `GRAPH_EXPORT = "loom.graph.json"`, used through the const by 11 and 9 files respectively — yet nine sites in eight other files write the literal instead. | COST: Two spellings of the same path in one crate. `store/open.rs:309` builds the export path from the literal while `commands/apply_cmd.rs:360` and `commands/status_cmd.rs:248` use `crate::GRAPH_EXPORT` for the same file, so the const no longer means "the export filename" — it means "the export filename, at the places that remembered". `release/mod.rs` alone spells `".loom"` five times across four separate exclusion arrays and `"loom.graph.json"` once, none through the consts. | FIX: Replace all nine literals with `crate::LOOM_DIR` / `crate::GRAPH_EXPORT`. Separately, `commands/journey/registry.rs:91-96` re-derives the `.loom/compiled/journeys/<name>` layout that `journey_runtime::artifacts::proof_path` (`artifacts.rs:13-18`) owns, just to delete it — that directory should come from a `journey_cache_dir(root, id)` in `artifacts.rs`. | SITES: src/release/section_03.rs:281, src/release/section_03.rs:365, src/release/section_08.rs:475, src/release/section_01.rs:151, src/travel.rs:319, src/checkpoint.rs:336, src/checkpoint.rs:502, src/release/section_03.rs:476, src/commands/status_cmd.rs:27
[SWEEP] src/checkpoint.rs:1120 | reuse | The "this path enters a reserved root" rule is implemented four times in four files with four different literal sets — `(".loom",".git")` twice verbatim, `(".git",".loom","target")` once, and a ten-entry `INVENTORY_RESERVED_COMPONENTS` once. | COST: Four independent answers to one question. `checkpoint.rs:1120-1123` and `journey/surface_setup.rs:218-221` are byte-identical `components().any(...)` closures; `candidate_surface_policy.rs:1080` adds `target` and only checks the FIRST component, so `a/.loom/b` passes there and fails in the other two. Adding a new reserved directory (the repo just added `.commandcode` to `release/mod.rs:85`) reaches one of the four and silently leaves the other three admitting it. | FIX: One `pub fn path_enters_reserved_root(path: &Path) -> bool` over a single `RESERVED_COMPONENTS` const (built from `LOOM_DIR` plus the rest), in a low module both the checkpoint and journey planes already depend on; `release/mod.rs`'s exclusion arrays then derive from that same list rather than restating it. | SITES: src/journey/surface_setup.rs:219, src/candidate_surface_policy.rs:1080, src/release/mod.rs:66, src/release/mod.rs:69, src/release/mod.rs:77, src/release/mod.rs:84 [BEHAVIOR-CHANGE: skip unless deliberately chosen]
[SWEEP] src/store/nodes/ratify.rs:227 | reuse | Journal event names are bare literals wherever the writer and its reader live in different files, and the `"rejected" -> "rejection" else "ratification"` mapping is a verbatim five-line duplicate in two files — while `audit.rs`, `batch_auth.rs` and `evidence.rs` already name their event strings as consts. | COST: Nine event names cross a file boundary as raw text between a writer and the matcher that recognises it (`audit.rs:986` vs `packet.rs:57`; `batch_auth.rs:538` vs `commands/intent/commands.rs:50`; `proofstrength/mod.rs:456-457` vs the fixtures in `journal.rs`). A rename touches only the writer, and the reader's `entry.event != "..."` then matches nothing — the journal keeps accumulating rows nobody reads, which fails as "no evidence" rather than as an error. The `ratification`/`rejection` derivation existing twice means the write boundary (`ratify.rs`) and the read-back check (`facts.rs`) can disagree about which event backs a rejected fact. | FIX: Consts beside each writer in the shape `audit.rs:43 INCIDENT_EVENT` / `batch_auth.rs:22 EVENT` already use, and one `fn ratification_event(state: &str) -> &'static str` (or a `Ratification::journal_event()` on the existing enum in `ratification.rs`) called by both `ratify.rs` and `facts.rs`. | SITES: src/journal.rs:627, src/journal.rs:631, src/journal.rs:666, src/journal.rs:679, src/journal.rs:683, src/journal.rs:708, src/journal.rs:717, src/journal.rs:780, src/journal.rs:1047, src/journal.rs:1055, src/divergence.rs:850, src/batch_auth.rs:1126
[SWEEP] src/release/section_06.rs:223 | reuse | "Cap captured output, keep both ends, disclose the omission" is implemented six independent times across the crate — two of them byte-for-byte identical — with four different omission markers and two separately-declared 8192-byte constants. | COST: Six copies of the same rule with four different marker strings (`…[N bytes omitted]…`, `…[N bytes omitted by Pattern budget]…`, `...[diagnostic output omitted]...`, and no marker at all), so an operator reading a truncated stream cannot tell which layer cut it; the two char-boundary walks are hand-written twice while `runner::excerpt` and `proof::output_excerpt` instead lean on `from_utf8_lossy`. The budgets have already drifted: `runner::EXCERPT_BYTES = 8192` and `proof::VALIDATION_OUTPUT_EXCERPT_BYTES = 8192` are the same number declared twice, `subprocess::KEEP_BYTES` and `journey_runtime::STREAM_EXCERPT_BYTES` are both `512 * 1024` declared twice, and only three of the six are registered in `limits::all()` — `VALIDATION_OUTPUT_EXCERPT_BYTES` and `FAILURE_DIAGNOSTIC_BYTES` are invisible to `loom limits` even though `limits.rs` claims to be the single list that cannot drift from enforcement. Also: `runner::excerpt` runs on bytes `subprocess::Window::render` already truncated, so one stream can carry two nested markers and neither copy knows about the other. | FIX: Zero-behavior-change first step: `release/section_06.rs:223 bounded_diagnostic_text(text, limit)` and `journey_runtime/process.rs:174 bounded_runtime_diagnostic(text)` are the same function — hoist one `pub fn bounded_head_tail(text: &str, limit: usize, marker: &str) -> String` into `src/text.rs` (the module created for exactly this: "one rule for capping rendered text", below every ring that renders) and have both call it with their own limit and the identical marker. Second step, a deliberate output choice: give `runner::excerpt`, `subprocess::Window::render`, `pattern::truncate_excerpt`, and `proof::output_excerpt` the same helper and one marker vocabulary, and register every remaining budget in `limits::all()`. | SITES: src/journey_runtime/process.rs:174, src/runner.rs:221, src/subprocess.rs:510, src/pattern.rs:179, src/proof.rs:295, src/prescan.rs:156, src/text.rs:15, src/limits.rs:55, src/limits.rs:62, src/limits.rs:97
[SWEEP] src/journey/spec.rs:620 | reuse | The recursive "sort every object's keys, recurse into arrays" JSON canonicalizer is written five times under four names, in five different modules, all feeding hashes that are compared against each other. | COST: Every one of these feeds a content hash that gates something: journey semantic hashes (`spec.rs:362`), release manifest hashes (`section_03.rs:185`, `section_04.rs:91/368`, `section_06.rs:487`), the derive-command's canonical form, the completeness exemption round-trip check at `completeness/mod.rs:237` (which asserts `to_string(canonicalize_json(parse(raw))) == raw`), and journey runtime capture canonicalization. Five bodies must agree exactly or two subsystems that both call their form "canonical" produce different bytes for the same value — and nothing in the crate compares them. `completeness`'s copy is also the only one taking `&Value` and cloning every scalar, so it is the odd one out on both signature and allocation. | FIX: One `pub fn canonicalize(value: Value) -> Value` (the by-value form four of the five already use) in a pure low module — `src/text.rs` is the precedent for exactly this shape, or a sibling `src/canonical.rs` — and delete the other four. `completeness/mod.rs:221` takes `&Value`; give it `canonicalize(value.clone())` at the one call site rather than a second body. | SITES: src/journey_runtime/values.rs:496, src/commands/journey/derive.rs:1158, src/release/section_08.rs:301, src/completeness/mod.rs:221
[SWEEP] src/commands/capture_cmd.rs:273 | altitude | Every `list` command hand-writes the same status/name/short-id row — `println!("{:<10} {} [{}]", n.status, n.name, model::short(&n.id))` — eleven times across nine files, while the FOOTER of those exact same loops was already extracted into `commands::page_footer`. | COST: The generalization was applied to half the renderer: `page_footer` is called from 15 sites so pagination hints cannot drift, but the row directly above it is copy-pasted. The copies have already drifted — the status column is padded to 8 in `wiki.rs:259`, 9 in `capture_cmd.rs:713`, 10 in six places, and 12 in `intent/commands.rs:455`, so identical statuses do not line up between `loom inbox list` and `loom intent list`, and `store/nodes/lookup.rs:159` renders the pair in the opposite order (`[{short}] {name}`). Adding a column, changing the bracket form, or widening the id prefix means editing eleven sites the compiler cannot find. | FIX: Add `pub(crate) fn status_row(status: &str, name: &str, id: &str, width: usize) -> String` next to `page_footer` in `commands.rs` (it already owns the shared text-mode rendering for list commands) and call it from all eleven; pass each site's current width so the change is byte-identical, then converge the widths as a separate, deliberate output change. | SITES: src/commands/capture_cmd.rs:291, src/commands/capture_cmd.rs:461, src/commands/capture_cmd.rs:484, src/commands/capture_cmd.rs:713, src/commands/capture_cmd.rs:958, src/commands/proof_cmd/validation.rs:395, src/commands/domain_cmd/hypothesis.rs:369, src/commands/domain_cmd/surface.rs:166, src/commands/proposal_cmd.rs:118, src/commands/intent/commands.rs:455, src/commands/wiki.rs:259, src/commands/proof_cmd/rule.rs:143
[SWEEP] src/commands/status_cmd.rs:418 | reuse | The excluded-codefile percentage — divide-by-zero guard and all — is written out verbatim in two renderers instead of living beside `CoverageScope::excluded()`, the derived accessor it is built from. | COST: `coverage.rs` already owns `excluded()` as a derived accessor precisely so callers do not recompute it; the percentage over the same two fields is the obvious next accessor and instead lives in two command modules. Both render the same `"{} owned, {} unowned, {} excluded ({:.1}%)"` phrase, so `loom status` and `loom coverage` must be edited together to stay consistent, and the `total_registered == 0` guard is stated twice — the second copy inline inside a `println!` argument list, where it is easy to miss. | FIX: Add `pub fn excluded_percent(&self) -> f64` to `CoverageScope` in `src/coverage.rs` next to `excluded()` (`if self.total_registered == 0 { 0.0 } else { self.excluded() as f64 * 100.0 / self.total_registered as f64 }`) and call it at both sites. | SITES: src/commands/diagnostics_cmd/coverage.rs:85, src/coverage.rs:43
[SWEEP] src/checkpoint.rs:884 | reuse | The byte-capping stdout filter for a `controlled_with_output` git run — Arc<AtomicBool> flag, running `accepted` counter, cap check, and the identical post-wait re-check — is duplicated between `checkpoint.rs` and `signal/debt/git.rs`, along with two separately-declared 16 MiB constants. | COST: Two spellings of the same safety rule ("never partial-parse a truncated git log") that must stay in sync but share nothing: two constants both equal to `16 * 1024 * 1024`, two closures, two post-wait re-checks. Neither constant appears in `limits::all()`, so `loom limits` cannot report the git output ceiling at all. Raising the cap for one git surface silently leaves the other at the old value. | FIX: One `pub(crate) fn capped_git_stdout(child, time_limit, cap) -> Option<Vec<u8>>` (or a `CappedStdout` helper returning the filter plus its flag) in a shared module; both call sites pass their own timeout and one shared `GIT_OUTPUT_CAP`, which then gets a `Limit` entry. | SITES: src/signal/debt/git.rs:79, src/signal/debt/git.rs:14, src/checkpoint.rs:23, src/limits.rs:55
[SWEEP] src/rolelease.rs:383 | simplification | `rolelease.rs` renders "seconds since last seen" with the same `now.saturating_sub(x.last_seen_ms) / 1000` expression at four sites, and `describe` and `roster_value` each carry a full private copy of the per-role debt computation. | COST: Four hand-written millis→seconds conversions next to a `RoleLease` that already owns the one derived time method (`is_fresh`), so age-of-lease is derived in four places and freshness in one. The bigger copy is the debt loop: `roster_value` (JSON) and `describe` (human) each walk `role_lanes(role)`, special-case `Lane::Review` by filtering `review::pending` on `owner_role`, and sum — so `loom role list --json` and `loom role list` compute the same number by two independent routes and can disagree, which is the exact drift the JSON/text split is supposed to avoid. | FIX: Add `pub fn age_secs(&self, now_ms: u64) -> u64` to `RoleLease` beside `is_fresh` and use it at all four sites. Then extract `fn role_debt(role, queues, review) -> (BTreeMap<&str, usize>, u64)` and have `roster_value` render its map to JSON and `describe` render its sum to text, so both read one computation. | SITES: src/rolelease.rs:211, src/rolelease.rs:221, src/rolelease.rs:393, src/rolelease.rs:321, src/rolelease.rs:65
[SWEEP] src/store/facts.rs:1666 | reuse | The 7-value finding-verdict vocabulary has no enum at all: it is hand-spelled as raw &str lists or match arms in 11 places across 6 modules, while model.rs:45 str_enum! generates exactly that surface for 16 sibling vocabularies. | COST: facts.rs:1666 is the write gate; adding or renaming a verdict means editing a const, two `match` guards in findings.rs, four `matches!` guards in adjudication.rs, an anchor.rs settling list, a JSON array in the schema command, and a println of the same list as prose — nine edits the compiler catches none of. The two adjudication.rs partitions (`resolving` 5 values vs `all` 7) must stay disjoint by hand, and discover_cmd publishes the list twice (JSON at :704, text at :750) with nothing tying either to the gate that enforces it. | FIX: `str_enum! { FindingVerdict { Needed => "needed", Justified => "justified", Rejected => "rejected", Deferred => "deferred", Blocked => "blocked", Duplicate => "duplicate", Resolved => "resolved" } }` in model.rs. facts.rs validates with `state.parse::<FindingVerdict>()`; findings.rs, adjudication.rs and anchor.rs match the typed value; discover_cmd emits `FindingVerdict::ALL`; `resolving_verdict` becomes one `matches!` on variants. | SITES: src/commands/diagnostics_cmd/findings.rs:223, src/commands/diagnostics_cmd/findings.rs:232, src/signal/adjudication.rs:68, src/signal/adjudication.rs:110, src/signal/adjudication.rs:124, src/signal/adjudication.rs:144, src/signal/adjudication.rs:188, src/anchor.rs:42, src/anchor.rs:102, src/workitem/mod.rs:797, src/workitem/mod.rs:803, src/commands/discover_cmd.rs:704
[SWEEP] src/store/judgments.rs:19 | reuse | JudgmentKind and JudgmentState hand-write the entire str_enum! surface twice in one file — per-variant `#[serde(rename)]`, `as_str`, `Display`, `FromStr` — so each variant's canonical string is typed four times (five counting the bail! prose). | COST: Six variants across two enums cost 30 literal spellings of 6 strings, all of which are the durable SQLite values and the CLI/JSON wire values (the module's own doc says so at :17-18 and :57-58). Only the serde/as_str pair is exercised by round-trip; a typo in a `from_str` arm compiles and fails at runtime on read. The file also has no `ALL`, so no caller can enumerate the vocabulary the way it enumerates `NodeType::ALL`. | FIX: Move both into model.rs's `str_enum!` (or `pub(crate) use str_enum;` and apply in place): `str_enum! { JudgmentKind { Ratify => "ratify", Reject => "reject", Redefine => "redefine" } }`. That deletes 60 lines and yields as_str + ALL + FromStr + Display + serde from one variant list. The `bail!` prose becomes `JudgmentKind::ALL` joined. | SITES: src/store/judgments.rs:61, src/store/judgments.rs:71, src/store/judgments.rs:80, src/store/judgments.rs:86, src/review.rs:66, src/review.rs:73, src/review.rs:82, src/proofstrength/mod.rs:95, src/proofstrength/mod.rs:106, src/ratification.rs:92, src/ratification.rs:102, src/lane.rs:103
[SWEEP] src/risk.rs:28 | reuse | Thirteen enums pair `#[serde(rename_all = "...")]` with a hand-written `as_str` that re-derives exactly the same strings — two independent mechanisms producing one vocabulary, in twelve modules. | COST: Every variant's canonical string is written twice per enum (once implicitly by rename_all, once literally in as_str), and nothing checks they agree. A variant renamed for readability silently changes the serde wire string while as_str keeps the old one — or vice versa if an as_str arm is edited. lane.rs already carries a deliberate divergence between the two (`Lane::Divergence` serializes as `divergence` but `as_str()` returns `"ratify"`, lane.rs:121-122), proving the two mechanisms are not kept in step. str_enum! at model.rs:45 exists precisely to emit `as_str`, `Display`, `FromStr`, `ALL` and serde from a single literal. | FIX: Route each through `str_enum!` (needs `pub(crate) use str_enum;` from model.rs). Where a variant's wire string must differ from its as_str name, that difference becomes an explicit second method rather than an invisible disagreement between an attribute and a match. | SITES: src/review.rs:20, src/review.rs:65, src/ratification.rs:79, src/lane.rs:23, src/divergence.rs:33, src/batch_auth.rs:28, src/absorb.rs:38, src/truth.rs:23, src/release/mod.rs:116, src/journey/derivation.rs:62, src/journey_runtime/types.rs:151, src/proofstrength/mod.rs:70
[SWEEP] src/grammar.rs:19 | reuse | `ACTIVE_LIFECYCLES` and `ALL_LIFECYCLES` restate the five variants of the `IntentLifecycle` str_enum as raw &str consts, and `loom schema` publishes both spellings side by side in one JSON document. | COST: `IntentLifecycle::ALL` already exists (model.rs:187, generated). The const pair means adding a lifecycle requires editing model.rs plus two consts, and until all three agree `loom schema --json` emits `intent_facets.lifecycle_at_add` and `intent_lifecycle` disagreeing about the same vocabulary in one payload. `check_lifecycle` validates by `contains` on a &str list rather than by parsing into the enum the store then stores. | FIX: Delete both consts. `ALL_LIFECYCLES` becomes `IntentLifecycle::ALL`; `ACTIVE_LIFECYCLES` becomes `IntentLifecycle::ALL.iter().filter(|l| **l != IntentLifecycle::Deprecated)`. `check_lifecycle` becomes `lifecycle.parse::<IntentLifecycle>()` plus the deprecated guard. | SITES: src/commands/intent/create.rs:92, src/commands/intent/create.rs:94, src/commands/intent/mod.rs:17, src/commands/discover_cmd.rs:656, src/commands/discover_cmd.rs:702, src/commands/discover_cmd.rs:735, src/commands/discover_cmd.rs:788
[SWEEP] src/ratification.rs:27 | simplification | `ratification::ASSERTED_STATES` is dead — zero references anywhere in the crate — and is a byte-identical copy of `grammar::RATIFICATION_STATES`, doc comment included. | COST: Two consts hold the same four strings; only one is enforced (store/facts.rs:1684 uses the grammar one). A future editor updating the state list next to the `Ratification` enum it documents will edit the dead one and nothing changes. The doc text was copy-pasted too, so both read as authoritative. | FIX: Delete `ASSERTED_STATES`. If ratification.rs wants a local name, `pub use crate::grammar::RATIFICATION_STATES as ASSERTED_STATES;`. Better still, derive it from the `Ratification` enum (`Ratification::ALL` minus `DeFacto`) once that enum goes through `str_enum!`, since its `as_str` at :92 already spells the same four strings a third time. | SITES: src/grammar.rs:33, src/ratification.rs:92, src/ratification.rs:102, src/store/facts.rs:1684, src/commands/discover_cmd.rs:660, src/commands/discover_cmd.rs:758
[SWEEP] src/maturity.rs:53 | altitude | `Ladder::phase` stores a typed `Lane` as a `String`, and three downstream sites recover the lane by string comparison instead of carrying the enum. | COST: `compass` has the `Lane` in hand (maturity.rs:470 `gate.lane.as_str().to_string()`) and throws the type away; workitem then linearly re-scans every rung comparing `as_str()` to that string to get it back, maturity.rs:358 compares against `Lane::Quality.as_str()`, and orient_cmd's 19-arm `phase_in_plain_english(&str)` has a silent `_ =>` fallback so a renamed lane loses its headline with no compiler error. Its doc comment admits the coupling: "The phase strings are owned by `maturity::compass`; keep this in step with them." | FIX: Make the field `pub phase: Option<Lane>` (None = complete) or add `pub lane: Option<Lane>` beside the string. Then `phase == Lane::Quality`, `rung.lane == gate_lane`, and `phase_in_plain_english(Lane)` becomes an exhaustive match the compiler completes. | SITES: src/maturity.rs:358, src/maturity.rs:470, src/workitem/mod.rs:753, src/commands/orient_cmd.rs:58, src/commands/orient_cmd.rs:122, src/commands/status_cmd.rs:737, src/commands/status_cmd.rs:797
[SWEEP] src/workitem/mod.rs:639 | altitude | The role/lane → TruthAxis mapping is written three times — once typed on `Lane`, once as a raw-&str match in workitem, once inline in orient_cmd's guide table — despite truth.rs's module doc claiming the mapping lives in one place. | COST: truth.rs:14-16 states "Keeping the mapping here (not duplicated per caller) means there is exactly one place that answers 'for this axis, what is the authoritative write…'", and names the three consumers. Two of those consumers instead re-derive which axis a role closes. `axis_for_role` also collapses everything unknown to `Verdict` via `_`, so a new lane silently gets the wrong truth gap stamped on its work packets, and orient_cmd's copy disagrees with lane.rs's already (orient maps `builder`→Implementation; lane.rs has no Build→role bridge at all). | FIX: Give `OwnerRole` an `axis()` (one match, in registry.rs beside the role), or map role→Lane→`Lane::axis()`. `axis_for_role` and orient_cmd's fourth tuple element both call it; the guide table then carries only prose. | SITES: src/lane.rs:165, src/commands/orient_cmd.rs:534, src/commands/orient_cmd.rs:540, src/commands/orient_cmd.rs:546, src/commands/orient_cmd.rs:552, src/commands/orient_cmd.rs:558, src/commands/orient_cmd.rs:564
[SWEEP] src/workitem/queues/roster.rs:124 | reuse | `node_entry` takes the lane as a raw `&str` and re-matches it to derive `owner_role` and `routing_hint`, re-spelling both typed vocabularies — while its sibling module packets.rs uses the shared `hint_judgment()`/`hint_mechanical()` accessors for the identical values 20 times. | COST: Fifteen call sites pass a bare string literal (`node_entry("validate", …)`, `node_entry("build", …)`) where a `Lane` is usually already in scope (line 19 passes `lane.as_str()`, immediately discarding the type), so the function re-parses what the caller already knew. The owner_role arm re-spells six of `OwnerRole`'s seven names and invents a seventh, `"human"`, that `OwnerRole` does not have. `hint_judgment`/`hint_mechanical` exist in the parent module precisely to hold these two strings once, and roster.rs is the only file that ignores them. | FIX: `fn node_entry(lane: Lane, effort: &str, …)`, matching on `Lane` variants; derive `owner_role` from `Lane`/`OwnerRole` (with an explicit `Human` variant or `None` for ratify); replace all eight `Some("judgment".into())`/`Some("mechanical".into())` with `super::super::hint_judgment()` / `hint_mechanical()`. | SITES: src/workitem/queues/roster.rs:102, src/workitem/queues/roster.rs:103, src/workitem/queues/roster.rs:104, src/workitem/queues/roster.rs:207, src/workitem/queues/roster.rs:591, src/workitem/queues/roster.rs:348, src/workitem/queues/roster.rs:406, src/workitem/queues/roster.rs:416, src/workitem/queues/roster.rs:426, src/workitem/queues/roster.rs:442, src/workitem/queues/roster.rs:476, src/workitem/queues/roster.rs:526
[SWEEP] src/cli/subcommands/ops.rs:212 | reuse | Extends the known cli.rs:445 lane/role cluster: `ClaimRoleArg` is a seventh independent spelling of the role vocabulary, with its own hand-written variant→OwnerRole bridge. | COST: The role vocabulary now exists as `registry::OwnerRole` (7 variants + as_str), `cli::RoleArg` (7 + as_str, adds `Monitor`), `cli::subcommands::ops::ClaimRoleArg` (6 + a 6-arm mapping to OwnerRole), `policy::GATEABLE_ROLES` (5 raw strings), `identity::Agent::parse` (6 raw arms), the two byte-identical bail! prose lists at identity.rs:42/:54, roster.rs:136-142 (7 raw strings incl. a non-existent `"human"`), and orient_cmd.rs:530-565 (6 raw arms). Nine sites, one guarding test. Adding a lane means nine edits; the compiler flags one. | FIX: Same design call as the recorded cli.rs:445 finding, extended: give `OwnerRole` `ALL` + `FromStr` (via str_enum!), then derive `ClaimRoleArg`'s mapping, `GATEABLE_ROLES`, `identity::Agent::parse` and its bail prose, and roster/orient's raw arms from that one list. `ClaimRoleArg`'s claimable subset is `OwnerRole::ALL` minus `Sync`. | SITES: src/registry.rs:17, src/registry.rs:30, src/cli.rs:514, src/cli.rs:525, src/policy.rs:67, src/identity.rs:42, src/identity.rs:45, src/identity.rs:54, src/workitem/queues/roster.rs:136, src/commands/orient_cmd.rs:530, src/commands/role_cmd.rs:20
[SWEEP] src/thresholds.rs:87 | simplification | The five threshold gate names are spelled four times in production code — a `GATES` const, `pairs()`, `set_gate`'s match, and `reset_gate`'s match — with no enum, and a test exists solely to guard the agreement. | COST: Adding a gate means four edits plus a struct field, and the failure mode is silent: a copy-pasted arm assigns the wrong field. The test at :430 admits exactly this — its comment names "a flipped match arm (e.g. setting max_nesting when handed max_args)" as the thing it guards. That is a test protecting a duplication rather than a construction that cannot drift, and `loom limits`/`GATES` order is a separate fifth statement of the list. | FIX: `str_enum! { Gate { MaxFileLoc => "max_file_loc", … } }` gives `Gate::ALL` (replacing `GATES`) and `FromStr` (replacing both matches' string arms); `pairs()` becomes `Gate::ALL.iter().map(|g| (g.as_str(), self.get(*g)))` with one `get`/`set` match over the typed gate. Four string lists collapse to one variant list plus two field matches the compiler makes exhaustive. | SITES: src/thresholds.rs:98, src/thresholds.rs:117, src/thresholds.rs:138, src/thresholds.rs:430
[SWEEP] src/commands/capture_cmd.rs:305 | altitude | The "declare a local `const FOO: &[&str]`, `contains` it, bail with `FOO.join("|")`" idiom is copy-pasted eight times across four modules as the crate's ad-hoc substitute for parsing into a typed enum. | COST: Each copy is a private vocabulary invisible to `loom schema`, so the CLI enforces sets no caller can enumerate; the one variant that deviates (capture_cmd.rs:525) writes the list as prose inside the bail instead of joining the const, so its message can drift from the const above it silently. Two of the eight (facts.rs:1694 challenge outcomes, and the ratification list) restate vocabularies that already have a typed home — `review::ChallengeOutcome` at review.rs:66 has as_str *and* FromStr, and the write gate ignores both. | FIX: Where a typed enum exists, parse instead: `state.parse::<ChallengeOutcome>()?` at facts.rs:1694. Where none exists, add one via `str_enum!` and let `FromStr`'s generated `ParseEnumError` carry the message — model.rs:29 already formats `invalid {kind}: '{value}'` for exactly this. That removes eight consts and eight bail! strings. | SITES: src/commands/capture_cmd.rs:522, src/commands/capture_cmd.rs:633, src/store/facts.rs:1666, src/store/facts.rs:1694, src/store/facts.rs:1684, src/commands/intent/create.rs:83, src/commands/intent/create.rs:90, src/commands/intent/create.rs:105, src/commands/edge.rs:26, src/commands/hook_cmd.rs:12, src/workitem/mod.rs:518
[SWEEP] src/limits.rs:5 | altitude | limits.rs claims to be the single list that "cannot drift from enforcement", but at least fourteen enforced caps, timeouts and budgets in src/ are absent from `all()` — including a 2700s step killer, three byte budgets, five co-change caps and four candidate-surface caps. | COST: The registry's own doc says a worker "must never bisect for a cause the runner knew" and that every entry references the constant its enforcing module uses. As it stands `loom limits` shows 17 of ~31 enforced bounds, so exactly the bisection it forbids is what a worker does when a Journey step is killed at 45 minutes, a co-change signal silently reports Unavailable at 1000 commits, or a candidate apply is refused at 128 adjudications. There is no test asserting the registry is complete, so every new cap defaults to invisible. | FIX: Register the missing constants (a `Limit` entry each), and add a completeness guard so a new cap cannot be added silently — the honest one is a naming convention (`MAX_*` / `*_TIMEOUT_SECS` / `*_BYTES` / `*_CAP`) plus a repo grep test that fails when a matching const is not referenced from `limits::all()`. That is the mechanism the doc already promises. | SITES: src/journey/spec.rs:14, src/journey_runtime/types.rs:26, src/journey_runtime/types.rs:27, src/release/mod.rs:110, src/checkpoint.rs:22, src/checkpoint.rs:23, src/signal/debt/git.rs:14, src/signal/debt/cochange.rs:12, src/signal/debt/cochange.rs:13, src/signal/debt/cochange.rs:14, src/signal/debt/cochange.rs:20, src/candidate_surface_policy.rs:25 [BEHAVIOR-CHANGE: skip unless deliberately chosen]
[SWEEP] src/candidate_surface_policy.rs:926 | altitude | The candidate-surface policy enforces four caps — three named consts plus a bare literal 128 — and every one of its violation messages names neither the limit nor its value, which is the single contract limits.rs exists to hold. | COST: limits.rs:4-5 states "the surfaced error must say WHICH limit fired and at what value — a worker must never bisect for a cause the runner knew". `evidence.rs:708` obeys it verbatim (`max_spans={MAX_SPANS}`). This module produces `argv exceeds candidate policy limits`, `nesting exceeds policy limit`, and `adjudication count is outside policy limits` — three refusals a driver cannot act on without reading the source, for four caps `loom limits` does not list. The bare `128` also has no name to put in a message even if one wanted to. | FIX: Name the 128 (`MAX_CONFINED_ADJUDICATIONS`), register all four in `limits::all()`, and interpolate name=value into each bail! the way evidence.rs does. | SITES: src/candidate_surface_policy.rs:556, src/candidate_surface_policy.rs:708, src/journey_gate.rs:651 [BEHAVIOR-CHANGE: skip unless deliberately chosen]
[SWEEP] src/journey_runtime/types.rs:32 | reuse | `EXECUTOR_PLATFORM_ENVIRONMENT` is the crate's named list of process-launch environment essentials, and three other spawn sites hand-write their own overlapping copy of it instead of using it. | COST: Four spellings of "which env vars a hermetic child still needs". They already disagree: checkpoint keeps TMPDIR/TEMP/TMP, release/section_07 does not; the const's non-Windows arm omits SYSTEMROOT/WINDIR/PATHEXT/COMSPEC that both release copies list unconditionally. Adding one variable to the hermetic set means finding four lists, and the cfg-split logic the const encodes is silently lost at each copy site. | FIX: Export the const (it is already `pub`) and have all three sites iterate it, appending only their genuinely local extras — `CARGO_BUILD_JOBS` for section_06, nothing for section_07, the git-identity vars for checkpoint. | SITES: src/checkpoint.rs:849, src/release/section_06.rs:65, src/release/section_07.rs:249 [BEHAVIOR-CHANGE: skip unless deliberately chosen]
[SWEEP] src/cli/subcommands/intent.rs:11 | altitude | `loom intent dependents` is documented as the intent-graph twin of `loom impact`, but its walk depth kept a bare literal 5 with no named default, no maximum, no clamp and no limits entry — the impact-depth hardening was applied only to the code-graph half. | COST: The recent work gave impact depth a named `DEFAULT_IMPACT_DEPTH`, a `MAX_IMPACT_DEPTH` enforced inside the shared `impact_report`, a `loom limits` row, and a `"maximum"` in the MCP schema. The twin walk accepts `--depth 1000000` and runs it. Two commands answering the same question from unrelated, undiscoverable bounds is precisely the drift the impact constants' doc says it exists to prevent. | FIX: Name the default and a maximum beside `Store::dependents` (src/store/edges.rs:762) the way callgraph names them beside the walk, clamp inside `dependents` so every caller inherits the bound, and register the pair in `limits::all()`. | SITES: src/commands/intent/lifecycle.rs:220, src/store/edges.rs:762, src/callgraph.rs:27 [BEHAVIOR-CHANGE: skip unless deliberately chosen]
[SWEEP] src/journey_runtime/types.rs:12 | simplification | A fifteen-line doc block explaining the Journey step timeout — including the literal values 2700s, 900s and 300s — is attached to `STREAM_EXCERPT_BYTES`, because the constant it describes was moved to journey/spec.rs and the comment stayed behind. | COST: The 2700 now exists twice: as prose in one module and as the const in another, with nothing linking them. Worse, `rustdoc` and every reader now attribute a wall-clock-timeout rationale to a 512 KiB byte budget; the actual constant at journey/spec.rs:14 carries no explanation at all, so the next person to tune it has no record of why it is 2700 and the stale prose here will not be updated when they do. | FIX: Move the doc block (lines 12-25) onto `DEFAULT_JOURNEY_TIMEOUT_SECONDS` in src/journey/spec.rs:14, drop the literal values from the prose in favour of referring to the constant, and give the two byte budgets their own one-line docs. | SITES: src/journey/spec.rs:14, src/journey_runtime/execute.rs:605
[SWEEP] src/commands/status_cmd.rs:306 | efficiency | `loom status` builds the FULL graph snapshot five times — `snapshot()` is the whole-graph read (nodes + edges + facts + one `evidence_for` query per fact + all facets + tags + meta), and five independent callers each build their own private copy on one command path. | COST: On this repo's own graph that is 5 × (1,398 nodes + 1,688 edges + 1,782 facts + **1,782 per-fact `evidence_for` point queries** + 4,788 facets) ≈ 8,900 SQLite point queries and ~48,000 rows materialised to render one status screen. The recorded symptom is already in the campaign notes ("status ~4.5s debug"). Worse, nothing in the code says a snapshot is expensive, so every new detector added to `signal/` silently adds a sixth full read. | FIX: Build the snapshot once at the command boundary and thread `&Snapshot` down. `signal::smells`, `signal::doctor`, `signal::debt` and `layer_detector_state` already do all their work off `snap` and nothing else — change them to take `&Snapshot` and add thin `fn smells(store)` wrappers for the standalone callers. `maturity::LadderInputs::gather` then builds one snapshot and passes it to both `signal::smells` and `audit::backlog`. | SITES: src/signal/debt.rs:56, src/signal/doctor.rs:21, src/signal/smells.rs:28, src/maturity.rs:206, src/audit.rs:718, src/audit.rs:742, src/sync.rs:776, src/commands/diagnostics_cmd/advisory.rs:5, src/commands/diagnostics_cmd/findings.rs:241, src/commands/domain_cmd/layers.rs:4, src/commands/domain_cmd/vocab.rs:56, src/travel.rs:142
[SWEEP] src/store/facts.rs:1083 | efficiency | `reverify_all` re-reads and re-hashes the cited file once per EVIDENCE ROW, so a file cited by many facts is opened dozens of times in one sync pass — even though the same function already carries a `SuccessorCache` proving the caching idea was understood here. | COST: On this repo's own graph the pass performs 1,268 span reads + 1,030 covered-file reads = 2,298 `read_to_string` + FNV passes over only ~183 distinct files (12.5x redundancy). `src/commands.rs` alone is opened and hashed 75 times (52 covered + 23 span) and `src/checkpoint.rs` 79 times, each a 31–42 KB read. This is the hot loop of every `loom sync`, and the redundancy grows linearly with how well-grounded the graph is — the better the repo is covered, the slower sync gets. | FIX: Hoist a `FileCache` beside `SuccessorCache` in `reverify_all`: `BTreeMap<String, Option<Rc<String>>>` keyed by root-relative path, threaded into `recheck` exactly as `successors` already is. `covered_intact` takes the cache (or a `&dyn Fn(&str) -> Option<&str>`) instead of hitting the filesystem itself. One pass, one read per distinct file. | SITES: src/store/facts.rs:1472, src/store/facts.rs:1541, src/store/facts.rs:1565, src/store/facts.rs:121, src/runner.rs:200, src/runner.rs:254, src/runner.rs:442
[SWEEP] src/locator.rs:285 | efficiency | `scan_markers` reads EVERY registered CodeFile off disk and line-splits it, and `resolve_anchor` calls it fresh on every single anchor resolution — so resolving one anchor costs a full read of the whole registered corpus, and `plan_anchor_staleness` does it once per anchor-form locator edge. | COST: One `resolve_anchor` = 198 `read_to_string` calls + 198 full line scans (~3.5 MB) on this repo, to locate one marker line. `sync::run` calls `plan_anchor_staleness` (sync.rs:678 → `validate_for_codefile` → `resolve_anchor`) once per anchor-form locator, so the cost is O(anchors × codefiles) reads per sync — the corpus is re-read from scratch for each anchor. This repo currently has zero anchor locators so it is latent, but it is the designed growth path (`loom anchor` issues them) and it scales quadratically. | FIX: Scan markers once per pass and pass the occurrence list down: give `resolve_anchor` a variant taking `&[MarkerOccurrence]`, have `plan_anchor_staleness` call `scan_markers` once for the whole loop, and cache it in the same place `SuccessorCache` lives conceptually. `scan_markers` also does not need `extract` — it is a pure line-prefix test, so it can stream lines instead of holding whole files. | SITES: src/locator.rs:111, src/locator.rs:127, src/locator.rs:175, src/locator.rs:245, src/locator.rs:703, src/sync.rs:678, src/commands/diagnostics_cmd/impact.rs:24
[SWEEP] src/packs.rs:85 | efficiency | `recommended_packs` performs a full recursive directory walk (depth 6, every file's extension classified into a counting map) and the ONLY thing it reads back out of that map is one `contains_key("sql")`. | COST: A whole-tree filesystem walk to answer a single boolean. On this repo that is thousands of `read_dir` entries and a `BTreeMap` of 6 counters, of which 5 are computed and discarded. It is invoked from `maturity::LadderInputs::gather` (:249) and `workitem/queues/predicates.rs:243` — i.e. from the `loom status` / compass path — so on a repo with no seeded rules `loom status` walks the entire tree for one `sql` check. | FIX: Replace `count_exts(root, &mut langs, 0)` with a short-circuiting `fn has_ext(root, "sql") -> bool` that stops at the first hit (or, better, take the language census as a parameter from the one caller — `detect_cmd` — that actually needs the counts, per the `count_exts` duplication finding above). | SITES: src/maturity.rs:249, src/workitem/queues/predicates.rs:243, src/commands/discover_cmd.rs:560
[SWEEP] src/coverage.rs:68 | efficiency | `matching_ignore_rules` re-queries the `ignores` meta row, re-parses its JSON, and compiles a fresh single-glob `GlobSet` PER RULE on every call — and it is called inside a per-codefile loop that has already fetched the same rule list two lines earlier. | COST: Each call = 1 SQLite `get_meta` + 1 `serde_json::from_str` + N `GlobSetBuilder::build()` compilations (6 rules on this repo). `contracts/build.rs:105` loops the codefile roster calling it per neighbour, so one coverage packet pays that whole cost per neighbouring file — while `ignore_precedents` fetched at :100 sits right there unused for the test. `coverage.rs:169` compounds it: it builds N one-glob GlobSets and then linear-scans them with `.position()`, which is exactly what a single `GlobSet::matches()` already does in one pass. | FIX: Give `matching_ignore_rules` a `&[IgnoreRule]` + prebuilt `GlobSet` form: `fn matching_rules<'a>(rules: &'a [IgnoreRule], set: &GlobSet, path: &str) -> Vec<&'a IgnoreRule>` using `set.matches(path)` (which returns the matching indices), and have the store-touching wrapper build both once. `contracts/build.rs` then compiles once outside its loop, and `coverage_scope_summary` drops its per-rule matcher Vec. | SITES: src/coverage.rs:169, src/workitem/contracts/build.rs:109, src/commands/codefile_cmd.rs:569
[SWEEP] src/callgraph.rs:94 | efficiency | `callgraph::build` issues three separate facet point-queries per CodeFile (`symbols_of`, `test_symbols_of`, `calls_of`) plus three `serde_json` parses, in a loop over the whole roster — an N+1 with no bulk facet read available in the store API. | COST: 594 `SELECT value FROM facet WHERE target_id=? AND target_kind=? AND key=?` round trips + 594 JSON parses per build, on this repo. `build` is called from seven places (`divergence`, `risk`, `absorb`, `proofstrength`, `journey/compile`, `proof_cmd/support`, `impact`), and `proofstrength::recompute` runs inside every `loom sync` — so a single sync pays it at least once and `loom impact` pays it in addition to two separate `store.codefiles()` scans in the same function. | FIX: Add `Store::facets_by_key(key: &str, kind: TargetKind) -> Result<BTreeMap<String, String>>` (one `SELECT target_id, value FROM facet WHERE key=?1 AND target_kind=?2`) beside the existing `nodes_where_facet`, and have `build` fetch the three maps once and index them. Same primitive removes the per-codefile `get_facet` in `workitem/queues/predicates.rs:160`. | SITES: src/divergence.rs:85, src/risk.rs:115, src/absorb.rs:127, src/proofstrength/mod.rs:476, src/journey/compile.rs:559, src/commands/proof_cmd/support.rs:286, src/commands/diagnostics_cmd/impact.rs:22, src/workitem/queues/predicates.rs:160
[SWEEP] src/evidence.rs:644 | efficiency | The citation loop deliberately caches the extraction per file (`symbol_cache`, with a comment saying so) but leaves the `read_to_string` and the `content.lines()` Vec inside the loop, so a file cited five times is still read from disk and re-split five times. | COST: The comment at :641 asserts the per-file invariant the code only half implements — the expensive AST extraction is cached, the I/O feeding it is not. A verdict citing ten spans in one file does ten reads and ten full `Vec<&str>` line splits of that file. It also means the cache and the read can observe different content if the file changes mid-loop. | FIX: Key one `BTreeMap<String, (String, Vec<Symbol>)>` on the file and populate content + symbols together in the same `or_insert_with`, so the doc comment at :641 becomes true of the read as well as the extraction. | SITES: src/evidence.rs:683, src/evidence.rs:714
[SWEEP] src/commands/domain_cmd/vocab.rs:56 | efficiency | Builds the entire graph snapshot — nodes, edges, facts, one `evidence_for` query per fact, all facets — and immediately discards everything but `.tags`, of which this graph has exactly one. | COST: ~1,782 extra SQLite point queries and ~9,700 discarded rows to read a single tag table that `Store` already reads with the private one-statement `list_all_tags`. It is inside a rename path, so the cost is paid per `vocab rename` invocation. | FIX: Make `list_all_tags` (`src/store/facets.rs:222`) `pub` (or add `pub fn tags(&self) -> Result<Vec<Tag>>` beside the existing `tags_of`/`nodes_with_tag`) and call that. | SITES: src/store/facets.rs:137, src/store/facets.rs:148, src/store/facets.rs:222
[SWEEP] src/commands/diagnostics_cmd/impact.rs:34 | efficiency | `impact` reads the full CodeFile roster twice in one function — once to linear-search for the target by name, once to build the name→node map it then uses — on top of the `callgraph::build` at :22 that already walked the same roster. | COST: Three full roster reads per `loom impact`; the first is a `list_nodes` + `O(n)` `find` that the map built 25 lines later would answer in `O(1)`. The identical shape is in `coverage_scope_summary`, which calls `unowned_codefiles` (itself a `codefiles()` scan) and then `codefiles()` again. | FIX: Build `codefiles_by_name` once at the top and resolve the target through it; in `coverage.rs`, pass the already-fetched roster into `unowned_codefiles` (or have it take `&[Node]`). | SITES: src/coverage.rs:98, src/coverage.rs:176, src/commands/diagnostics_cmd/impact.rs:59
[SWEEP] src/commands/status_cmd.rs:782 | altitude | `require_lane` re-implements the store's own lane gate at the command plane — a byte-for-byte copy of `Store::check_lane`, including its bail text, even though `check_lane` is already `pub(crate)` and reachable. | COST: The authority rule that decides which lane may write which facts now has three implementations. The store's copy is the one every write actually passes through; the command-plane copy is a preflight that can silently stop agreeing with it (a change to `OwnerRole::satisfies` semantics, a new Solo-like agent, a reworded refusal). Five command call sites take the copy and never learn about a store-side change; the error string is duplicated so a fix to the message lands in one surface only. | FIX: Delete `commands/status_cmd.rs:782` and have its five callers (capture_cmd.rs:388, judgment_cmd.rs:256, intent/update.rs:5, intent/lifecycle.rs:7, intent/lifecycle.rs:102) call `store.check_lane(owner)` directly — same match, same message, one implementation. Then `check_grounding_write` becomes the only remaining variant and its Fixer exception is the only thing that needs justifying. | SITES: src/store/open.rs:462, src/store/open.rs:444, src/commands/capture_cmd.rs:388, src/commands/judgment_cmd.rs:256, src/commands/intent/update.rs:5, src/commands/intent/lifecycle.rs:7, src/commands/intent/lifecycle.rs:102
[SWEEP] src/store/open.rs:479 | altitude | The `Implements`-edge lane exception is bolted onto three separate call sites as `if kind == EdgeKind::Implements { check_grounding_write() } else { check_lane(spec(kind).owner) }`, because `EdgeKindSpec` can only express a single `owner` and has no room for a second permitted lane. | COST: The registry doc at registry.rs:8 says "Endpoint types, allowed truth classes, and the owning role all live here, in one place" — but one edge kind's real authority rule lives outside it, restated at every gate. A fourth gate (a new edge-write path, or a second kind that a lane may co-write) must remember the `if` or it silently applies the wrong owner; the compiler cannot see the omission because `check_lane` accepts any `OwnerRole`. | FIX: Give `EdgeKindSpec` the missing field — `also_writable_by: &'static [OwnerRole]` (empty for every kind except `Implements`, which lists `Fixer`) — and collapse all three branches plus `check_grounding_write` into a single `check_lane(spec)` that consults it. The special case becomes registry data, and every gate site reduces to one unconditional call. | SITES: src/store/edges.rs:24, src/store/edges.rs:484, src/store/edges.rs:246, src/store/edges.rs:408, src/store/open.rs:444
[SWEEP] src/checkpoint.rs:883 | reuse | The byte-capped, timeout-controlled git invocation harness exists twice — checkpoint.rs and signal/debt/git.rs — with two identical 16 MiB caps under two names, two identical 10 s timeouts, two identical stdout-filter closures, and two identical post-run cap re-checks; neither goes through `src/subprocess.rs`, the crate's process chokepoint. | COST: subprocess.rs's module doc makes it the one place that owns capture bounding (`KEEP_BYTES`, head+tail window, timeout, process-group termination); these two modules re-derive a weaker version of it and then diverge in what a cap means (checkpoint errors, cochange returns `Unavailable`). The two 16 MiB constants and the two 10 s timeouts are already independent values that happen to agree — and only one of the four is discoverable: `signal::GIT_TIMEOUT_SECS` is in `limits::all()` at limits.rs:132 while `checkpoint::GIT_TIMEOUT` (same value, same job) is not. A third hardened-git policy in release/section_07.rs:247 clears a *different* set of environment keys than checkpoint.rs's, so "how loom hardens a git child" now has three answers. | FIX: One `subprocess::run_capped(cmd, timeout, cap) -> Result<CappedOutput>` that owns the filter closure, the timeout, and the cap re-check, plus one `GIT_OUTPUT_CAP` / `GIT_TIMEOUT` pair registered in `limits::all()`. checkpoint.rs and signal/debt/git.rs then differ only in how they map a cap hit to their own failure type. Fold the env-hardening key list into one `git_hardened_command(root)` used by all three call sites. | SITES: src/signal/debt/git.rs:79, src/signal/debt/git.rs:14, src/signal/debt/git.rs:13, src/checkpoint.rs:22, src/release/section_07.rs:243, src/subprocess.rs:37, src/limits.rs:132
[SWEEP] src/mcp.rs:247 | altitude | Two MCP tools — `loom_absorb` and `loom_journal` — implement command logic inside the transport module, against the file's own stated contract; `loom_absorb` even re-derives the `ready`/`needs_you` split that `diagnostics_cmd::absorb_cmd` already computes. | COST: mcp.rs:3-6 states "Every tool here is a thin wrapper over the SAME function the CLI calls; this module owns no truth and makes no decisions. If a tool and its CLI twin can ever disagree, the wrapper is wrong." Nine of eleven tools honour that (`status_value`, `next_output`, `served_context`, `impact_report`, `observe_run`, `apply_value`); these two do not. `ready`/`needs_you` is now defined in two places, so an MCP client and a `--json` CLI caller can be told different numbers about the same working tree. `loom_journal` additionally re-inserts `origin` by hand (mcp.rs:292-294) purely because `Entry`'s `#[serde(skip_serializing_if = "Origin::is_local")]` at journal.rs:71 hides it — a serialization decision patched in the transport instead of at the model, so any future journal reader must remember the same patch. | FIX: Add `absorb_value(&Store) -> Result<Value>` beside `absorb_cmd` in diagnostics_cmd/impact.rs and export it through the `commands` in-band list at commands.rs:47-59, exactly like `impact_report`; the MCP call becomes one line and the CLI renders from the same value. For `loom_journal`, add a `journal::tail(root, limit, event) -> Result<Vec<Value>>` (or a view struct that serializes `origin` unconditionally) so the filter/tail/origin rules live in journal.rs, where the format is owned. | SITES: src/mcp.rs:280, src/commands/diagnostics_cmd/impact.rs:329, src/journal.rs:71, src/commands.rs:47
[SWEEP] src/commands.rs:613 | altitude | `query_terms` — a pure text helper with exactly one consumer, `discover_cmd::search`— lives in the dispatcher module alongside the routing table, together with its stopword list and its unit test. | COST: It is the only `pub(crate)` item in commands.rs with a single external caller (every other shared helper there — `open_read`, `node_json`, `pagination_envelope`, `page_footer`, `resolve_root` — has 13 to 82). Keeping it here means the dispatcher owns query-normalization vocabulary (a 29-word stopword list, a 3-char floor, a 2-char fallback floor) that only search uses, and the module cannot be read as "routing plus the helpers everyone shares". | FIX: Move `query_terms`, its `STOP` list and its test into `commands/discover_cmd.rs` next to its only caller — or, if it is wanted crate-wide, into `src/text.rs`, the pure-text module the repo just created for exactly this displacement. The already-recorded duplicate-pipeline cleanup at commands.rs:624 should be applied in the new home rather than here. | SITES: src/commands/discover_cmd.rs:20
[SWEEP] src/candidate_surface_policy.rs:553 | altitude | Three enforced caps in candidate_surface_policy (and checkpoint's git output cap) bail on violation but are absent from `limits::all()`, and their refusals do not name the limit or its value — the exact contract limits.rs claims to hold. | COST: limits.rs:5-7 says "the surfaced error must say WHICH limit fired and at what value — a worker must never bisect for a cause the runner knew. This registry is the single list `loom limits` renders; every entry references the constant its enforcing module uses, so the list cannot drift from enforcement." It has drifted at four more constants than the one already recorded. An author whose surface is refused with "argv exceeds candidate policy limits" cannot learn the threshold from the message, from `loom limits`, or from anything short of reading the source — and cannot tell whether it was the token count or the byte count that fired. | FIX: Make the three `candidate_surface_policy` constants `pub`, add `Limit` entries for `max_argv_tokens`, `max_argv_bytes`, `max_operation_nesting` and `git_output_cap_bytes` referencing them, and reword the bails to name the constant and the observed value (`argv tokens {n} > max_argv_tokens={MAX_ARGV_TOKENS}`), as the registry already requires. Same treatment as the recorded `MAX_ADVERSARIAL_REVIEW_FRONTIER` gap. | SITES: src/candidate_surface_policy.rs:707, src/checkpoint.rs:902, src/checkpoint.rs:22, src/policy.rs:34 [BEHAVIOR-CHANGE: skip unless deliberately chosen]
[SWEEP] src/store/mod.rs:91 | reuse | `PORTABLE_META_KEYS` re-spells as bare literals three keys that already have named constants elsewhere, and the other five keys in the list have no constant at all — so "is this setting portable?" is answered by string matching across seven files. | COST: policy.rs:22 and thresholds.rs:17 both carry a prose comment telling the reader the key must also appear in `PORTABLE_META_KEYS`, which is precisely the acknowledgement that nothing in code ties them together. The allowlist is the entry that must match exactly or the setting silently stops travelling in the export; a renamed constant compiles fine and drops the key from every import. The recorded `layer_order` finding is one instance of this — the full shape is that five of the eight portable keys (`layer_order`, `ignores`, `codefile_globs`, `observed_globs`, `upstream_graphs`) are bare literals at their writers as well. | FIX: Give every portable meta key a `pub const` beside its reader/writer (`scan::ADAPTERS_META_KEY`, `thresholds::THRESHOLDS_META_KEY` and `policy::EVIDENCE_POLICY_META_KEY` already exist; add the missing five) and build `PORTABLE_META_KEYS` from those constants instead of from string literals. The prose reminders in policy.rs:22 and thresholds.rs:17 then become unnecessary. | SITES: src/scan.rs:17, src/thresholds.rs:18, src/policy.rs:23, src/store/facets.rs:180, src/commands.rs:650, src/travel.rs:388, src/commands/domain_cmd/layers.rs:21, src/signal/smells.rs:891
[SWEEP] src/workitem/contracts/quality.rs:71 | reuse | The `loom rule verdict` write-back template is spelled twice, byte-identically, once inside `verdict_write_back` (the function that exists to own write-back strings) and once inline in `quality_contract_body`. | COST: contracts/mod.rs:25-28 documents `verdict_write_back` as "The exact re-record command that closes THIS edge's verdict" — it is the designated owner of this vocabulary. The quality contract bypasses it because the unmeasured-pair path has no `Edge` to pass, so the fix was a copy rather than a signature change. The two copies are the command an agent is told to run: if one gains a flag or drops `--confidence`, half of loom's packets instruct a command the other half does not. | FIX: Split `verdict_write_back` so the Governs arm delegates to `pub(super) fn rule_verdict_write_back(rule: &str, intent: &str) -> String`, and call that from quality.rs:71. One template, both callers, no `Edge` required. | SITES: src/workitem/contracts/mod.rs:32
[SWEEP] src/release/section_08.rs:301 | reuse | The recursive "sort every JSON object key" canonicalizer is written five times under five names in five modules, and every copy feeds a hash that other modules compare against. | COST: Five independent spellings of one hashing precondition. `release/section_03.rs:185` and `section_04.rs:370` compare a digest against one minted by `commands/journey/derive.rs:117`; those two digests only agree because two separately-typed canonicalizers happen to sort identically today. Change one (add float normalization, drop nulls, sort arrays) and release verification starts rejecting manifests it approved, with no compiler signal. `completeness/mod.rs:221` is a sixth variant that takes `&Value` and clones instead of consuming. | FIX: Keep ONE — `journey_runtime/values.rs:496 canonicalize` is the best survivor: it is already `pub(crate)`, already consumes by value, and already has a caller outside its own module (`journey_runtime/compile.rs:467`). Promote it to a crate-level `crate::canonical::json(Value) -> Value` and delete the other four. `completeness/mod.rs:221` becomes `canonicalize(value.clone())`. | SITES: src/commands/journey/derive.rs:1158, src/journey/spec.rs:620, src/journey_runtime/values.rs:496, src/completeness/mod.rs:221
[SWEEP] src/release/section_02.rs:112 | reuse | The manifest-hash recipe `fingerprint(to_string(canonical_json(to_value(x))))` is hand-inlined at five release sites even though `commands/journey/derive.rs:117 derivation_manifest_hash` is exactly that function, and the release sites compare their result against the hash that function minted. | COST: Six copies of a cross-module digest contract. `section_03.rs:186` bails with "manifest changed after approval" and `section_04.rs:370` bails with "changed before replay" purely on this recipe agreeing byte-for-byte with `derive.rs:117`. Nothing in the type system ties them; a pretty-print switch, a trailing newline, or `to_vec` instead of `to_string` in any one site silently converts every approved release into a forged one. | FIX: Export one `pub(crate) fn manifest_hash<T: Serialize>(value: &T) -> Result<String>` (the body of `derive.rs:117`, which already has the canonical form and the name) and call it from all six sites. `derive.rs:117 derivation_manifest_hash` should survive — it is the site that MINTS the hash the others verify. | SITES: src/release/section_03.rs:184, src/release/section_04.rs:91, src/release/section_04.rs:368, src/release/section_06.rs:486, src/commands/journey/derive.rs:117, src/commands/journey/derive.rs:442
[SWEEP] src/store/codec.rs:13 | reuse | The FNV-1a 64-bit schedule is re-typed five times across three modules — extending the known artifact.rs pair with a TreeHasher and a second fingerprint_bytes in release/section_08.rs and fnv_hex_digest in store/codec.rs. | COST: Five hand-copies of the two magic constants that define every content-addressed id in the crate: derived node ids (`codec.rs:31 derived_id`), debt cluster ids (`signal/debt.rs:50`), artifact fingerprints, and the release tree hash. `release/section_08.rs:292 fingerprint_bytes` is functionally identical to the already-public `crate::artifact::fingerprint_bytes` and duplicates it for no reason; the same file then writes the schedule a second time as `TreeHasher`. A typo in any one constant produces a silently different id plane that the test at `signal/debt.rs:170` would only catch for one of the five. | FIX: One `crate::artifact::Fnv1a` streaming hasher with `write(&[u8])`/`finish() -> String`; `fingerprint`, `fingerprint_bytes`, `fnv_hex_digest`, and `TreeHasher` all become thin wrappers over it. `release/section_08.rs:292` should just call `crate::artifact::fingerprint_bytes` and be deleted outright. | SITES: src/release/section_08.rs:292, src/release/section_08.rs:216, src/artifact.rs:18, src/artifact.rs:30
[SWEEP] src/checkpoint.rs:1100 | reuse | "Is this a normalized, confined relative path?" is hand-written six times across six modules with five different reserved-component lists — and checkpoint.rs's copy is a near byte-identical twin of journey/surface_setup.rs's. | COST: This predicate is the confinement boundary: it decides whether a caller-supplied string can reach `.loom`, `.git`, `target`, or `../`. Six copies means six chances to miss one component form, and they already disagree — `checkpoint.rs:1121` and `surface_setup.rs:219` reserve `.loom|.git`, `candidate_surface_policy.rs:1080` reserves `.git|.loom|target` but only at the FIRST component, `release/section_08.rs:17` uses an `INVENTORY_RESERVED_COMPONENTS` const, and `store/open.rs:193` and `release/section_01.rs:223` reserve nothing. Hardening one leaves the other five soft, and no test can prove the set is complete because there is no set. | FIX: One `crate::fsglob::confined_relative(raw: &str, reserved: &[&str]) -> Result<&Path>` carrying the full five-part predicate (non-empty, trimmed, no backslash, not absolute, all `Component::Normal`, round-trips through the `/`-join). `checkpoint.rs:1100 validate_fixture_path` should survive as the body — it is the strictest and the only one that checks the normalized round-trip AND the reserved set; the other five become calls with their own reserved list and their own error label. | SITES: src/journey/surface_setup.rs:195, src/candidate_surface_policy.rs:1061, src/store/open.rs:193, src/release/section_01.rs:223, src/release/section_08.rs:1, src/commands/hook_cmd.rs:33 [BEHAVIOR-CHANGE: skip unless deliberately chosen]
[SWEEP] src/completeness/mod.rs:58 | altitude | "Is this Validation a compiled-Journey proof?" — the gate that decides whether a proof is compiler-owned — is hand-inlined at eight sites in eight modules, spelled four different ways, with no shared predicate. | COST: Eight independent readings of one truth-class rule, written as raw string comparison against a JSON body key. Four different accessor spellings (`|value| value.as_str()`, `|v| v.as_str()`, `Value::as_str`, `serde_json::Value::as_str`) make them un-greppable as one thing. Three sites additionally require `profile == "proof"` and three do not, so "compiler-owned" means something slightly different in each module — and none of the eight can be changed with confidence that the other seven agree. | FIX: One `pub fn is_compiled_journey_proof(validation: &Node) -> bool` (and a `..._for_profile` variant) in `src/journey/mod.rs`, beside `JOURNEY_SCHEMA`. Every site calls it. `proofstrength/entries.rs:362 compiled_journey_proves_edge` should survive as the strict form and delegate its first clause to the new predicate. | SITES: src/sync.rs:429, src/proofstrength/mod.rs:270, src/proofstrength/entries.rs:366, src/commands/proof_cmd/support.rs:220, src/journey_exercises.rs:436, src/journey/compile.rs:284, src/journey/compile.rs:607, src/commands/journey/runtime.rs:264
[SWEEP] src/workitem/queues/roster.rs:211 | altitude | roster.rs and packets.rs independently re-derive the reason string and Target for the SAME queue candidate from the SAME predicates — 20 literals are duplicated verbatim between the two files. | COST: Every lane's "why this item" sentence and its Target shape exist twice: once for the roster `loom status` prints and once for the packet `loom next` serves. They are the same item selected by the same predicate from `queues/predicates.rs`, so the two must agree, and nothing makes them. Editing a reason in one file silently makes `loom status` describe an item differently from the packet a driver receives for it. The rule/intent-pair block below is duplicated character-for-character across 8 lines including the Target literal. | FIX: Have each `predicates.rs` selector return the candidate WITH its reason and Target (a `QueueCandidate { reason, target, effort }`), and have both roster and packet consume it. roster's `node_entry`-based arms should survive as the shape; the packet builders add only `prompt_contract` and `context` on top. | SITES: src/workitem/queues/packets.rs:236, src/workitem/queues/packets.rs:258, src/workitem/queues/packets.rs:330, src/workitem/queues/packets.rs:349, src/workitem/queues/packets.rs:363, src/workitem/queues/packets.rs:390, src/workitem/queues/packets.rs:413, src/workitem/queues/packets.rs:497, src/workitem/queues/packets.rs:509, src/workitem/queues/packets.rs:561, src/workitem/queues/packets.rs:630, src/workitem/queues/packets.rs:730
[SWEEP] src/commands/journey/support.rs:92 | simplification | Two different `pub(crate) fn node_json(&Node) -> Value` exist with different output shapes, and the journey module's glob import silently shadows the crate-wide one — so the same JSON key carries a 6-field node in journey handlers and a 9-field node everywhere else. | COST: `commands/journey/mod.rs:7` does `pub(crate) use super::*;` (bringing in `commands::node_json`) and `mod.rs:32-34` then re-exports `support::node_json`, which wins. Result: `"intent": node_json(&intent)` at `derive.rs:262` emits no `truth_class`, `created_at`, or `updated_at`, while `"finding": node_json(&finding)` at `diagnostics_cmd/findings.rs:105` does. Same CLI, same key name, two contracts, and a reader has no way to tell which one a call site resolved to. | FIX: Keep `commands.rs:541 node_json` as the single node serializer — it carries `truth_class`, which is the crate's central concept and should never be dropped from a node envelope. Delete `support.rs:92`; if a trimmed shape is genuinely wanted, name it `node_json_brief` so the difference is visible at the call site. | SITES: src/commands.rs:541, src/commands/journey/mod.rs:32, src/commands/journey/derive.rs:262, src/commands/journey/registry.rs:160, src/commands/diagnostics_cmd/findings.rs:105 [BEHAVIOR-CHANGE: skip unless deliberately chosen]
[SWEEP] src/journey_runtime/types.rs:32 | altitude | `EXECUTOR_PLATFORM_ENVIRONMENT` is a named public const for the process-launch env allowlist, and three other modules re-type the same list inline with three different memberships. | COST: The one place the crate says "these are the environment variables a hermetically-launched child needs" is a const nobody outside `journey_runtime` uses. `checkpoint.rs:849` re-lists it with 8 names, `release/section_07.rs:249` with 5, `release/section_06.rs:65` with 6 (adding CARGO_BUILD_JOBS). A platform fix — say Windows needs `PROGRAMDATA` — lands in one list and the other three keep breaking. The same fragmentation applies to the git hardening flags: `GIT_CONFIG_NOSYSTEM`/`GIT_TERMINAL_PROMPT`/`GIT_CONFIG_GLOBAL` are set at three sites with three different combinations, and `signal/debt/git.rs:67` sets none of them. | FIX: Make `EXECUTOR_PLATFORM_ENVIRONMENT` a crate-level const (it already has the right doc and the `#[cfg(windows)]` split the others lack) and have all three inline lists consume it. Add a sibling `GIT_HERMETIC_ENVIRONMENT: &[(&str, &str)]` for the git flags; `checkpoint.rs:866-871` should survive as its content — it is the most complete. | SITES: src/checkpoint.rs:849, src/release/section_07.rs:249, src/release/section_06.rs:65, src/signal/debt/git.rs:67 [BEHAVIOR-CHANGE: skip unless deliberately chosen]
[SWEEP] src/commands/proof_cmd/support.rs:24 | altitude | Two different functions named `covered_hashes` both build the `RunRecord.covered` file→fingerprint map, with opposite policies for an unreadable file — one stores the empty hash, the other aborts. | COST: `runner.rs:178-180` documents the empty-hash rule as load-bearing ("absence is a state, not a gap") and `runner.rs:200 covered_intact` re-verifies with the same `unwrap_or_default()`. But `proof_cmd/support.rs:32` mints the same map with a hard error on an unreadable file. So a proof recorded through the command path can never contain the empty hash that the recheck path is built to interpret, and the two callers of the same field are governed by two policies with one name. Whichever a reader lands on, they will mis-predict the other. | FIX: One `pub fn covered_hashes(root: &Path, files: &[String]) -> BTreeMap<String,String>` in `runner.rs` — the documented empty-hash policy should survive, because `runner.rs:200 covered_intact` is the only reader of the field and is written against it. `proof_cmd/support.rs:24` keeps only the `store.files_grounding` lookup and delegates the hashing. | SITES: src/runner.rs:181, src/runner.rs:200, src/runner.rs:134, src/commands/proof_cmd/support.rs:185 [BEHAVIOR-CHANGE: skip unless deliberately chosen]
[SWEEP] src/store/mod.rs:157 | simplification | A self-deleting temp-directory test guard is written eight times across eight modules under four names — four of the copies are byte-identical, and one carries a doc comment admitting the duplication. | COST: `thresholds.rs:313` literally documents the problem ("same shape as the integration-test `Tmp` and the store module's own `TmpRoot`") and then adds a ninth variant anyway. The copies have already drifted in ways that matter for parallel test runs: four use a shared `AtomicU64` counter for uniqueness, `divergence.rs:576` and `store/facts.rs:1976` use only pid+nanos (collidable under `cargo test` threads), `evidence.rs:948` uses only the pid (guaranteed to collide across two tests in the same binary). `divergence.rs:586` removes the directory but never creates it, `scan.rs:819` returns `Result`, the rest `unwrap`. | FIX: One `pub(crate) struct TempRoot` behind `#[cfg(test)]` in a `src/testsupport.rs`, with `new(prefix)`, `path()`, `write(rel, text)` and the counter-based unique name. `store/schema.rs:587` / `store/open.rs:579` / `store/mod.rs:157` / `fsglob.rs:113` are the identical four and should survive as its body; the other four call it with their prefix. | SITES: src/store/schema.rs:587, src/store/open.rs:579, src/fsglob.rs:113, src/thresholds.rs:315, src/batch_auth.rs:752, src/divergence.rs:574, src/store/facts.rs:1973, src/scan.rs:816, src/prescan.rs:174, src/evidence.rs:945
[SWEEP] src/store/nodes/ratify.rs:163 | simplification | Four `Store` methods differ only in name — `ratify_intent`/`ratify_pattern` and `ratify_intent_from_human`/`ratify_pattern_from_human` have byte-identical bodies, and the intent/pattern distinction the names promise is enforced nowhere. | COST: The API advertises a node-type constraint it does not have: `ratify_pattern(intent_id, ..)` and `ratify_intent(pattern_id, ..)` are the same call. A reader auditing "can a pattern be ratified through the intent path?" has to read both bodies to learn there is no difference, and a future type check added to one silently leaves the other as a bypass. Four public methods where two would do. | FIX: Keep `ratify_from_human(id, evidence, decision)` and `ratify(id, evidence, presence)` (drop the subject-type suffix, since the body has none) — or, if the distinction is meant to be real, add the `node_type` assertion to each and the names stop lying. `ratify_intent_from_human` should survive as the name of the delegating form; it has six call sites to `ratify_pattern_from_human`'s one. | SITES: src/store/nodes/ratify.rs:139, src/store/nodes/ratify.rs:144, src/store/nodes/ratify.rs:168, src/commands/pattern_cmd.rs:190
[SWEEP] src/commands/journey/derive.rs:384 | reuse | `command_relationship_path_exists` is a byte-identical copy of `journey/derivation.rs:270 relationship_path_exists`, re-typed under a `command_` prefix in a different module. | COST: The same recursive reachability walk (with the same `seen` cycle guard) maintained twice. The prefix makes it look like a distinct algorithm, so grep for `relationship_path_exists` finds one site and a reader concludes there is one implementation. Both are called from cycle-detection loops that produce the same class of error message, so a fix to one leaves half the graph unchecked. | FIX: Make `journey/derivation.rs:270` `pub(crate)` and delete `derive.rs:384`. `journey/derivation.rs` should survive — it owns the derivation relationship model that both walkers traverse. | SITES: src/journey/derivation.rs:270
[SWEEP] src/risk.rs:205 | reuse | `days_between` — including its private nested `ord` helper and its fake `y*365 + m*31 + day` calendar — is written twice in two modules, identical except for a `.max(0)`. | COST: Two copies of a deliberately-approximate date arithmetic whose approximation is documented in only one of them (`divergence.rs:399-400`). The `risk.rs` copy clamps negatives and the `divergence.rs` copy does not, so the same two timestamps yield different day counts depending on which module asks — and there is no doc in `risk.rs` telling a reader the calendar is fake at all. | FIX: One `pub(crate) fn days_between(a: &str, b: &str) -> i64` in `journal.rs` beside the other timestamp helpers, clamping at zero. `divergence.rs:401` should survive as the body — it carries the doc comment explaining why the coarse calendar is acceptable. | SITES: src/divergence.rs:401 [BEHAVIOR-CHANGE: skip unless deliberately chosen]
[SWEEP] src/completeness/mod.rs:214 | reuse | `projection_current` — the one-line rule for whether a Journey projection edge counts as current — is defined three times in three modules, two of them byte-identical. | COST: A readiness rule that decides whether a proof satisfies completeness, restated three times. Only `completeness/mod.rs:211-213` carries the doc explaining why `independent` is excluded; a maintainer editing the other two has no way to know that reasoning exists. Adding a fourth `InspectionStatus` variant means finding three private functions with the same name and no cross-reference. | FIX: One `pub fn projection_current(status: InspectionStatus) -> bool` (or an `InspectionStatus::is_projection_current` method on the enum in `model.rs`, which is where the variants live). `completeness/mod.rs:214` should survive as the documented form; the two `Status`-taking copies become calls with `edge.status`. | SITES: src/journey_exercises.rs:137, src/proofstrength/entries.rs:351
[SWEEP] src/signal/debt/cochange.rs:593 | simplification | The `codefile` and `empty_snap` test fixture builders are byte-identical in signal/debt.rs and signal/debt/cochange.rs, and travel.rs writes the same `Snapshot { .. }` literal inline four more times. | COST: `Snapshot` has nine fields; every fixture that wants "a snapshot with just these nodes" spells all nine plus a four-field `Identity`. Adding a tenth field to `Snapshot` breaks six sites at once, five of which are the same fixture. `cochange.rs` is a child module of `debt.rs` and could reach the parent's helper with `super::` — the duplication is not even a module-boundary problem. | FIX: One `pub(crate) fn empty_snapshot(nodes: Vec<Node>) -> Snapshot` and `pub(crate) fn codefile_node(id, path) -> Node` behind `#[cfg(test)]` in `store/mod.rs` beside `Snapshot` itself. `signal/debt.rs:151` should survive as the body; `cochange.rs:607` becomes `use super::debt_tests::empty_snap` or the shared helper, and travel.rs's four inline literals call it. | SITES: src/signal/debt.rs:137, src/signal/debt.rs:151, src/travel.rs:421, src/travel.rs:494, src/travel.rs:554, src/travel.rs:628
[SWEEP] src/subprocess.rs:202 | simplification | `set_fd_flag` and `set_status_flag` are the same 15-line fcntl read-modify-write body, differing only in the F_GETFD/F_SETFD vs F_GETFL/F_SETFL constant pair. | COST: Two copies of an unsafe fcntl sequence with two `last_os_error()` paths each. The names give no hint that the only difference is which fcntl command pair is used, so a fix to the error handling in one (or a change to how `enabled` maps to bit set/clear) is easy to apply to only one. | FIX: One `fn set_fcntl_flag(fd, get: c_int, set: c_int, flag: c_int, enabled: bool)`, called as `set_fcntl_flag(fd, F_GETFD, F_SETFD, ..)` and `set_fcntl_flag(fd, F_GETFL, F_SETFL, ..)`. `set_fd_flag` should survive as the body since it is written first and is the descriptor-flag case. | SITES: src/subprocess.rs:185
[SWEEP] src/extract/calls.rs:391 | simplification | The `extract` test shim — including its four-line doc comment — is duplicated verbatim in two sibling `#[cfg(test)]` modules inside the same file, 100 lines apart. | COST: Two identical private shims in one file, each shadowing `super::extract` inside its own module. A reader who finds one has no signal the other exists; a change to the parse-then-delegate contract (e.g. the shim starts asserting the tree parsed) must be made twice or the two test modules silently exercise different paths. | FIX: Hoist one copy into a single `#[cfg(test)] mod test_support` (or just a `#[cfg(test)] fn extract_for_tests` at file scope) and have both test modules `use super::extract_for_tests as extract`. Either copy survives — they are identical; keep `:290` since it is first. | SITES: src/extract/calls.rs:290
[SWEEP] src/commands/status_cmd.rs:378 | simplification | `println!("{}", serde_json::to_string_pretty(&x)?)` is written 56 times across 40 command handlers with no shared emitter, even though the sibling envelope helper `pagination_envelope` was centralized. | COST: Every JSON-emitting handler independently owns the decision of pretty vs compact, trailing newline, and stdout vs a writer. There is no single place to add a `--compact` flag, redirect JSON to a writer for testing, or handle a broken pipe (the SIGPIPE issue already recorded for this repo). `commands/journey/support.rs:119 emit_packet` is the one handler that wrapped it, proving the shape is wanted — it just was not generalized. | FIX: One `pub(crate) fn emit_json<T: Serialize>(value: &T) -> Result<()>` in `commands.rs` beside `pagination_envelope`, and replace all 56 sites. `commands/journey/support.rs:119 emit_packet` should be re-expressed on top of it rather than being a second emitter. | SITES: src/commands.rs:662, src/mcp.rs:434, src/commands/capture_cmd.rs:288, src/commands/capture_cmd.rs:933, src/commands/hook_cmd.rs:161, src/commands/discover_cmd.rs:253, src/commands/discover_cmd.rs:483, src/commands/pattern_cmd.rs:233, src/commands/edge.rs:854, src/commands/limits_cmd.rs:13, src/commands/checkpoint_cmd.rs:30, src/commands/audit_cmd.rs:151
[SWEEP] src/divergence.rs:563 | simplification | Six `pub fn` items across six modules have zero callers anywhere in src/ or tests/, and four of them carry doc comments naming a consumer that does not exist. | COST: `publish = false` in Cargo.toml means `pub` is not an external API contract, so nothing will ever call these; meanwhile `cargo clippy --all-targets` is silent on them (it does not dead-code-lint pub lib items), so they accumulate invisibly. Worse, their docs assert live wiring — `is_subject` says "used by the queue", `stale_passing_edge` says "Journey boundary failures use this narrower transition", `acquire_for_artifact` says it is the lock for `journey diagnose` — so a reader tracing those subsystems believes a code path exists that was deleted. | FIX: Delete all six. If any is a deliberate seam, demote to `pub(crate)`/private so the compiler will report the next one that dies. Verified with `grep -rw <name> src/ tests/` per name: only the definition line matches. | SITES: src/divergence.rs:554, src/store/derived.rs:209, src/harness.rs:91, src/subprocess.rs:589, src/callgraph.rs:361
[SWEEP] src/ratification.rs:27 | reuse | `ratification::ASSERTED_STATES` is a byte-identical copy of `grammar::RATIFICATION_STATES`, with a paraphrase of the same doc comment, and has zero references anywhere. | COST: Two constants claim to own the ratification vocabulary; only `grammar::RATIFICATION_STATES` is actually enforced (store/facts.rs:1684 is the write gate) and published (discover_cmd.rs:660, :794). Adding a state to the dead copy silently does nothing, and a reader who finds `ratification::ASSERTED_STATES` first — the module named after the concept — will believe they changed the gate. | FIX: Delete `ratification::ASSERTED_STATES`; `grammar.rs` is the module that already owns write-gate vocabulary (`LEVELS`, `ASPECTS`, `VISIBILITIES`, `ALL_LIFECYCLES`) and is the one the gate reads. Verified dead with `grep -rn ASSERTED_STATES src/ tests/` → one hit, the definition. | SITES: src/grammar.rs:33, src/store/facts.rs:1684, src/commands/discover_cmd.rs:660, src/commands/discover_cmd.rs:794
[SWEEP] src/policy.rs:26 | simplification | `policy::INTENT_ORIGINS` is a dead `pub const` whose four-value vocabulary has already drifted from the two values the code actually writes and publishes. | COST: The const documents itself as "Origins that may appear on an Intent's immutable provenance facet", but nothing reads it — no validation, no schema publication. The real vocabulary lives as two disagreeing literals: `create.rs:210` produces only `human`/`llm`, and `discover_cmd.rs:659` advertises only `["human", "llm"]`. So `drive` and `import` are documented-but-unreachable, and a reader adding a fifth origin edits the const and nothing changes. | FIX: Either delete `INTENT_ORIGINS`, or make it the single source: have `discover_cmd.rs:659` publish it and the facet writer validate against it. Verified dead with `grep -rn INTENT_ORIGINS src/ tests/` → one hit, the definition. | SITES: src/commands/discover_cmd.rs:659, src/commands/intent/create.rs:210
[SWEEP] src/coverage.rs:27 | simplification | `CoverageScopeSummary::in_scope` is a stored field that is literally `owned + unowned_files.len()` — in a struct that already exposes the same derived quantities as methods rather than fields. | COST: The struct itself sets the convention two lines below: `unowned()` and `excluded()` are computed from their sibling `Vec`s. `in_scope` breaks it, and its own doc comment restates the formula in prose ("Files carrying an ownership obligation (`owned + unowned`)") — meaning the invariant is asserted twice in English and once in code, checked nowhere. Any future construction site (or a serde `Deserialize` if one is added) can produce a summary where `in_scope != owned + unowned()`, and the two JSON surfaces at status_cmd.rs:359 and diagnostics_cmd/coverage.rs:45 would print the inconsistent number. | FIX: Delete the field and add `pub fn in_scope(&self) -> usize { self.owned + self.unowned() }` beside `unowned()`/`excluded()`; update the two `.in_scope` readers to call it. The struct is `Serialize`-only, so keep the JSON key by serializing the method's value. | SITES: src/coverage.rs:204, src/commands/status_cmd.rs:359, src/commands/diagnostics_cmd/coverage.rs:45
[SWEEP] src/commands/proof_cmd/support.rs:51 | simplification | Two of `ProofCommandCollision`'s four fields — `detected` and `prior_behavior_count` — are stored copies of `!prior_behaviors.is_empty()` and `prior_behaviors.len()`. | COST: Both constructors have to set all three consistently, and they already spell the derivation out at the two sites (`none()` writes `detected: false, prior_behavior_count: 0` alongside an empty `Vec`). The struct's only internal reader, `warn()`, reads `self.detected` and `self.prior_behavior_count` rather than the Vec it is about to iterate three lines later — so the warning text can disagree with the list it prints if a future writer sets one and not the other. | FIX: Keep only `command` and `prior_behaviors`; add `fn detected(&self) -> bool` / `fn count(&self) -> usize`. The struct is `serde::Serialize` and `tests/ring36_shared_proof.rs:467,550` assert the `prior_behavior_count` key, so preserve the emitted JSON shape (a manual `Serialize` impl or `#[serde(skip)]` + computed keys) rather than dropping the keys. | SITES: src/commands/proof_cmd/support.rs:60, src/commands/proof_cmd/support.rs:130, src/commands/proof_cmd/support.rs:68, src/commands/proof_cmd/support.rs:77
[SWEEP] src/proofstrength/mod.rs:136 | simplification | `ProofAssessment` stores `any_passing` and `meaningful_passing`, both derived from the sibling `best_passing_strength`; the derivation that the struct refuses to own is then re-spelled as an identical five-line prelude at three call sites. | COST: `assess()` computes both from `best_passing_strength` at :161-163 and then stores all three, so a hand-constructed `ProofAssessment` (workitem/mod.rs:1014 does exactly this) can claim `any_passing: true` with `best_passing_strength: None`. Because the struct exposes only raw flags, the real business predicate — "passed but only liveness" — plus the S0 fallback for the display string is copy-pasted verbatim in three modules; a change to what counts as weak means editing all three or letting the queue, the packet, and the prompt contract disagree about the same intent. | FIX: Drop the two derived fields; add `impl ProofAssessment { fn any_passing(&self) -> bool, fn meaningful_passing(&self) -> bool, fn weak_passing(&self) -> bool, fn best_str(&self) -> &'static str }` and have validate.rs / roster.rs / packets.rs call `proof.weak_passing()` and `proof.best_str()`. `any_registered` stays a real field (it is not derivable from the others). | SITES: src/workitem/contracts/validate.rs:156, src/workitem/queues/roster.rs:337, src/workitem/queues/packets.rs:659, src/proofstrength/mod.rs:161, src/workitem/mod.rs:1014
[SWEEP] src/completeness/mod.rs:433 | simplification | Three module facades keep re-exports alive with `#[allow(unused_imports)]` plus a comment naming a consumer; the `completeness` one has no consumer at all, and the `signal` one exists only so two children can spell a path the other two children spell properly. | COST: An `#[allow(unused_imports)]` is the compiler being told to stop reporting exactly the fact you want to know. `completeness/mod.rs:435` re-exports `journey_readiness_with_journal` "so crate::completeness::journey_readiness_with_journal still resolves" — nothing writes that path; both callers (readiness.rs:13 and :207) are inside `readiness.rs` and call it unqualified. `signal.rs:20` hoists `graph::node_name` into the parent purely so `debt.rs:113` can write `super::node_name` and `cochange.rs:502` can write `super::super::node_name`, while the sibling modules `doctor.rs:1` and `smells.rs:1` do the ordinary `use super::graph::{…, node_name}`. Two conventions for one helper, one of them costing a lint suppression. | FIX: Delete `completeness/mod.rs:433-435` outright. In `signal`, delete `signal.rs:18-20` and add `use super::graph::node_name;` to `debt.rs` and `use crate::signal::graph::node_name;` to `debt/cochange.rs`, matching doctor.rs and smells.rs. Re-check `store/mod.rs:118` — `DebtPromotionInput`/`DebtPromotionResult` are genuinely used via `crate::store::` at advisory.rs:128, so the suppression there is stale and can go too. | SITES: src/signal.rs:19, src/store/mod.rs:118, src/signal/debt.rs:113, src/signal/debt/cochange.rs:502, src/completeness/readiness.rs:13, src/completeness/readiness.rs:207
[SWEEP] src/commands.rs:50 | simplification | Three items in the `commands` public facade have no production caller — they exist only so integration tests can reach them, and one of them flattens a named struct back into an anonymous 4-tuple to do it. | COST: The comment at :57-58 justifies this block ("Public so callers other than the CLI — absorb, fixtures — take the same path rather than a seam") and is true for `apply_value`/`batch_schema` (mcp.rs:348, discover_cmd.rs:707) — but the lines around it are not. `prove_intent` has zero src callers. `observe_validation` is only called from inside its own file (support.rs:319). `code_ownership_summary` has zero src callers and exists purely to re-shape `CoverageScopeSummary` into `(usize, usize, Vec<String>, usize)`, which the four test call sites then read as `let (_, _, unowned, _) =` — throwing away the field names the struct already has. Meanwhile the two src consumers of the same data call `crate::coverage::unowned_names` directly, bypassing the facade entirely. | FIX: Delete `code_ownership_summary` and have the four tests call `coverage::coverage_scope_summary(...)` and read `.unowned_files` / `.owned` by name. Drop the `commands::` re-export of `unowned_names` (src already uses `crate::coverage::unowned_names`). Keep `prove_intent`/`observe_validation` if tests need them, but move them behind a `#[cfg(any(test, feature = "test-support"))]` or an explicit `pub mod fixtures`, so the facade stops claiming they are part of the command surface. | SITES: src/commands.rs:61, src/coverage.rs:150, src/commands/proof_cmd/mod.rs:23, src/commands/proof_cmd/support.rs:310, src/absorb.rs:135, src/workitem/contracts/build.rs:102
[SWEEP] src/risk.rs:28 | reuse | EXTENDS the existing truth.rs/registry.rs `str_enum!` finding: eleven more enums across nine modules carry BOTH `#[serde(rename_all = "snake_case")]` and a hand-written `as_str` that reproduces exactly the same strings, while `model.rs:45 str_enum!` generates as_str/ALL/FromStr/Display/serde from one variant list for seventeen enums. | COST: For each of these enums two independent mechanisms must produce identical strings and only one of them is exercised by any test — so a variant rename silently forks the JSON name from the display name. None of the eleven gets `ALL` or `FromStr` either, which is why parse-or-enumerate keeps being re-implemented by hand elsewhere (the existing `identity.rs:45` / `GATEABLE_ROLES` finding is the same disease). The macro that removes the whole class exists in the crate and is applied seventeen times in model.rs alone. | FIX: `str_enum!` is private to model.rs; `pub(crate) use str_enum;` it and convert these enums. Where a variant string genuinely is not the snake_case of its name, that is the signal that the enum needs an explicit table — but then the `rename_all` attribute must go, not stay as a second source. | SITES: src/lane.rs:23, src/absorb.rs:38, src/journey_runtime/types.rs:151, src/journey/derivation.rs:62, src/release/mod.rs:116, src/ratification.rs:79, src/review.rs:20, src/review.rs:65, src/divergence.rs:33, src/batch_auth.rs:28, src/truth.rs:23, src/model.rs:45
[SWEEP] src/store/facts.rs:1122 | simplification | The "recompute the content-addressed evidence id, probe for a colliding twin, delete-or-update" block is written out twice in the same `match`, arms `AnchorFate::Moved` and `AnchorFate::Refreshed`. | COST: The two copies each carry the same eleven-line `new_id != row.id && SELECT 1 FROM evidence WHERE id = ?1 … .is_some()` probe and the same UPDATE statement — and the UPDATE strings already differ in whitespace/continuation (`expiry_reason = ''` followed by a newline at :1140 versus a `\` continuation at :1170), which is the visible start of the drift. The comment at :1117-1121 explains the identity rule once, above only one of the two implementations of it. | FIX: Extract `fn recompute_row_identity(&self, fact_id: &str, row: &mut EvidenceRow, payload: Evidence) -> Result<bool>` returning whether a twin absorbed the row; both arms then differ only in what they push to `reanchors` and `out`. | SITES: src/store/facts.rs:1153, src/store/facts.rs:136
[SWEEP2:residue] src/journey/sources.rs:77 | altitude | The entire Journey runtime-source validation policy is implemented twice — once in `journey/sources.rs` (authoring side) and once in `journey_runtime/compile.rs` (compiler side) — as four pairs of near-identical functions with divergent error texts. | COST: Four rules (source availability, run.id gating, secret-input refusal, redacted/non-scalar refusal) each live in two places. A policy change (e.g. permitting run.id in one more position, or a new RuntimeSource variant) must be made twice with no compiler link between the copies; the error texts have ALREADY drifted ("is not an output of a prior Journey step" vs "is not available from an earlier step"), so the same rejection reads differently depending on whether the manifest was rejected at authoring or at compile time. | FIX: Delete `compile.rs`'s `validate_source_reference`, `validate_scalar_source`, `validate_sources`, `validate_temporal_sources` and `validate_compiled_scalar_source`; promote `sources.rs`'s `validate_available_source` / `validate_interpolated_source` / `validate_operation_references` / `validate_temporal_action_references` to `pub(crate)` and call them. The compiled-side variant differs only in taking `BTreeSet<&str>` input ids instead of `BTreeMap<&str,&JourneyInput>` — parameterize on a small `Inputs` trait or pass the map through. | SITES: src/journey/sources.rs:93 validate_interpolated_source ≡ src/journey_runtime/compile.rs:412 validate_scalar_source ≡ src/journey_runtime/compile.rs:711 validate_compiled_scalar_source, src/journey/sources.rs:144 validate_operation_references ≡ src/journey_runtime/compile.rs:356 validate_sources, src/journey/sources.rs:130 validate_temporal_action_references ≡ src/journey_runtime/compile.rs:396 validate_temporal_sources [BEHAVIOR-CHANGE: skip unless deliberately chosen]
[SWEEP2:residue] src/journey_runtime/compile.rs:127 | simplification | The compiled-setup rule set (before_steps well-formedness, plus the four setup-operation rules) is written out twice inside compile.rs itself — once in `compile_surface`'s setup closure against the authored `SurfaceSetup`, once in `CompiledJourneyProof::validate` against the `CompiledSetup`. | COST: The before_steps loop is byte-identical apart from the name of the step-id set; the four setup-operation rules (no repeats, must be mutable, must assert a fixture, must not also be a primary step operation) are the same rules with reworded messages. Adding or relaxing a setup rule requires editing both copies 400 lines apart, and the two already word the same refusal differently ("must assert its fixture" vs "has no fixture assertion"). | FIX: Extract `fn validate_before_steps(step_ids: &BTreeSet<&str>, before_steps: &BTreeMap<String, Vec<SurfaceFileAction>>) -> Result<()>` and `fn validate_setup_operation_rules(...)`, and call both from `compile_surface` and from `CompiledJourneyProof::validate`. | SITES: src/journey_runtime/compile.rs:120 vs :518 — "compiled Journey setup must contain an operation or before_steps file action" (byte-identical bail), src/journey_runtime/compile.rs:150 vs :560 — setup repeats operation, src/journey_runtime/compile.rs:161 vs :566 — setup operation must be mutable, src/journey_runtime/compile.rs:170 vs :572 — must assert its fixture / has no fixture assertion, src/journey_runtime/compile.rs:154 vs :582 — is also a primary step operation
[SWEEP2:residue] src/cli/subcommands/proof.rs:11 | altitude | Twenty-three closed vocabularies across eight `cli/subcommands/*.rs` files are declared as bare `String` args with the valid values written only in a doc comment — while the crate has `str_enum!` types for most of them and `clap::ValueEnum` is used three files over. | COST: Every one of these is a spelling of a vocabulary that already exists as a typed enum (`InspectionStatus`, `IntentLifecycle`, `ValidationType`, `EdgeKind`, `GroundingRole`, the thresholds gate list, `GATEABLE_ROLES`). The doc comment is the only contract, so `--help` text, the parse-time refusal, and the enum can drift independently: adding a variant to `ValidationType` leaves `loom proof add --type` documenting six of seven, and every handler must re-validate the string by hand or accept garbage into the graph. | FIX: Declare a `#[derive(clap::ValueEnum)]` arg type per vocabulary with a `From<Arg> for <the typed enum>` bridge — exactly the shape `ScanFormatArg` already uses at diagnostics.rs:60-73 — or `#[arg(value_parser)]` over the `str_enum!` `FromStr`. Clap then generates the value list in `--help` from the type, and drift becomes a compile error. | SITES: src/cli/subcommands/diagnostics.rs:89, src/cli/subcommands/diagnostics.rs:117 (five threshold gate names — a fifth spelling of the thresholds vocabulary), src/cli/subcommands/diagnostics.rs:141 (gateable lanes), src/cli/subcommands/capture.rs:20, src/cli/subcommands/capture.rs:81, src/cli/subcommands/capture.rs:102, src/cli/subcommands/intent.rs:21, src/cli/subcommands/intent.rs:46, src/cli/subcommands/intent.rs:130, src/cli/subcommands/intent.rs:136, src/cli/subcommands/intent.rs:139, src/cli/subcommands/intent.rs:173 [BEHAVIOR-CHANGE: skip unless deliberately chosen]
[SWEEP2:residue] src/proofstrength/command.rs:604 | altitude | `loom_cli_handler` is a hand-maintained shadow copy of the `commands::run` dispatch table: 23 `cli::Command` variants mapped to hardcoded source-file paths and function-name strings, with a comment admitting it restates `commands::run`'s branches. | COST: Twenty-three `("src/commands/…rs", "symbol")` string pairs that no compiler checks. A module split or a renamed handler silently turns a mapping into no credit — the S3 call witness quietly stops crediting a real proof, and nothing fails: `loom_cli_handler` returns `None`, `exact_entry` returns an empty vec, and the strength grade drops with no diagnostic. The known module splits in this repo's recent history are exactly the change that breaks it. | FIX: Have the dispatch itself carry its own identity — a `const fn entry(&Command) -> Option<(&'static str, &'static str)>` beside `commands::run`, or attach the handler path/symbol to the dispatch arm — so the route table and the router are one artifact. Failing that, add a test that asserts each named file defines each named symbol via `callgraph::file_defines`, so drift fails loudly instead of downgrading proofs. | SITES: src/proofstrength/command.rs:596 cli_handler (the constructor for the 23 entries), src/commands.rs (the real dispatch match these 23 arms shadow)
[SWEEP2:residue] src/proofstrength/command.rs:176 | simplification | The cargo option vocabulary is spelled four different ways inside one file — a reject list, a `strip_prefix` chain, a `takes_value` `matches!`, and a second reject list in `cargo_run_entries` — with the `word == "--x" || word.starts_with("--x=")` idiom hand-expanded 11 times and the `--color` value check written 4 times. | COST: Eleven cargo options appear in two or three of the four lists; `--manifest-path`, `--target` and `--package` appear in all four. Teaching the parser one new cargo flag means finding every list that should mention it, and a flag added to only three of the four silently changes whether a command fails closed or credits the wrong entry symbol — the exact false-credit failure this module exists to prevent. | FIX: Declare one `const CARGO_OPTS: &[(&str, OptKind)]` table (kind = Flag | TakesValue | Unmodeled) and drive all four call sites from it, plus a `fn flag_or_eq(word: &str, name: &str) -> bool` for the `--x` / `--x=` pair and a single `fn valid_color(value: &str) -> bool`. | SITES: src/proofstrength/command.rs:179,181,187,196,198,200,202,400,404 (the remaining eight `starts_with("--…=")` expansions), src/proofstrength/command.rs:512, :527, :536 — the interpreter list `matches!(first, "python" | "python3" | "node" | "bash" | "sh" | "zsh")` written three times, the third only to recompute an index the second already implies, src/proofstrength/command.rs:508-510 and :514 — the `.py`/`.js`/`.ts` script-extension list written twice
[SWEEP2:residue] src/workitem/contracts/inspect.rs:39 | simplification | `PromptContract` has five fields that are empty in almost every contract, and because the struct does not derive `Default` all 26 constructions spell them out: `evidence_template: None, examples: None, pre_screen: None, pre_screened_hits: Vec::new(), human_gate: None`. | COST: About 128 lines of pure boilerplate across seven contract modules, and it grows by five lines with every new packet contract. Adding a sixth optional field means editing all 26 construction sites; the field list also drowns the two or three lines that actually distinguish one contract from the next, which is what a reader of these files is looking for. | FIX: `#[derive(Default)]` on `PromptContract` (all five are `Option`/`Vec`; the four `String` fields default to empty) and change each construction to `PromptContract { role: …, mindset: …, …, ..Default::default() }`. | SITES: src/workitem/contracts/triage.rs (34 boilerplate field lines), src/workitem/contracts/repair.rs (28), src/workitem/contracts/validate.rs (36), src/workitem/contracts/journey.rs (20), src/workitem/contracts/build.rs (20), src/workitem/contracts/quality.rs (3)
[SWEEP2:residue] src/completeness/scorecard.rs:128 | efficiency | `BoundaryIndex` was built to stop per-intent store round trips, but it only covers the boundary axis: `apply_waiver`, `scenarios_axis`, `prerequisites_axis`, `proof_axis` and `questions_axis` still issue point queries per intent (and per family member), so `all_scorecards_with` remains an N+1 over every feature intent. | COST: Per intent, `all_scorecards_with` costs one `get_facet(level)`, one `get_facet(visibility)`, five `apply_waiver` facet reads, two `edges_with` calls plus one `get_node`+`get_facet` per scenario family member, one `edges_with` plus a `get_node` and a `realizing_groundings` per prerequisite, and two `edges_with` (see residue-9) plus a `get_node` per validation. `BoundaryIndex::load` also re-reads the full Intent list that `all_scorecards_with` itself lists two lines later. The file's own comment at :74-77 records that this exact class of per-card whole-graph work already cost CPU-minutes once (finding 6825299d). | FIX: Extend `BoundaryIndex` (or add a sibling `ScorecardIndex`) to preload what the other five axes read: the `visibility`/`level`/`aspect`/`waiver:*` facets for all intents in one facet scan, the `Requires`/`Validates`/`Questions` edge families in three `edges_with(kind, None, None)` calls, and the intent node map — the same shape `load` already uses for Hierarchy/ScenarioOf/Governs. | SITES: src/completeness/scorecard.rs:83-86 — per-intent `get_facet("level")` inside the all-intents loop, src/completeness/scorecard.rs:279-284 — `BoundaryIndex::load` re-lists all Intent nodes that `all_scorecards_with:81` lists again, src/completeness/scorecard.rs:617-621 — `questions_axis` does one `get_node` per Questions edge only to filter on node_type
[SWEEP2:residue] src/completeness/scorecard.rs:525 | efficiency | `proof_axis` re-queries the `Validates` edges of the intent that `proofstrength::assess` already fetched one line earlier and threw away. | COST: Two identical `edges_with(Validates, None, Some(intent_id))` per intent per scorecard, because `ProofAssessment` reports `any_registered` as a bool instead of returning the edges it read. On the `all_scorecards_with` path that is one wasted edge query per feature intent, on the slow branch of the axis that already costs a `get_node` per validation. | FIX: Have `proofstrength::assess` return the `Vec<Edge>` it loaded (or take the edges as a parameter), and let `proof_axis` reuse them for the blocked-validation count. | SITES: src/proofstrength/mod.rs:165 — `any_registered: !proofs.is_empty()` is the only thing kept from the query
[SWEEP2:residue] src/research.rs:53 | reuse | `research::substantive` inlines its own five-token placeholder list instead of using `grammar::is_placeholder` / `grammar::PLACEHOLDER_TOKENS`, the crate's named and documented owner of that judgment — and `capture_cmd.rs:877` inlines a byte-identical third copy that re-checks a value `substantive` already checked. | COST: Three answers to "is this field filler?": grammar's 18-token list, research's 5-token subset, and capture_cmd's copy of research's subset. A token added to `PLACEHOLDER_TOKENS` (the list `loom schema` publishes as the contract) does not reach research task validation at all. Worse, `loom task close` on a research task runs both: `capture_cmd` rejects the result string, then `validate_completion` calls `substantive("research result", …, 12)` on the same string with the same rule — the same check, spelled twice, in two modules. | FIX: Make `substantive` call `crate::grammar::is_placeholder(v)` for the token test (keeping its own length floor and angle-bracket check), and delete the inline block at capture_cmd.rs:877-885 — `store.update_node` already routes through `research::validate_record` → `validate_completion` → `substantive`. | SITES: src/grammar.rs:58 is_placeholder (the shared predicate), src/research.rs:253 validate_completion — calls substantive on the same value capture_cmd already tested [BEHAVIOR-CHANGE: skip unless deliberately chosen]
[SWEEP2:residue] src/research.rs:183 | simplification | `stamp_conclusion_freshness` re-evaluates the "is this source still usable?" predicate that `validate_close` — which it calls on its own first line — has just evaluated over the same list, with the closure written out verbatim a second time. | COST: The freshness rule (a source with no `fresh_until`, or one whose `fresh_until` parses and is not before `now`) is defined twice, four lines apart, and the two must stay in lockstep or a source can pass `validate_close` and then be excluded from the conclusion binding. Every `fresh_until` RFC-3339 string is also parsed twice per close. | FIX: `fn is_usable(&self, source: &ResearchSource, now: DateTime<Utc>) -> bool` on `ResearchBody`; `validate_close` becomes `self.sources.iter().any(|s| self.is_usable(s, now))` and `stamp_conclusion_freshness` filters on the same method. | SITES: 
[SWEEP2:residue] src/store/nodes/mutate.rs:37 | simplification | The "a quality rule changed, so stale every governs edge it owns" block is written twice in one file — in `update_node` and in `set_node_body` — differing only in the word inside the cause string. | COST: One rule about quality-verdict invalidation, two implementations 40 lines apart. A change to what staling a rule means (filtering by edge status, recording a note, batching the writes) has to find both, and a fix applied to only one leaves half the rule-edit paths borrowing an old verdict — which is precisely the failure the comment at :74-77 says this code exists to prevent. | FIX: `fn stale_governed_edges(&self, rule_id: &str, what_changed: &str, rule_name: &str) -> Result<()>` on `Store`, called from both. | SITES: src/store/nodes/ratify.rs:194 — the same Governs-stale walk for pattern guidance changes
[SWEEP2:residue] src/store/nodes/mutate.rs:221 | reuse | `delete_edge_records` exists as the edge-side "delete every row keyed to this id" helper, but the node-side equivalent has no helper and is hand-inlined twice inside `delete_node` — once for the target node, once per dependent note. | COST: Six `conn.execute` calls where two helper calls belong, and the two inline copies must stay identical. Adding a fourth node-keyed table (the way `hit_adjudication` was added edge-side) means finding both blocks in `delete_node` plus any future node-delete path — and an omission leaves orphan rows in a store whose whole invariant is that a wipe+rebuild is byte-identical. | FIX: `fn delete_node_records(&self, id: &str) -> Result<usize>` mirroring `delete_edge_records`, called at :221 and inside the `dependent_notes` loop. | SITES: src/store/nodes/mutate.rs:234-245 (the second inline copy)
[SWEEP2:residue] src/store/nodes/mutate.rs:313 | efficiency | `notes_for` loads every Note node in the graph and filters in memory on a JSON body field, on every call — and `delete_node` re-implements the same full Note scan rather than using it, despite `notes_for`'s doc claiming to be the single owner of that lookup. | COST: Four production callers (`capture_cmd`, `context_cmd`, `hypothesis`, `workitem/context`) each pay a full Note-table read per target; `context_cmd:389` then keeps six of them. Notes accumulate monotonically — they are the audit trail and are never pruned — so this is the one query in the file whose cost grows without bound with graph age. `delete_node` builds the same index at :195-201 with an explicit comment about avoiding a full scan per target, which is exactly what `notes_for` does per call. | FIX: Index the body `target_id` (a generated column plus an index, or a `note_target` side table written by `add_note`) and have `notes_for` issue one WHERE query; then have `delete_node` build its closure from repeated `notes_for` calls or expose the index it builds as the shared reader. | SITES: src/commands/capture_cmd.rs:682, src/commands/context_cmd.rs:389, src/commands/domain_cmd/hypothesis.rs:167, src/workitem/context.rs:281
[SWEEP2:residue] src/commands/proof_cmd/observe.rs:131 | reuse | POSIX single-quote shell escaping is implemented four times in four modules under four names, with two different escape sequences for an embedded quote. | COST: Four copies of a security-shaped one-liner: `observe.rs` builds the string handed to `sh -c`, `workitem/mod.rs::q` builds commands a worker copies verbatim, `pattern.rs::shell_escape` builds prefilled scan commands, and `hook_cmd.rs` builds a generated shell script. They already disagree on spelling (`'\''` vs `'"'"'`), so a reader cannot tell whether the difference is deliberate, and any hardening applied to one (e.g. rejecting NUL or newline) reaches none of the others. | FIX: One `pub(crate) fn shell_single_quote(s: &str) -> String` in a shared module; `observe::shell_quote` keeps only its unquoted fast path on top of it, and the other three call it directly. | SITES: src/workitem/mod.rs:652, src/pattern.rs:219, src/commands/hook_cmd.rs:40
[SWEEP2:residue] src/journey_runtime/artifacts.rs:77 | reuse | "Write to a temp sibling, then rename over the target" is implemented three times — twice in sibling modules of the same `journey_runtime` package (`artifacts::atomic_write`, `temporal::atomic_replace_temporal_file`) and once in `rolelease::write_lease`. | COST: Three temp-name schemes (`.{name}.{pid}.tmp`, `.{name}.loom-temporal-{pid}-{seq}`, `.{role}.tmp-{pid}`) and three durability policies: only the temporal copy fsyncs, only the temporal copy preserves permissions and retries on name collision, only `artifacts` creates the parent directory. A crash-durability or permissions fix applied to one leaves the other two on the old behavior, and the two journey_runtime copies sit in modules that already `use super::` each other. | FIX: One `pub(crate) fn atomic_write(path, bytes, opts)` — parent creation, collision-retried temp name, fsync, permission preservation — and have all three call it with the options they need. | SITES: src/journey_runtime/temporal.rs:152, src/rolelease.rs:133
[SWEEP2:residue] src/seed.rs:392 | altitude | `derive_findings` writes the four per-callable detectors as four copy-pasted `if metric > threshold { out.push(FindingDesc { … }) }` blocks that differ only in five values, with the rule key spelled a second time as the finding kind (`complex_symbol` vs `complex-symbol`). | COST: Adding a fifth per-symbol gate means copying a 12-line block and remembering to keep the `kind` (underscored) and the `rule` (hyphenated) in sync with `BUILTIN_RULES` and with `workitem/queues/predicates.rs:496-501`, which restates all six kinds a third time. The two spellings of every detector name — `complex_symbol` and `complex-symbol` — are related only by convention, so nothing catches a mismatch; the finding simply attaches to a rule node that does not exist. | FIX: Declare one table — `const SYMBOL_DETECTORS: &[(kind, rule, fn(&Symbol) -> u64, fn(&Thresholds) -> u64, title_verb, detail_fmt)]` — colocated with `BUILTIN_RULES` so the kind/rule pair is declared once, and drive the per-symbol loop from it. The oversized_file and panic_marker detectors stay separate (they are file-level). | SITES: src/seed.rs:404 (large_symbol / large-symbol), src/seed.rs:424 (excess_args / excess-args), src/workitem/queues/predicates.rs:496-501 — the six kinds restated as a match list, src/store/derived.rs:621 — "oversized_file" as a bare literal
[SWEEP2:residue] src/extract/metrics.rs:140 | simplification | `TS_METRICS` restates `JS_METRICS` in full — the seven-entry `branch_kinds` list and `flat_branch_kinds` are byte-identical — and `TS_SPEC` likewise restates `JS_SPEC`'s `imports`, `callables` and `declarators` while its `symbols` list is a superset. | COST: TypeScript is JavaScript plus declarations, but the tables encode it as two independent languages. A JS branch-kind correction (the way `catch_clause` was moved to `flat_branch_kinds`) must be applied twice or TS complexity silently diverges from JS complexity for the same construct — a divergence no test would catch, since both numbers are plausible. | FIX: Define the shared JS core once (`const JS_BRANCH_KINDS: &[&str]`, `const JS_CALLABLES: &[&str]`) and have both `JS_METRICS`/`TS_METRICS` and `JS_SPEC`/`TS_SPEC` reference it, listing only the TypeScript additions inline. | SITES: src/extract/langs.rs:55-71 JS_SPEC vs src/extract/langs.rs:73-94 TS_SPEC — identical `imports`, identical `callables`, identical `declarators`, superset `symbols`, src/extract/metrics.rs:132-136 vs :153-155 — params_kind identical
[SWEEP2:residue] src/workitem/queues/prescreen.rs:70 | efficiency | `prescreen_for` calls `store.is_hit_suppressed` once per pre-screen hit — up to 200 separate `SELECT COUNT(*)` round trips per work packet — when the store already exposes a bulk reader for the same rule's adjudications. | COST: `PRESCREEN_HIT_CAP` is 200, and this runs on the packet-build path that `loom next` and `loom status` walk per queue candidate. Each iteration also recomputes `canonical_excerpt` + `hit_hash` inside the store call. The bulk reader `hit_adjudications(Some(rule_name))` already exists and is used two functions away by `resolve_hit_adjudication`. | FIX: Load `store.hit_adjudications(Some(&rule.name))?` once into a `BTreeSet<String>` of content hashes before the loop, then test membership with a locally computed `hit_hash(&canonical_excerpt(&h.excerpt))`. | SITES: src/store/adjudications.rs:138 — `hit_adjudications(Some(rule_name))`, the bulk reader that already exists, src/workitem/queues/prescreen.rs:51-55 — a second per-item query: `store.get_node(&e.to_id)` per realizing grounding edge
[SWEEP2:efficiency-real] src/store/edges.rs:555 | efficiency | `edge_retired(edge_id)` re-SELECTs the edge row its two callers are already holding in hand, adding one wholly redundant `get_edge` per edge to every live-edge scan in the crate. | COST: `live_edges_by_status` runs on `loom status` seven times per invocation (maturity::gather x3 at maturity.rs:165/168/171-ish, workitem::graph_state x2 at mod.rs:812/821, and review::pending x2 via the two review::summary calls). Each call pays, per matching edge: 1 edge_superseded SELECT + 1 edge_retired get_edge SELECT + up to 2 get_node SELECTs = 4 single-row queries. On this repo's own 1,688-edge graph that is thousands of SELECTs per status, and a quarter of them re-read a row the loop already owns. The same waste is inside `realizing_implementers`, the function whose own doc comment says EVERY ownership query MUST route through it. | FIX: Add `fn edge_retired_for(&self, edge: &Edge) -> Result<bool>` taking the already-materialized edge and have `edge_retired(&str)` be the thin id-fetching wrapper; switch edges.rs:321 and edges.rs:538 (both of which hold `e`) to the by-value form. Better still, since `edge_superseded` and `grounding_role` are two separate single-key facet SELECTs on the same target, replace both with one `facets_of(&e.id, TargetKind::Edge)` read. | SITES: src/store/edges.rs:321 — realizing_implementers, holds `e`, calls edge_retired(&e.id), src/store/edges.rs:538 — live_edges_by_status, holds `e`, calls edge_retired(&e.id), src/maturity.rs:165 — live_edges_by_status (stale) on the status path, src/maturity.rs:171 — live_edges_by_status (uninspected) on the status path, src/workitem/mod.rs:812 — live_edges_by_status(.., NeedsReverification|Failing).len(), src/workitem/mod.rs:821 — live_edges_by_status(.., Uninspected).len(), src/review.rs:261 — live_edges_by_status(.., Passing|Independent) inside review::pending
[SWEEP2:efficiency-real] src/coverage.rs:98 | efficiency | `unowned_codefiles` issues one `edges_with` query PER registered CodeFile and then two-to-five facet/node SELECTs per incident edge — and its own caller `coverage_scope_summary` re-reads the whole CodeFile roster a second time immediately afterwards. | COST: On this repo's graph (203 CodeFiles, 259 implements edges) one call is ~203 edges_with queries plus, per implements edge, edge_superseded + grounding_role (test branch) or the full realizing_implementers chain (edge_superseded + edge_retired[get_edge + up to 2 get_node] + grounding_role = 5 SELECTs). That is 400-1,500 single-row queries per call. It is called from five places (coverage.rs:135, coverage.rs:171, maturity.rs:280, roster.rs:435, packets.rs:166) and via unowned_names from absorb.rs:135 and build.rs:102, so `loom status` alone pays it at least twice — once through maturity::gather and once through coverage_scope_summary. Inside coverage_scope_summary the codefile roster is materialized twice: once at line 98 inside unowned_codefiles, once again at line 176. | FIX: Read the implements edge set once (`store.edges_with(Some(EdgeKind::Implements), None, None)`) plus one bulk facet read of the `role` / `superseded_by` / `stale_cause` keys, group both by `to_id` into a BTreeMap, and drive the codefile loop off the map — turning ~N+M queries into 2. Then thread the already-fetched `codefiles` Vec from coverage.rs:176 into `unowned_codefiles` instead of re-querying it. | SITES: src/coverage.rs:135 — unowned_names wrapper, src/coverage.rs:171 — coverage_scope_summary, src/maturity.rs:280 — unowned_codefiles(store)?.len() inside LadderInputs::gather, src/workitem/queues/roster.rs:435 — roster_coverage, src/workitem/queues/packets.rs:166 — coverage packet, src/absorb.rs:135 — unowned_names, src/workitem/contracts/build.rs:102 — unowned_names, src/commands/diagnostics_cmd/impact.rs:74 — realizing_implementers called inside a per-file loop, src/store/derived.rs:692 — realizing_implementers called inside a loop
[SWEEP2:efficiency-real] src/store/edges.rs:271 | efficiency | The "is this a live realizing grounding?" predicate is two-to-three separate single-key facet SELECTs on the SAME edge, hand-inlined in loops at 20 sites, where the store's existing `facets_of` would answer all of them in one query. | COST: `grounding_role` (edges.rs:223) and `edge_superseded` (edges.rs:273) each call `get_facet`, which is one `SELECT value FROM facet WHERE target_id=?1 AND target_kind=?2 AND key=?3` (derived.rs:754-759). Twelve loops call both on the same edge id, and eight of those add a third `get_facet(.., "locator")` on the same edge — three round trips for three rows of one table row-group. `facets_of` (derived.rs:767) already returns every facet for a target in ONE query and is used only by `edge show`. On this repo's 259 implements edges this is 500-800 avoidable queries per full grounding walk, and the walks run on sync, status, absorb, divergence, ratification and every queue gather. | FIX: Add `pub fn edge_facets(&self, edge_id: &str) -> Result<BTreeMap<String,String>>` over `facets_of`, plus a `GroundingView { superseded, retired, role, locator }` built from it, and replace the 2-3 call clusters with one read. For the whole-graph loops (sync.rs:577, sync.rs:663, ratification.rs:265, store/edges.rs:290) expose `list_all_facets` (already private in store/facets.rs:206) so the entire predicate becomes one bulk read plus a map lookup. | SITES: src/coverage.rs:113-114, src/locator.rs:694-699, src/locator.rs:722-731, src/checkpoint.rs:491-496, src/sync.rs:577-586, src/sync.rs:663-666, src/ratification.rs:154-155, src/ratification.rs:265, src/divergence.rs:449-457, src/absorb.rs:108-115, src/proofstrength/entries.rs:320-321, src/commands/discover_cmd.rs:360-372
[SWEEP2:efficiency-real] src/commands/diagnostics_cmd/findings.rs:123 | efficiency | `loom findings` walks the entire Finding roster THREE times in three consecutive lines — `untriaged_findings`, `stale_findings`, and `findings_view` are the same whole-graph walk, the first two being literally `findings_view(store)?` plus a filter. | COST: `findings_view` materializes every Finding node (330 on this repo's graph, each with a serde_json body parse) and then issues three point queries per node — `adjudication`, `finding_codefile_hash`, `finding_metric`. Three calls = 990 node rows + ~2,970 single-row SELECTs, where one walk plus two `.iter().filter().count()` over the `findings` vector already bound at line 125 would cost a third of that. The same duplicate walk happens on `loom status`: `workitem::graph_state` (mod.rs:794) does one full walk and `maturity::gather` (maturity.rs:256, via needed_findings) does a second. | FIX: In findings.rs, hoist `let mut findings = crate::signal::findings_view(&store)?;` to line 123 and derive `untriaged`/`stale_findings` as counts over it before the `retain` filters. On the status path, have `LadderInputs::gather` take the `findings` slice `graph_state` already computed (or vice versa) the same way `journey_readiness` is already threaded at maturity.rs:143. | SITES: src/signal/adjudication.rs:174 — untriaged_findings wrapper re-walks, src/signal/adjudication.rs:186 — needed_findings wrapper re-walks, src/signal/adjudication.rs:193 — stale_findings wrapper re-walks, src/signal/adjudication.rs:200 — fourth wrapper re-walks, src/maturity.rs:256 — needed_findings inside LadderInputs::gather (status path), src/workitem/mod.rs:794 — findings_view inside graph_state (same status path), src/workitem/queues/predicates.rs:211 — needed_findings, another full walk, src/commands/codefile_cmd.rs:563 — findings_view then filtered by file
[SWEEP2:efficiency-real] src/commands/orient_cmd.rs:213 | efficiency | `loom orient session` pays the whole-graph Journey readiness walk TWICE — once inside `all_scorecards` and again inside `ladder_and_depths` — even though the `_with` seam that exists precisely to prevent this is right there. | COST: `all_scorecards` (scorecard.rs:69-70) calls `all_journey_readiness`, which walks every non-deprecated Journey and computes seven readiness stages each; `ladder_and_depths` five lines later calls `LadderInputs::gather`, which calls `all_journey_readiness` again at maturity.rs:143. The crate's own comment at scorecard.rs:72-75 dates this exact regression: "~43 user-visible intents x ~5.7s of readiness turned one status call into CPU-minutes" (finding 6825299d). The fix that landed added `all_scorecards_with`/`journey_derive_gaps_with` and wired maturity to them — but the orient session path still uses the eager wrapper, so orient re-pays a walk maturity is about to perform. | FIX: Move `let (ladder, queues) = crate::maturity::ladder_and_depths(&store)?;` above the `open_axes` computation, have `ladder_and_depths` return (or a sibling expose) the `journey_readiness` vector gather already holds, and call `all_scorecards_with(&store, &readiness)` instead of `all_scorecards(&store)` — exactly the shape maturity.rs:143-150 already uses. | SITES: src/workitem/queues/roster.rs:412 — roster_derive calls journey_derive_gaps (eager wrapper, own readiness walk), src/workitem/queues/roster.rs:422 — roster_surface calls journey_surface_gaps (eager wrapper, own readiness walk), src/commands/diagnostics_cmd/scan_config.rs:112 — all_scorecards eager wrapper, src/completeness/readiness.rs:353 — journey_surface_gaps eager wrapper
[SWEEP2:efficiency-real] src/commands/status_cmd.rs:310 | efficiency | Four whole-table materializations — every Journey, Intent, CodeFile node and every edge, each row JSON-parsed — are performed solely to call `.len()` on them, and the store already demonstrates the SQL COUNT idiom it needs. | COST: `row_to_node` (store/codec.rs:112-122) does `serde_json::from_str` of the body for every row it builds. On this repo's graph `gather_status_facts` parses 33 + 121 + 203 node bodies plus 1,688 edge rows and throws all 2,045 objects away to keep four integers; `loom orient session` repeats three of the four (orient_cmd.rs:208-212), and `LadderInputs::gather` materializes the CodeFile roster yet again at maturity.rs:273-275 immediately before `unowned_codefiles` at :280 materializes it a fourth time. The store already knows how to count in SQL — `truth_class_census` (derived.rs:819-835) issues `SELECT truth_class, COUNT(*) ... GROUP BY truth_class` — so the capability exists and is simply not offered to callers. | FIX: Add `pub fn count_nodes(&self, node_type: Option<NodeType>) -> Result<usize>` and `pub fn count_edges(&self) -> Result<usize>` as single `SELECT COUNT(*)` queries next to `truth_class_census`, and use them at every `list_*(.., usize::MAX)?.len()` site. Where a filter follows (audit.rs:763-772, maturity.rs:160-164), push the predicate into the WHERE clause the way `nodes_by_status` already does. | SITES: src/commands/orient_cmd.rs:208, src/commands/orient_cmd.rs:209, src/commands/orient_cmd.rs:210-212, src/maturity.rs:273-275 — CodeFile roster materialized for .len(), then again at :280, src/maturity.rs:160-164 — TaskRecord roster materialized, filtered, counted, src/audit.rs:763-767 — Intent roster materialized, filtered, counted, src/audit.rs:768-772 — QualityRule roster materialized, filtered, counted, src/workitem/mod.rs:809-811 — nodes_by_status(..).len(), src/workitem/mod.rs:812-823 — live_edges_by_status(..).len(), twice
[SWEEP2:efficiency-real] src/absorb.rs:185 | efficiency | `absorb::observe` re-runs FNV-1a over the entire file body once per newly-seen symbol and once per stale locator, inside loops where the file's fingerprint is a loop invariant already available one scope up. | COST: `fingerprint(&content)` hashes every byte of the file and allocates a String. It sits inside `for sym in &extraction.symbols` (line 178) and inside the stale-locator loop (line 201), so a file with K new symbols is hashed K times instead of once. On a first `loom absorb` nothing is `known`, so K equals the file's whole symbol count — this repo has 3,058 symbols across 203 files averaging ~15 per file, meaning the entire registered corpus is hashed roughly fifteen times over in one command. The correct value is already computed once at line 172 for the coverage branch of the same iteration. | FIX: Hoist `let content_fingerprint = crate::artifact::fingerprint(&content);` immediately after the `extract` call at line 150 and clone the short hex String at lines 172, 185 and 215. | SITES: src/absorb.rs:172 — same value computed a third time in the same iteration, src/absorb.rs:215 — per stale locator, src/seed.rs:263-266 — fingerprint per symbol span inside a per-file loop (span-scoped, so correct, but the same shape)
[SWEEP2:function-size] src/commands/journey/derive.rs:703 | altitude | `journey_derive_accept` is 371 lines (703-1073) and holds six sequential responsibilities in one body — manifest load, pre-mutation intent resolution, Proposal record selection, an idempotent-return path, the whole write transaction, and emission — even though its own file has already factored out fourteen sibling helpers including `reconcile_derivation_relationships` for the *other* half of the same reconcile step. | COST: The file's convention is one named helper per derivation concern (`plan_relationships`:298, `validate_prospective_relationship_cycles`:348, `derivation_proposals`:423, `proposal_body`:434, `reconcile_derivation_relationships`:484, `derivation_acceptance_is_current`:601). The acceptance path breaks it, and the cost is already visible inside the function: the identical 'derived intent resolves to Intent already selected by another mapping' dedupe is written twice — once at :755 over `resolved_intent_ids`, once at :878 over `accepted_ids` — because the pre-resolution pass and the transaction pass are the same walk over `manifest.intents` split across 120 lines of unrelated code. The Derives-edge projection (six `set_facet` calls plus the prune loop, :933-992) sits inline beside a *call* to the extracted relationship reconciler at :994, so a reader cannot tell why one reconcile half is a function and the other is not. Any new provenance facet must be added inside a 170-line transaction block whose failure modes span node creation, ratification batching, edge writes and journal append. | FIX: Lift four named blocks, no behavior change, each already a contiguous span: `resolve_preexisting_intents(&store, &manifest, &journey.name) -> Result<BTreeMap<String,Node>>` (:728-763); `select_derivation_proposal(&proposals, &manifest, &manifest_hash, &journey.name) -> Result<Option<Node>>` (:765-796, the conflict bail + filter + >1 bail + pop); `emit_already_accepted(...)` (:798-849); and `write_derives_projection(&store, &journey, &accepted, &spec, &semantic_hash, &proposal, &manifest_hash) -> Result<usize>` (:933-992) so it sits beside `reconcile_derivation_relationships` at the same altitude it already has. The residue is a ~90-line orchestrator that reads as the six named steps. | SITES: src/commands/journey/derive.rs:484 (reconcile_derivation_relationships — the extracted twin of the inline :933-992 projection), src/commands/journey/derive.rs:298 (plan_relationships), src/commands/journey/derive.rs:601 (derivation_acceptance_is_current), src/commands/journey/derive.rs:878 (second copy of the :755 dedupe bail)
[SWEEP2:function-size] src/commands/codefile_cmd.rs:12 | altitude | `codefile_cmd::dispatch` is 314 lines because three of its six match arms carry their whole command body inline (`Add` 105 lines, `Remove` 149 lines, `List` 46 lines) while the other three are already one-line delegations to `codefile_anchor` / `codefile_rescan` / `codefile_show` in the same file — the convention is established and then broken in the same match. | COST: Across all eleven top-level `pub(crate) fn dispatch` functions the median is 16 lines and the next-largest is 78 (hook_cmd.rs:69); codefile_cmd is 314. Because `Add` and `Remove` have no name, their logic is unreachable from a test or from MCP without going through the clap enum, unlike `codefile_show`/`codefile_rescan` which the file exposes as functions. The `Remove` arm alone contains two closures (`live`, `describe`), a three-way successor resolution, a validate-then-write cascade, two journal appends and two rendering branches — all at match-arm indentation, which pushes its inner conditions to 24-space indent (:188, :311). A reader looking for 'what does codefile remove do' finds a match arm; a reader looking for 'what does codefile show do' finds a function. | FIX: Extract `codefile_add(graph, &path, observed, json)`, `codefile_remove(graph, &key, successor.as_deref(), json)` and `codefile_list(graph, limit, offset, json)` next to the existing `codefile_anchor`/`codefile_rescan`/`codefile_show`, and reduce all six arms to one-line calls. Byte-identical bodies, no output change; `dispatch` drops from 314 lines to ~20 and matches every sibling dispatcher. | SITES: src/commands/capture_cmd.rs:765 (task, 207 lines, same shape), src/commands/pattern_cmd.rs:39 (dispatch, 202 lines), src/commands/capture_cmd.rs:371 (question, 185 lines), src/commands/capture_cmd.rs:211 (inbox, 144 lines), src/commands/pattern_cmd.rs:262 (exemplar, 107 lines), src/commands/intent/mod.rs:25 (dispatch, 103 lines), src/commands/capture_cmd.rs:629 (note, 98 lines), src/commands/domain_cmd/vocab.rs:3 (vocab, 93 lines), src/commands/hook_cmd.rs:69 (dispatch, 78 lines), src/commands/proof_cmd/rule.rs:3 (rule, 67 lines), src/commands/edge.rs:122 (dispatch, 66 lines), src/commands/diagnostics_cmd/coverage.rs:103 (ignore_cmd, 61 lines)
[SWEEP2:function-size] src/store/facets.rs:261 | altitude | `restore_inner` is 312 lines and labels its own decomposition in comments — `// Phase 1:` (:294), `// Phase 2:` (:427), `// Phase 3, mandatory:` (:566) — so three functions already exist in the author's head and only the comments name them; worse, the orphan-partition rule is split across the Phase 1/Phase 2 boundary, with facets/tags partitioned at :384-425 and the structurally identical fact partition at :481-522, after the transaction has opened. | COST: The comments are doing a function name's job, so the compiler cannot enforce the phase ordering the doc insists on ('validate every edge against the registry BEFORE any write'). The same four-branch orphan cascade — `has_target` → push, `is_soft_ref_*` → count and push, `repair` → record dropped, else `bail!`— is written three times inside one body (facets :385-406, tags :408-426, facts :492-522) with the only variation being which `report` vector receives the tuple and which soft-ref predicate applies; two of the three bail texts are byte-identical ('re-run `loom import --repair-orphans` to drop dangling facets/tags'). Adding a fourth referencing table means finding and copying a fourth cascade in the middle of a 312-line function. | FIX: Three lifts, each a contiguous span with no behavior change: `fn validate_snapshot(&self, snap: &Snapshot, node_types: &HashMap<&str, NodeType>) -> Result<()>` for :294-364; `fn partition_orphans<'a>(snap, has_target, repair, report) -> Result<(Vec<&'a Facet>, Vec<&'a Tag>, Vec<Fact>)>` collecting all three cascades (moving the fact cascade above the `self.conn.transaction()` at :428, which is what Phase 1's own comment already promises); and `fn write_snapshot(tx, snap, &facets, &tags, &facts, &evidence) -> Result<()>` for the insert block. `restore_inner` becomes the ~25-line three-phase orchestrator its comments describe. | SITES: src/store/facets.rs:385 (facet orphan cascade), src/store/facets.rs:408 (tag orphan cascade), src/store/facets.rs:492 (fact orphan cascade — same four branches, after the tx opens)
[SWEEP2:function-size] src/signal/doctor.rs:249 | altitude | `journey_integrity_issues` is 349 lines holding five mutually independent integrity checks — retired metadata, registration/artifact agreement, Derives bindings, Surfaces bindings, Proves chain — each of which already announces itself with its own `let X: Vec<&Edge> = snap.edges.iter().filter(...)` header and its own `kind:` string, in a module whose every other check IS a named function. | COST: doctor.rs's shape is one predicate per concern (`hierarchy_cycles`, `compiled_journey_provenance_problem`, `facet_value`), and `doctor` at :20 composes them. This one function violates that at 349 lines, and the cost is measured: the Surfaces block reaches 5 levels of nesting and its 'is this a valid InterfaceSurface target' test is an 80-line `is_some_and` closure (:583-635 in the original span) that reconstructs an entire `InterfaceSurfaceDefinition` from `body.get(...).and_then(as_str).unwrap_or("")` field by field just to call `.validate()`. Adding a sixth integrity rule means appending to a 349-line body rather than writing a function and adding it to a list; there is no place to unit-test one check in isolation. | FIX: Four extractions along the existing seams, each returning `Vec<DoctorIssue>` and each keeping its bodies byte-identical: `retired_journey_metadata_issues(snap)` (:276-293, which does not even use the `journey` loop variable and should move out of scope entirely), `journey_registration_issues(store, journey)` (:302-377), `journey_derives_issues(snap, journey, &step_ids, &ratified)` (:379-411), `journey_surfaces_issues(snap, &nodes, journey, &step_order, semantic_hash)` (:413-511), `journey_proves_issues(store, snap, &nodes, journey, &derives, &surfaces, semantic_hash)` (:513-594). Inside the surfaces one, the reconstruct-and-validate closure becomes `interface_surface_from_body(&Node) -> Option<InterfaceSurfaceDefinition>`. `journey_integrity_issues` then reads as a five-line `extend` chain. | SITES: src/signal/doctor.rs:20 (doctor, 225 lines — the same accumulate-into-`issues` shape one level up), src/signal/doctor.rs:339 (`Ok(hash)` match guard spanning 18 lines of inline re-derivation, at 24-space indent)
[SWEEP2:function-size] src/journey/surface_ops.rs:422 | altitude | `InterfaceSurfaceDefinition::validate` is 269 lines in which 250 are one `for operation in &self.operations` body that validates four nested levels — operation, argument, output assertion, exercise — inline, while the same file's convention is a small named validator per leaf (`validate_stable_id`, `nonempty`, `insert_unique`, `validate_json_pointer`, `validate_runtime_source`, `validate_process_environment_name`) and each nested level is already its own public type. | COST: The type hierarchy has four levels and the validator has one. `OutputAssertion`'s rules — the six-way operator-exclusivity count, the minimum/number pairing, the exists exclusivity, the matches/String pairing, the contains/String-or-Json pairing, the regex compile — occupy 85 lines (:534-619) inside a loop two levels away from the type they constrain; `OutputAssertion` itself already enforces a seven-operand version of the *same* exclusivity rule in its hand-written `Deserialize` at :206-221 with a different message, and nothing in the code links the two. The shell/HTTP/`--graph` executable gates (:474-501) are a security policy buried at 12-space indent between argv parsing and argument ids. There is no way to unit-test 'is this assertion well formed' without constructing a whole `InterfaceSurfaceDefinition`. | FIX: Push each level's rules onto its own type, bodies unchanged: `impl CliOperation { fn validate(&self) -> Result<()> }` takes the loop body; inside it `fn validate_executable_safety(&self)` takes :474-501, `impl OutputAssertion { fn validate(&self) -> Result<()> }` takes :536-619, and `fn validate_exercises(&self, assertion_ids: &BTreeSet<&str>)` takes :627-679. `InterfaceSurfaceDefinition::validate` shrinks to the surface-level checks plus `for operation in &self.operations { operation.validate()?; }` — about 20 lines. With `OutputAssertion::validate` existing, the duplicated exclusivity rule at :206 becomes visible as something to reconcile rather than something invisible. | SITES: src/journey/surface_ops.rs:206 (OutputAssertion Deserialize — second, seven-operand spelling of the exclusivity rule at :537), src/journey/surface_manifest.rs:313 (validate_for, 246 lines — same flattened-nested-validator shape), src/journey/derivation.rs:96 (validate_for, 148 lines)
[SWEEP2:function-size] src/journey_runtime/compile.rs:110 | altitude | An 88-line fallible validator with nine `bail!` sites is written as a closure passed to `Option::map`, then unwrapped with `.transpose()?` — `let compiled_setup = setup.map(|setup| { ... }).transpose()?` — inside the already-283-line `compile_surface`. | COST: `Option::map` is the crate's idiom for a one-expression transform; here it carries a whole validation pass (setup-non-empty, before_steps step existence, per-action path uniqueness, setup-operation uniqueness, primary-operation collision, missing operation, read_only, capture-free, assertion-present, source validation) plus construction of every `CompiledSetupOperation`. The result is that a reader scanning `compile_surface` for its phases sees a `let` binding where a call belongs, the whole block runs at 12-space indent, and the nine refusal messages that a Journey author actually hits are unreachable from any test that does not compile a full surface. `compile_surface`'s neighbours (`validate_sources`, `canonical_environment`, `profile_for`) are all named functions; this is the one that is not. | FIX: `fn compile_setup(setup: &SurfaceSetup, spec: &JourneySpec, bindings: &[SurfaceBinding], by_operation: &BTreeMap<&str, &CliOperation>, input_by_id: &BTreeMap<&str, &JourneyInput>, profile: &JourneyProfile, has_human_decision: bool) -> Result<CompiledSetup>` holding :112-196 verbatim; the call site becomes `let compiled_setup = setup.map(|s| compile_setup(s, spec, bindings, &by_operation, &input_by_id, &profile, has_human_decision)).transpose()?;` — one line, same control flow, same messages. | SITES: src/journey_runtime/compile.rs:41 (compile_surface, 283 lines — the enclosing function; :199-284 is a second liftable block, the per-step compile loop)
[SWEEP2:function-size] src/journey/spec.rs:306 | simplification | Inside the 180-line `JourneySpec::validate`, the profile loop walks `profile.inputs` twice, and the second walk (:306-313) re-checks a condition the first walk's match arm at :238 already bails on — `input.secret && binding.template.is_some()` is exactly the `(Some(_), None) if input.secret` arm — so the second loop and its `expect("validated above")` are unreachable. | COST: A second pass over the same map, at the same nesting level, 70 lines below the first, that can never fire: the `(Some(_), Some(_))` case bails at :234, `(Some(_), None) if input.secret` bails at :238, and `(None, ...)` leaves `binding.template == None`, so no surviving binding satisfies the second predicate. Its message string ('secret input '{}' must not have a literal or template binding') appears nowhere else in src, tests, docs or journeys — no caller, test or doc has ever seen it, which is the confirmation it is dead. It also carries an `expect()` justified by a comment referring to code 80 lines earlier, which is the only reason a reader must hold the whole 180-line function in their head to judge the last twenty of it. | FIX: Delete :306-314. The dead branch survives only because it is 70 lines from the rule that subsumes it; extracting the profile-binding walk as `fn validate_profile_bindings(profile_id: &str, profile: &JourneyProfile, spec_inputs: &BTreeMap<String, JourneyInput>, input_by_id: &BTreeMap<&str, &JourneyInput>) -> Result<()>` puts the two passes side by side where the redundancy is a two-line read. | SITES: 
[SWEEP2:function-size] src/store/facts.rs:1063 | altitude | `reverify_all` is 246 lines carrying four distinct per-fact responsibilities in one loop body: applying the four `AnchorFate` outcomes to each evidence row (:1087-1186, at 4-5 levels of nesting), computing the fact's new strength, re-opening the subject edge with a `stale_cause` facet and proof reset, and writing the fact row — none of which is named. | COST: The four-arm `match self.recheck(...)` is 100 lines of SQL-and-identity work at 16-to-24-space indent; the deepest branches (`if collision`, `if let Evidence::Span(span) = &mut payload`) sit at 24 spaces and are unreadable without scrolling past the arm header. Because the whole thing is anonymous, the edge-reopen policy — arguably the most consequential rule in the crate, guarded by a 15-line comment block at :1213-1234 explaining exactly why it keys off edge status rather than prior verification — cannot be called, tested, or referenced from anywhere; it exists only as an `if` at :1235 inside a 246-line method. `restore_inner` calls `reverify_all` as its Phase 3 (facets.rs:571), so an import failure surfaces as a failure somewhere in these 246 lines with no intermediate name to localize it. | FIX: Two lifts along existing seams, bodies unchanged: `fn apply_anchor_fate(&self, fact: &Fact, row: EvidenceRow, fate: AnchorFate, reanchors: &mut Vec<(String, String, Reanchor)>, out: &mut Reverified) -> Result<Option<EvidenceRow>>` holding :1087-1185 (returning `None` for the two `continue` collision cases), and `fn reopen_stale_verdict(&self, fact: &Fact, checked: &[EvidenceRow]) -> Result<usize>` holding :1224-1256 together with its explanatory comment. The loop body then reads as: recheck each row, compute strength, count, reopen if expired, persist — five lines with names. | SITES: src/store/facts.rs:1424 (recheck, 176 lines — the callee, same shape), src/store/facets.rs:571 (`self.reverify_all(&std::collections::BTreeSet::new())?` — the import-path caller)
[SWEEP2:error-vocab] src/store/nodes/lookup.rs:154 | altitude | The 0/1/many "resolve a subject by key" refusal is written seven times with seven different message pairs, and two production sites steer control flow by `.to_string().starts_with(...)` on that prose. | COST: There is no shared resolver error, so the miss/ambiguity distinction lives only in English. `capture_cmd.rs:601` and `lookup.rs:180` both branch on the literal prefix "no node matches"; `capture_cmd.rs:623` branches on the literal prefix "ambiguous". Three of the seven ambiguity messages do NOT start with "ambiguous" (`adjudications.rs:151` "'{key}' matches {n} suppressions…", `support.rs:54` "Journey key '{key}' is ambiguous", and every `journey/registry.rs` variant), so that guard already silently mis-classifies them as total misses. Rewording any one of these seven bail strings for clarity changes CLI control flow at a distance, in a different crate module, with no compiler error. | FIX: Introduce one `store::ResolveError { NotFound { kind, key }, Ambiguous { kind, key, candidates } }` (or a `resolve_one(kind, key, matches)` helper that mints both arms) and have all seven resolvers return it. Replace the three `.to_string().starts_with(...)` guards with a typed `downcast_ref::<ResolveError>()` match. Message text then becomes presentation, free to reword. | SITES: src/store/edges.rs:97, src/store/edges.rs:101, src/store/judgments.rs:189, src/store/judgments.rs:193, src/store/derived.rs:734, src/store/derived.rs:743, src/store/adjudications.rs:151, src/commands/journey/support.rs:53, src/commands/journey/support.rs:54, src/commands/diagnostics_cmd/advisory.rs:177, src/commands/diagnostics_cmd/advisory.rs:182, src/commands/capture_cmd.rs:601
[SWEEP2:error-vocab] src/journey/compile.rs:132 | altitude | The "load a registered Journey's artifact and verify it" block is copy-pasted across journey/compile.rs, commands/journey/support.rs, commands/journey/lint.rs, commands/journey/runtime.rs and journey_exercises.rs, carrying eight byte-identical error strings — and three of the pairs have already drifted. | COST: Five modules independently answer "is this Journey loadable and current?". The drift is already visible and is exactly the kind a reader cannot see: `compile.rs:147` and `support.rs:83` say "has no semantic_hash" while `journey_exercises.rs:200` says "has no semantic hash"; `compile.rs:203` and `journey_exercises.rs:245` say "is not a current reusable CLI" while `runtime.rs:162` dropped the word "current"; and for the same registration-vs-artifact mismatch `compile.rs:150` says "registration no longer matches its authored artifact" (no remediation) while `support.rs` tells the operator to run `loom journey add`. A driver hitting the same real condition gets a different string and different advice depending on which command it typed. | FIX: Extract one `journey::load_registered(store, key) -> Result<(Node, JourneySpec, String)>` that owns the artifact lookup, the stable-id check, the semantic-hash check and their messages, and one `journey::surface_operations(store, surface) -> Result<Vec<CliOperation>>` for the InterfaceSurface decode. `support.rs:59` already has that exact signature — promote it and delete the four other copies. | SITES: src/commands/journey/support.rs:67, src/commands/journey/support.rs:83, src/commands/journey/lint.rs:24, src/commands/journey/runtime.rs:124, src/commands/journey/runtime.rs:162, src/commands/journey/runtime.rs:171, src/commands/journey/runtime.rs:173, src/commands/journey/runtime.rs:175, src/journey_exercises.rs:200, src/journey_exercises.rs:245, src/journey_exercises.rs:254, src/journey_exercises.rs:256 [BEHAVIOR-CHANGE: skip unless deliberately chosen]
[SWEEP2:error-vocab] src/store/edges.rs:52 | simplification | The read-after-write invariant violation ("we just inserted it and it is not there") is spelled twenty different ways across eleven modules, and not one of the twenty names the id that vanished. | COST: This is the crate's loudest "the store is corrupt or the transaction is broken" signal and it has no shared shape at all. Nineteen of the twenty carry zero identifying data — an operator who hits `derived edge vanished` or `Pattern vanished after update` gets no id, no table, no operation to reproduce, and no way to grep back to the site because each phrase is unique. Nothing can match on the class, so none of these can be routed to a `loom doctor` integrity path even though that is exactly what they mean. Every new store mutation adds a twenty-first phrase. | FIX: One `fn reread(kind: &str, id: &str, after: &str) -> anyhow::Error` (or a `LoomError::Invariant { kind, id, after }`) used by all twenty call sites, so the message becomes `Pattern '{id}' is absent immediately after update — graph integrity failure; run `loom doctor`` and callers can classify it. | SITES: src/store/edges.rs:181, src/store/edges.rs:380, src/store/edges.rs:458, src/store/edges.rs:525, src/store/nodes/lookup.rs:90, src/store/derived.rs:312, src/store/derived.rs:371, src/store/derived.rs:393, src/store/derived.rs:905, src/store/judgments.rs:160, src/commands/pattern_cmd.rs:119, src/commands/pattern_cmd.rs:333 [BEHAVIOR-CHANGE: skip unless deliberately chosen]
[SWEEP2:error-vocab] src/store/nodes/mutate.rs:14 | reuse | `get_node(id)? .ok_or_else(|| anyhow!("no node '{id}'"))` and its edge twin are inlined 20 times in the store, the edge copy split across two format strings (`{edge_id}` vs `{id}`), and five further modules phrase the identical "subject id is not in the graph" failure five other ways. | COST: There is no `require_node` / `require_edge` on `Store` even though `require_edge_owner` and `require_edge_kind_owner` already establish that naming convention right next to these sites (`nodes/mutate.rs:273`, `open.rs:479`). Twenty inline copies means the graph's most common refusal cannot gain an id-prefix hint, a "did you mean" list, or a doctor hook without twenty edits. It has already forked: the same edge miss prints `no edge '{edge_id}'` from `edges.rs` and `no edge '{id}'` from `facts.rs`, and outside the store the same condition reads `from node '{from_id}' does not exist`, `audit node subject '{id}' is absent`, `debt cluster subject '{id}' is not a registered node`, and `target CodeFile '{}' is missing`. | FIX: Add `Store::require_node(&self, id) -> Result<Node>` and `Store::require_edge(&self, id) -> Result<Edge>` beside `require_edge_owner`, returning the one typed miss from finding msg-1, and replace all twenty inline `ok_or_else`/`if n == 0 { bail! }` copies plus the five outlying phrasings. | SITES: src/store/nodes/mutate.rs:52, src/store/nodes/mutate.rs:72, src/store/nodes/mutate.rs:98, src/store/nodes/mutate.rs:148, src/store/nodes/mutate.rs:180, src/store/nodes/mutate.rs:276, src/store/derived.rs:271, src/store/derived.rs:339, src/store/derived.rs:342, src/store/facts.rs:503, src/store/facts.rs:568, src/store/nodes/ratify.rs:250 [BEHAVIOR-CHANGE: skip unless deliberately chosen]
[SWEEP2:error-vocab] src/store/open.rs:113 | reuse | The crate's entry error — "there is no graph here, run `loom init`" — is written four times in production with two different wordings and two different subjects (the db path vs the search cwd). | COST: This is the first message every new user and every misconfigured driver sees, and it is not derivable from one place. `store/open.rs` and `rolelease.rs` print the resolved `.loom/graph.db` path; `commands.rs` prints the cwd it searched upward from — so the same underlying condition tells you two different things depending on whether `--graph` was passed. A test at commands.rs:758 already pins a third spelling (`no loom graph found — run `loom init``) that no production site emits. Changing the remediation (e.g. to `loom init --observed`) means finding four hand-written copies. | FIX: One `store::no_graph_error(root: &Path, searched_from: Option<&Path>) -> anyhow::Error` (or a `LoomError::NoGraph`) in `store/open.rs`, called by all four sites, so the wording and the remediation live once. | SITES: src/store/open.rs:152, src/rolelease.rs:151, src/commands.rs:349, src/commands.rs:758, src/commands.rs:763 [BEHAVIOR-CHANGE: skip unless deliberately chosen]
[SWEEP2:error-vocab] src/journey/spec.rs:427 | reuse | `nonempty(label, value)` exists and is used 25 times, but is `pub(super)` to the journey module, so eleven "must not be empty" refusals outside that module are hand-written — one of them with the negation flipped. | COST: The one emptiness refusal in the crate is locked behind a module boundary for no stated reason, so every module outside `journey` re-types the check and its prose. The vocabulary has already forked twice: `scan.rs` writes the same two strings at two different call sites each (`:100`/`:130`, `:103`/`:137`), and `domain_cmd/hypothesis.rs:145` says "must be non-empty" where everyone else says "must not be empty". `nonempty` also trims before testing; several hand-written copies do not, so `" "` is accepted in some places and refused in others. | FIX: Move `nonempty` to a crate-level helper (it takes only `&str, &str`) and make it `pub(crate)`; replace the eleven hand-written bails with `nonempty("scan adapter name", name)?` etc. | SITES: src/scan.rs:100, src/scan.rs:103, src/scan.rs:130, src/scan.rs:137, src/pattern.rs:89, src/store/nodes/lookup.rs:53, src/commands/domain_cmd/vocab.rs:41, src/commands/domain_cmd/hypothesis.rs:145, src/journey/surface_setup.rs:62, src/journey_runtime/compile.rs:488, src/journey/spec.rs:165, src/journey/spec.rs:497 [BEHAVIOR-CHANGE: skip unless deliberately chosen]
[SWEEP2:error-vocab] src/store/facts.rs:505 | simplification | "This fact is derived — sync owns it, you may not write it" is the store's core authority refusal and it is written six times in six different sentences, three of which name the remediation command and three of which do not. | COST: A driver that hits this must learn six different strings to recognise one refusal, and half of them leave it without the fix (`loom sync` / "remove its source"). The two store-plane copies (`facts.rs:505`, `facets.rs:69`) and the two command-plane copies (`edge.rs:589`, `edge.rs:666`) describe the same rule with different scope words ("its truth" / "loom computes it" / "rebuilt by" / "sync-owned"), so nothing signals that they are one policy. | FIX: One `fn derived_write_refused(subject: &str, kind: &str) -> anyhow::Error` on the store that names the subject, the owning refresh command, and the real fix ("remove its source, not the fact") — invoked by all six sites. | SITES: src/store/facts.rs:1661, src/store/facets.rs:69, src/store/nodes/mutate.rs:182, src/commands/edge.rs:589, src/commands/edge.rs:666 [BEHAVIOR-CHANGE: skip unless deliberately chosen]
[SWEEP2:json-envelopes] src/commands/challenge_cmd.rs:168 | reuse | Seven `list`-family `--json` handlers still print a bare top-level JSON array instead of routing through `commands.rs:565 pagination_envelope`, the shared list envelope the crate documents as a breaking contract change; two of them (`challenge list`, `pattern lookup`) even accept `--limit`/`--offset` and then discard the paging facts. | COST: docs/commands.md:1131 publishes "**Breaking in 0.28.0:** paginated `list --json` output is an object rather than a bare array. Rows are under `items`; `pagination` reports `offset`, `limit`, `returned`, `total`, `has_more`, and `next_offset`" — that migration note is only true for 14 of 21 list subcommands. A driver written against the published contract does `response.items[]` and gets `undefined` from `loom challenge list --json`, `loom audit disclosures --json`, `loom ignore list --json`, `loom findings --json`, `loom vocab list --json`, `loom proof rule suppressions --json`, `loom graph list --json`. Worse, `challenge list` silently truncates at `--limit` with no `total`/`has_more`, so a paging client cannot tell a full page from the end of the data. | FIX: Route each of these seven through `commands.rs:565 pagination_envelope(&rows, offset, limit, total)` (and `commands.rs:592 page_footer` for the text branch), exactly as `proof_cmd/rule.rs:133 rule_list` already does two hundred lines above `rule_suppressions`. For the four that take no `--limit`/`--offset`, pass `offset=0, limit=rows.len(), total=rows.len()` so the envelope key set is still uniform. | SITES: src/commands/audit_cmd.rs:218, src/commands/diagnostics_cmd/coverage.rs:147, src/commands/diagnostics_cmd/findings.rs:137, src/commands/domain_cmd/vocab.rs:86, src/commands/proof_cmd/rule.rs:364, src/commands/graph_cmd.rs:381, src/commands/pattern_cmd.rs:179 (pattern lookup — see envelope-3) [BEHAVIOR-CHANGE: skip unless deliberately chosen]
[SWEEP2:json-envelopes] src/commands/graph_cmd.rs:380 | simplification | Two commands change the *type* of their top-level `--json` document — bare array on the clean path, object envelope on the dirty path — so a consumer must type-sniff the response before it can read it. | COST: `loom graph list --json` returns `[...]` when no orphan shadows exist and `{linked, orphan_shadows, orphan_aliases, hint}` when they do; `loom audit --json` returns `[...]` when there are no independence warnings and `{findings, warnings}` when there are. Every consumer needs `Array.isArray(response) ? response : response.linked` — and the graph_cmd comment concedes the schema is now load-bearing in two shapes ("tests pin both shapes"), so the drift is frozen into the test suite. This is also the exact shape whiplash `pagination_envelope` was introduced in 0.28.0 to end, reproduced inside two commands rather than across them. | FIX: Always emit the object. `graph_cmd`: `{"linked": rows, "orphan_shadows": orphans.len(), "orphan_aliases": …, "hint": …}` with `orphan_shadows: 0` and `hint: null` on the clean path. `audit`: `{"findings": findings, "warnings": warnings}` unconditionally, `warnings: []` when clean. Better still, route both through `commands.rs:565 pagination_envelope` so the rows land under `items` like every other list. | SITES: src/commands/diagnostics_cmd/impact.rs:242 [BEHAVIOR-CHANGE: skip unless deliberately chosen]
[SWEEP2:json-envelopes] src/pattern.rs:16 | altitude | `PatternGuidance` is a second, independently-named pagination envelope: it restates the same six paging facts under six different key names (`offset`/`matched`/`included`/`omitted`/`items`/`lookup_command`) instead of using the crate's published `pagination` block. | COST: One crate now publishes two paging vocabularies. `total` is spelled `matched`, `returned` is spelled `included`, `has_more`/`next_offset` are collapsed into `omitted` plus a pre-rendered shell string `lookup_command`. A driver that already knows how to page `loom intent list` cannot page `loom pattern lookup` — it has to learn a second dialect and parse a command string to find the next offset. And because `lookup_command` bakes the next offset into text, the two representations can disagree the moment `guidance_page`'s clamping changes. | FIX: Have `guidance_page` return the items plus `(offset, limit, total)` and let `pattern_cmd.rs:179` wrap them with `commands.rs:565 pagination_envelope`, keeping only the genuinely pattern-specific fact (the excerpt-byte truncation) as an extra sibling key. `omitted` is derivable as `total - offset - returned`; `lookup_command` is `page_footer`'s job (commands.rs:592), not a serialized field. | SITES: src/commands/pattern_cmd.rs:179, src/commands.rs:565 [BEHAVIOR-CHANGE: skip unless deliberately chosen]
[SWEEP2:json-envelopes] src/commands/wiki.rs:282 | simplification | The `removed` key is emitted by 22 delete handlers in three different JSON types — boolean `true` at 14 sites, a bare id/name string at 6, and an array of strings at 1 — with no shared removal envelope. | COST: `if (out.removed)` works everywhere; `out.removed === true` breaks on `loom wiki remove`, `loom inbox remove`, `loom question remove`, `loom pattern remove`, `loom pattern exemplar remove`, `loom scan adapter remove`, and `loom hook remove`. And a client that wants to know *what* was removed has to look in a differently-named sibling key per command — `intent`, `codefile`, `journey`, `surface`, `hypothesis`, `rule`, `validation`, `note`, `task`, `proposal`, `edge`, `glob`, `term` — thirteen names for one slot. Nothing in the crate names the removal contract, so each new delete command invents a fourteenth. | FIX: Add one `pub(crate) fn removed_envelope(kind: NodeType, removed: &Node, extra: serde_json::Value) -> Value` beside `commands.rs:541 node_json` emitting `{"removed": true, "subject": node_json(removed)}` merged with `extra`, and route all 22 sites through it. Keep `removed` boolean-only; move the id/name payloads into `subject`. | SITES: src/commands/capture_cmd.rs:348, src/commands/capture_cmd.rs:549, src/commands/capture_cmd.rs:663, src/commands/capture_cmd.rs:919, src/commands/pattern_cmd.rs:221, src/commands/pattern_cmd.rs:362, src/commands/diagnostics_cmd/scan_config.rs:94, src/commands/hook_cmd.rs:141, src/commands/edge.rs:635, src/commands/proposal_cmd.rs:189, src/commands/codefile_cmd.rs:258, src/commands/proof_cmd/validation.rs:331 [BEHAVIOR-CHANGE: skip unless deliberately chosen]
[SWEEP2:json-envelopes] src/commands/proposal_cmd.rs:67 | reuse | The node-summary row is hand-written at 19 more sites in five mutually incompatible key subsets, on top of the two `node_json` helpers that already exist — three of those copies are byte-identical inside a single file. | COST: `commands.rs:541 node_json` emits 9 keys and `commands/journey/support.rs:92 node_json` emits 6; these 19 sites add `{id,name}`, `{id,name,status}`, `{id,name,status,description,body}`, `{id,name,status,body,created_at,…}` and `{id,name,status,edge_status}` on top. So the JSON key `intent`/`proposal`/`validation` carries a different node shape depending on which subcommand produced it, and adding a field to `Node` (or renaming one) means auditing 21 emit sites to find which ones are supposed to carry it. `proposal_cmd.rs` alone repeats the same 5-key literal three times (:67 add, :93 list, :136 show), so `loom proposal add` and `loom proposal show` can silently diverge from `loom proposal list`. | FIX: Collapse to one projection with an explicit field selector — e.g. `node_json(&n)` for the full row and a single `node_ref(&n) -> {id, name, status}` beside it in `commands.rs` — and route all 19 sites through the pair. The three `proposal_cmd` copies become one call each immediately. | SITES: src/commands/proposal_cmd.rs:93, src/commands/proposal_cmd.rs:136, src/commands/capture_cmd.rs:808, src/commands/discover_cmd.rs:418, src/commands/discover_cmd.rs:427, src/commands/discover_cmd.rs:435, src/commands/domain_cmd/hypothesis.rs:329, src/commands/edge.rs:925, src/commands/edge.rs:933, src/commands/graph_cmd.rs:327, src/commands/graph_cmd.rs:332, src/commands/intent/commands.rs:83 [BEHAVIOR-CHANGE: skip unless deliberately chosen]
[SWEEP2:json-envelopes] src/mcp.rs:887 | reuse | An MCP tool failure builds its own `{"error": …}` document instead of the crate's shared failure envelope `commands.rs:315 json_error_envelope`, so the same command failure reaches a CLI driver and an MCP driver under different keys — and the MCP path silently discards both the anyhow context chain and any command-attached `JsonErrorEnvelope`. | COST: Every MCP tool is a thin wrapper over a CLI command value (`status_value`, `next_output`, `served_context`, `impact_report`, `observe_run`, `apply_value`), yet their failure shapes are unrelated: CLI gives `{"status":"error","detail":"<full anyhow chain via {error:#}>"}` and MCP gives `{"error":"<Display of the outermost error only>"}`. `e.to_string()` drops every `.context(...)` layer, so an MCP client sees "no such node" where the CLI shows the full "resolving --for target … : no such node" chain. Worse, `journey/runtime.rs:526` attaches a rich `JsonErrorEnvelope` for `--json` failures; the MCP branch never downcasts for it, so that structured failure document is unreachable through the transport the tool schemas are published for. | FIX: Replace `json!({ "error": e.to_string() })` with `crate::commands::json_error_envelope(&e)` (already `pub`, already downcasts the attached envelope and already formats `{error:#}`), keeping `isError: true` on the `tool_content` wrapper. | SITES: src/commands.rs:315, src/commands.rs:330, src/commands/journey/runtime.rs:526 [BEHAVIOR-CHANGE: skip unless deliberately chosen]
[SWEEP2:json-envelopes] src/commands/diagnostics_cmd/impact.rs:310 | simplification | `loom absorb --json` emits three different envelopes for one command — a 3-key document on the empty path, a 5-key document on the populated path, and a third 3-key set from its MCP twin — and the empty path is the only JSON print in the crate that skips `to_string_pretty`. | COST: A driver that reads `out.ready`/`out.needs_you` gets `undefined` exactly when the graph is clean, which is the case it most wants to branch on; a driver that pretty-prints or diffs output sees the empty case arrive as one compressed line while all ~104 other JSON prints are indented. The MCP twin at `mcp.rs:252` returns yet another key set (`items`/`ready`/`needs_you`, no `proposal`), so the same observation reaches a CLI driver and an MCP driver under two schemas. | FIX: Build the envelope once — `{"proposal": …, "persisted_proposal": …, "items": items, "ready": ready, "needs_you": items.len() - ready}` with nulls and zeroes on the empty path — and print it with `serde_json::to_string_pretty` like every sibling. Then have the `loom_absorb` MCP tool call that one function instead of recomputing `ready`/`needs_you` itself. | SITES: src/commands/diagnostics_cmd/impact.rs:331, src/mcp.rs:252 [BEHAVIOR-CHANGE: skip unless deliberately chosen]
[SWEEP2:json-envelopes] src/mcp.rs:847 | reuse | Two JSON/serialized version fields call `env!("CARGO_PKG_VERSION")` inline instead of `lib.rs:86 CRATE_VERSION`, the named constant whose doc comment exists precisely to keep this value distinguishable from `SCHEMA_VERSION`. | COST: `CRATE_VERSION` is the crate's declared name for "the version of this binary" and ten sites use it (`commands.rs:368`, `commands.rs:378`, `store/open.rs:138`, `store/schema.rs:436/455/461/477`). These two do not, so a grep for `CRATE_VERSION` — the obvious way to audit every place the binary stamps its own version into a published document — misses the MCP `serverInfo.version` handshake and the observation evidence record's `loom_version`, the two that external consumers actually read. Any future indirection on that constant (a `-dev` suffix, a build-metadata strip) silently skips both. | FIX: `"version": crate::CRATE_VERSION` at mcp.rs:847 and `loom_version: crate::CRATE_VERSION.to_string()` at runner.rs:174. | SITES: src/runner.rs:174
[SWEEP2:json-envelopes] src/commands/status_cmd.rs:344 | altitude | The `compass` sub-envelope is hand-projected out of `maturity::Ladder` at four sites in three modules under three different key sets — one with `rung`, two without, and one that flattens the same fields to the document's top level under different names. | COST: `Ladder` is already `Serialize` and `status_cmd.rs:349` emits it whole as `"maturity"`, so `phase` and `next_command` are published twice per `loom status --json` document from two independent code paths. A consumer reading `compass.rung` works against `loom status` and breaks against `loom closeout` and `loom door`; a consumer reading `compass.phase` breaks against `loom welcome`, which spells the same two facts as top-level `phase`/`next_command`. Adding a compass field (Ladder already carries `truth_axis` and `derived_floor`) means editing four literals and deciding four times whether this projection wants it. | FIX: One `pub(crate) fn compass(ladder: &Ladder) -> serde_json::Value` in `commands/pulse.rs` beside the other shared envelope helpers, emitting a fixed key set (`phase`, `rung`, `next_command`), called from all four sites — including `orient_cmd::welcome`, which should nest it as `compass` rather than flattening. | SITES: src/commands/status_cmd.rs:649, src/commands/capture_cmd.rs:134, src/commands/orient_cmd.rs:69 [BEHAVIOR-CHANGE: skip unless deliberately chosen]
[SWEEP2:tests-quality] tests/common/mod.rs:20 | reuse | The "unique temp dir named pid+nanos+counter, rmdir on Drop" harness is written 14 separate times — 10 private copies inside src/ cfg(test) mods, 3 inside tests/ files that already have the canonical one in scope, and the canonical one itself. | COST: Every copy carries its own counter/nonce scheme, and they have already drifted into three incompatible behaviours: `src/divergence.rs:574` never calls `create_dir_all` at all (only `remove_dir_all`), `src/scan.rs:816` returns `Result` and pre-seeds `src/lib.rs`, and `src/prescan.rs:174` omits the atomic counter so two same-nanosecond fixtures collide. A fix to the collision scheme (or to the observer-agent isolation that only `tests/common/mod.rs:11 isolate_from_observer_agent` performs) reaches exactly one of the 14. The three tests/ copies are the worst case: `tests/ring48_journey_cli.rs`, `tests/ring57_journey_trust_boundary.rs` and `tests/post_update_defects.rs` are in the same test target family as `tests/common/mod.rs`, so they are re-declaring a type they could `mod common; use common::Tmp;` for free. | FIX: Promote one form. For tests/: delete the three private `Tmp` structs and add `mod common; use common::Tmp;` (add the `label`/`prefix` argument to `common::Tmp::new` since ring48/ring57/post_update pass one). For src/: add a `#[cfg(test)] pub(crate) mod testutil;` exporting a single `TmpRoot::new(prefix)` with `path()`/`write()`, and delete the 10 in-module copies. | SITES: src/fsglob.rs:113, src/thresholds.rs:315, src/batch_auth.rs:752, src/store/mod.rs:157, src/store/schema.rs:587, src/store/open.rs:579, src/divergence.rs:574, src/store/facts.rs:1973, src/prescan.rs:174, src/scan.rs:816, tests/ring48_journey_cli.rs:13, tests/ring57_journey_trust_boundary.rs:61
[SWEEP2:tests-quality] tests/ring5.rs:1678 | reuse | `fn loom_bin() -> PathBuf` is re-declared in 12 test files, in two mutually incompatible implementations: 10 return `env!("CARGO_BIN_EXE_loom")`, while `tests/ring27_observe.rs:96` and `tests/ring30_runner.rs:29` walk `current_exe()` up out of `deps/` to guess the path. | COST: Two different answers to "where is the binary under test", both live. The `current_exe()` form silently resolves to a stale or wrong binary under a custom `CARGO_TARGET_DIR` or a cross-compiled `--target` layout, and no test names the discrepancy; the two files using it will diverge from the other ten under exactly the CI conditions where the difference matters. Ten of the twelve files also already declare `mod common;`, whose `loom_command()` (tests/common/mod.rs:55) resolves the binary AND strips the inherited `LOOM_AGENT`/`LOOM_AGENT_PROFILE` — so every hand-rolled `loom_bin()` call site quietly opts out of that isolation. | FIX: Delete all 12 `loom_bin` definitions and route every spawn through `common::loom_command()`, which already resolves the path and does the env isolation. Where a raw path is genuinely needed, expose one `pub fn loom_bin()` in `tests/common/mod.rs` and use it. | SITES: tests/debt_promotion.rs:19, tests/ring15_context.rs:13, tests/ring27_observe.rs:96, tests/bootstrap.rs:11, tests/ring9.rs:1268, tests/ring30_runner.rs:29, tests/ring11_roles.rs:1302, tests/ring8.rs:620, tests/find_explain.rs:9, tests/ring12_federation.rs:15, tests/ring55_journey_operation_exercises.rs:2630
[SWEEP2:tests-quality] tests/ring12_federation.rs:19 | reuse | `fn loom_init(root, name)` is re-declared in 8 test files, every one of which already writes `mod common;` — and `tests/common/mod.rs:65` exports exactly that function. | COST: Eight copies of "spawn `loom init <root> [--name n]` and assert success" that the file already has in scope. They have drifted: `tests/find_explain.rs:13` hard-codes `--name t` and asserts success with no diagnostic at all, `tests/bootstrap.rs:15` takes `&str` instead of `Option<&str>`, and only `common::loom_init` goes through `loom_command()` so only it strips the inherited `LOOM_AGENT`. When `loom init`'s argv changes, seven of the eight need a separate edit that nothing points at. | FIX: Delete the eight local `loom_init`/`init` definitions and call `common::loom_init(root, name)`, which is already imported by `use common::*;` in each of these files. | SITES: tests/ring11_roles.rs:1306, tests/ring5.rs:1684, tests/ring9.rs:1272, tests/ring8.rs:624, tests/bootstrap.rs:15, tests/find_explain.rs:13, tests/ring15_context.rs:38
[SWEEP2:tests-quality] tests/ring12_federation.rs:46 | reuse | "Run `loom --graph <root> <args> --json`, assert exit zero, parse stdout as JSON, panic with stdout/stderr on failure" is written out 18 times across 16 test files, under six different names (`loom_json`, `loom_json_global`, `loom_json_out`, `run_cli_json`, `cli_json`, `builder_json`, `builder`, and one confusingly named `loom_command`). | COST: 18 slightly different answers to the same question, with no shared home. They already disagree on things that matter to every one of them: `tests/ring27_observe.rs:105` never asserts the exit status at all, `tests/ring49_journey_runtime.rs:2891` falls back to wrapping raw stdout in `json!({"stdout": ...})` instead of failing on unparseable output, `tests/ring44_v12_journey_schema.rs:19` `.unwrap()`s the parse with no diagnostic, and only `tests/ring58_trust_surfaces.rs:12` and `tests/ring49_journey_runtime.rs:2891` clear the inherited `LOOM_AGENT`. Adding a flag to the CLI harness (a new env var, a timeout, a `--graph` rename) is an 18-site edit that nothing enumerates, and the debuggability of a failing CI run depends on which copy the test happened to use. | FIX: Add one `pub fn loom_json(root: &Path, args: &[&str]) -> serde_json::Value` (built on the existing `common::loom_command()`) plus a `loom_raw`/`loom_ok` sibling to `tests/common/mod.rs`, and delete the 18 local definitions. Every one of these files except `tests/ring48_journey_cli.rs`, `tests/ring49_journey_runtime.rs` and `tests/post_update_defects.rs` already declares `mod common;`. | SITES: tests/debt_promotion.rs:39, tests/debt_promotion.rs:63, tests/ring11_roles.rs:1348, tests/ring15_context.rs:34, tests/ring42_edge_lint.rs:15, tests/ring43_retarget.rs:19, tests/ring44_v12_journey_schema.rs:19, tests/ring45_judgment_inbox.rs:26, tests/ring58_trust_surfaces.rs:12, tests/ring8.rs:640, tests/ring9.rs:1292, tests/ring5.rs:1705
[SWEEP2:tests-quality] tests/ring1.rs:92 | simplification | The two-line opener `let tmp = Tmp::new(); let store = Store::init(tmp.path(), Some("t"), false).unwrap();` appears verbatim 321 times across tests/, and the `Tmp::new()` + `Store::init(...)` pair 403 times — while `tests/common/mod.rs` exports a `Tmp` and a `codefile` helper but no `fresh_store()`. | COST: 642 lines of pure boilerplate whose only content is `Some("t"), false`. Every change to `Store::init`'s signature — a third argument, a Result shape, an option struct — is a 403-site mechanical edit across 60 files. `src/thresholds.rs:344` already wrote the correct helper for the in-crate copy of the same pattern and it is not shared, so the fix exists in the repo and is used once. | FIX: Add `pub fn fresh_store() -> (Tmp, Store)` (and a `fresh_store_named(name)` for the ~80 sites that pass something other than `"t"`) to `tests/common/mod.rs` — the exact shape of `src/thresholds.rs:344 fn fresh_store() -> (TmpRoot, Store)` — and collapse the 321 verbatim pairs to `let (tmp, store) = fresh_store();`. | SITES: tests/ring6.rs:37 call sites, tests/ring12_phase1.rs:33 call sites, tests/ring5.rs:30 call sites, tests/ring10.rs:28 call sites, tests/ring1.rs:28 call sites, tests/ring25_audit_deepen.rs:20 call sites, tests/ring4.rs:19 call sites, tests/ring3.rs:19 call sites, tests/ring9.rs:17 call sites, tests/ring13_precision.rs:14 call sites, tests/ring35_locator_drift.rs:13 call sites, tests/ring14_ratify.rs:13 call sites
[SWEEP2:tests-quality] tests/ring23_chokepoint.rs:31 | reuse | `fn source_files() -> Vec<(String, String)>` — a recursive `read_dir` walk of `src/` collecting `(relative path, file body)` — is copied verbatim (one `.to_string()` vs `.replace('\\', "/")` apart) into two structural tests, and two more files hand-roll the same walk with different collection types. | COST: Both structural tests (ring23's chokepoint rule, ring65's ring-direction rule) exist to catch architecture drift by reading the source tree; the reader they depend on is duplicated, so the two rules can silently start disagreeing about what "loom's sources" are. They already do: only `tests/ring65_ring_direction.rs:56` normalises Windows separators, so the same path string compares differently against `DRIVER_PLANE` than against `CHOKEPOINT`. Adding a directory to skip (say `src/generated/`) or a new source extension requires finding four unrelated implementations. | FIX: Move one `pub fn source_files() -> Vec<(String, String)>` into `tests/common/mod.rs` (keeping the `replace('\\', "/")` normalisation and the `out.len() > 30` sanity assert) and have ring23, ring65, terminology.rs and intake_fidelity.rs call it with the directory list they need. | SITES: tests/ring65_ring_direction.rs:55, tests/terminology.rs:44, tests/terminology.rs:94, tests/intake_fidelity.rs:30
[SWEEP2:tests-quality] tests/ring43_validation_specific_strength.rs:899 | simplification | Inside a single test function, the identical 16-line recursive walk of `src/` is written twice back to back — once to copy the files into a temp root, once to register them as CodeFile nodes — differing only in the two lines at the bottom of the loop body. | COST: Two `read_dir` traversals of the whole `src/` tree per run of this test where one would do, and, more importantly, the two loops must stay in exact agreement about which files they visit: if the copy loop and the register loop ever diverge on a filter, the fixture registers CodeFiles that were never copied (or copies files it never registers) and the call-graph assertion below silently measures a different tree than it built. Nothing enforces the agreement. | FIX: Extract one local `let rust_sources: Vec<(PathBuf, String)> = collect(&src_root);` before both loops (or reuse the shared walker from residue-7) and iterate it twice, so the two passes cannot disagree about the file set. | SITES: tests/ring43_validation_specific_strength.rs:922
[SWEEP2:tests-quality] tests/common/mod.rs:88 | simplification | `common::prove` — 32 lines of helper plus a 9-line doc comment explaining the evidence-spine rationale — has zero callers; it compiles only because of the `#[allow(dead_code)]` on the line above it. | COST: A documented fixture nobody uses, kept compiling by an annotation that exists to hide exactly this. Because it is `#[allow(dead_code)]` in an integration-test crate, no lint will ever report it, so it will keep being maintained alongside `Command::Validation` grammar changes (`ValidationCmd::Add { name, r#type, command, intent }` is spelled out by hand) for no test's benefit. It also actively misleads: its doc comment reads as the canonical way fixtures should earn a proof, but every test that needs one calls `s3_journey_proof`/`prove_s2` instead. | FIX: Delete `tests/common/mod.rs:86-112` (the `#[allow(dead_code)]`, doc block and `pub fn prove`). If the documented rationale is worth keeping, move that paragraph onto `prove_s2`, which is the helper tests actually use. | SITES: 
[SWEEP2:tests-quality] tests/ring18_journey_baseline.rs:16 | simplification | The minimal Journey-spec JSON envelope — `"schema": JOURNEY_SCHEMA` / `"inputs": {}` / `"preconditions": []` / `"profiles":{"proof":{"inputs":{},"workspace":{}}}` — is hand-written 33 times across 11 test files (plus once inside `tests/common/mod.rs` itself), including four as single-line raw string literals. | COST: Every field in that envelope is scaffolding no test asserts on; only `id`, `name` and `steps` differ. When the Journey schema gains a required key or renames `profiles`, all 33 literals must be edited by hand, and the four raw-string forms in `tests/ring5.rs` (`r#"{"schema":"loom.journey/v1",...}"#`) also hard-code the schema string instead of `JOURNEY_SCHEMA`, so they will not even fail to compile — they will fail at parse time, one at a time. `tests/common/mod.rs:203-214` already builds this exact envelope inside `s3_journey_proof_with_ratification` and does not expose it. | FIX: Extract `pub fn journey_spec_json(id: &str, name: &str, steps: serde_json::Value) -> serde_json::Value` from `tests/common/mod.rs:203` and have the 33 sites call it, overriding only the fields they are actually testing (`spec["actor"] = ...`, `spec["steps"] = ...`). Replace the four `r#"..."#` literals in ring5 with `serde_json::to_string(&journey_spec_json(..))`. | SITES: tests/ring4.rs:996, tests/ring40_packet_closure.rs:696, tests/ring45_semantic_journey.rs:22, tests/ring47_journey_integration.rs:750, tests/ring49_journey_runtime.rs:733, tests/ring50_journey_doctor.rs:42, tests/ring55_journey_operation_exercises.rs:241, tests/ring55_journey_operation_exercises.rs:1530, tests/ring55_journey_operation_exercises.rs:1619, tests/ring57_journey_trust_boundary.rs:61, tests/ring58_surface_manifest_validate.rs:89, tests/ring5.rs:729
