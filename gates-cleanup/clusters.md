# Fix clusters — grouped by the mechanism one fix closes

Source: 22 solo + 74 wave-1 + 58 wave-2 findings, all adversarially verified. 154 total.
Every cluster is PENDING until marked APPLIED or SKIPPED with a reason. Gate G20 checks for zero PENDING.
Rule: fix the MECHANISM once across every site. A half-fixed duplication is worse than an untouched one.

## A-key-constants  —  6 findings, 136 corroborating sites

- [x] STATUS: PARTIALLY APPLIED 2026-08-23 — the `locator` facet key is done: `model::LOCATOR_FACET` added, `Store::edge_locator` added, 24 inlined three-argument reads collapsed across 14 files, all 8 remaining raw literals routed through the const; zero raw facet-key literals left in src/. CORRECTION TO THE FINDING: it claimed 62 sites; only 30 are the facet key — the other 32 are JSON output keys, `RunProducer::Locator => "locator"`, test fixtures and schema prose, and replacing those would have been wrong. CORRECTION TO THE FIX: the finding proposed locator.rs as the const's home, but store/facets.rs:74 needs the key and store sits BELOW locator, so it went in model.rs to avoid a ring inversion. clippy 0, tests 1304/0. ALSO DONE 2026-08-23: `store::LAYER_ORDER_META_KEY` added beside PORTABLE_META_KEYS (the allowlist entry is the one that must match exactly, or the key silently stops travelling in the export) and routed through all 6 production sites; only the const itself and one travel.rs test fixture still spell it. STILL PENDING: journey provenance facet keys (registry.rs:161), journal event names (nodes/ratify.rs:227), the rest of PORTABLE_META_KEYS.   (0 behavior-changing, default SKIPPED)

  - `src/locator.rs:699` (w1, reuse, 54 sites) — The edge facet key "locator" is a bare string literal at 62 sites across 27 files, and the read idiom `store.get_facet(&edge.id, TargetKind::Edge, "locator")?` is hand-inlined at 24 of them even though `commands/context_cmd.rs:505` already wraps it as `edge_locator`.
  - `src/commands/journey/registry.rs:161` (w1, reuse, 34 sites) — The provenance-facet vocabulary on `Surfaces`/`Derives` edges — "journey_hash", "step_ids", "operation_bindings", "setup", "proposal_id", "manifest_hash" — is written as bare literals at 19, 19, 13, 10, 12 and 12 sites spanning 15, 7, 7, 5, 3 and 2 files, with no const anywhere; three of the readers are near-identical projection loops.
  - `src/store/nodes/ratify.rs:227` (w1, reuse, 23 sites) — Journal event names are bare literals wherever the writer and its reader live in different files, and the `"rejected" -> "rejection" else "ratification"` mapping is a verbatim five-line duplicate in two files — while `audit.rs`, `batch_auth.rs` and `evidence.rs` already name their event strings as consts.
  - `src/store/mod.rs:92` (w1, altitude, 14 sites) — `PORTABLE_META_KEYS` re-spells all eight meta keys as literals — including the three that already have consts (`evidence_policy`, `thresholds`, `scan_adapters`) — while the other five (`ignores`, `codefile_globs`, `observed_globs`, `upstream_graphs`, and the unlisted `upstream_hashes`) have no const and are bare at 21 sites across 9 files.
  - `src/store/mod.rs:91` (w1, reuse, 8 sites) — `PORTABLE_META_KEYS` re-spells as bare literals three keys that already have named constants elsewhere, and the other five keys in the list have no constant at all — so "is this setting portable?" is answered by string matching across seven files.
  - `src/coverage.rs:27` (w1, simplification, 3 sites) — `CoverageScopeSummary::in_scope` is a stored field that is literally `owned + unowned_files.len()` — in a struct that already exposes the same derived quantities as methods rather than fields.

## B-enum-vocab  —  9 findings, 108 corroborating sites

- [ ] STATUS: PARTIALLY APPLIED 2026-08-23, verified clippy 0 / tests 1304-0. DONE: (a) `str_enum!` exported crate-wide (`pub(crate) use str_enum;` in model.rs) — it was textually scoped, which is WHY the two holdouts hand-rolled it; (b) `registry::OwnerRole` -> str_enum!, gaining ALL/FromStr/Display/serde; (c) `truth::TruthAxis` -> str_enum!, deleting its hand-written as_str AND its `#[serde(rename_all)]` — `TRUTH_AXES` kept as a one-line alias to `TruthAxis::ALL` because tests/ring9.rs names it; (d) `identity::Agent::parse` now parses via `OwnerRole::FromStr` and builds its error from ALL — the two byte-identical bail! sentences are gone; (e) `policy::GATEABLE_ROLES` -> `gateable_roles()` derived from ALL; (f) `grammar::ACTIVE_LIFECYCLES`/`ALL_LIFECYCLES` deleted, replaced by `IntentLifecycle::active_names()`/`all_names()` in model.rs (grammar cannot import model — model re-exports grammar::is_placeholder); (g) `ratification::ASSERTED_STATES` deleted — 0 callers, byte-identical to grammar::RATIFICATION_STATES; (h) thresholds' five gate names went from FOUR spellings (GATES const, pairs(), set_gate match, reset_gate match) to one `Gate` enum; `GATES` kept as public API but now built from `Gate::as_str` (made const fn) so tests/ callers are untouched. STILL PENDING: the 23 bare-String clap vocabularies (behavior-changing), the compiled-Validation predicate spelled 7x (sync.rs:429), the 7-value finding-verdict vocabulary with no enum (store/facts.rs:1666), ClaimRoleArg, and the 11 remaining serde-rename_all/as_str pairs.

  - `src/cli/subcommands/proof.rs:11` (w2, altitude, 23 sites) **[behavior-change]** — Twenty-three closed vocabularies across eight `cli/subcommands/*.rs` files are declared as bare `String` args with the valid values written only in a doc comment — while the crate has `str_enum!` types for most of them and `clap::ValueEnum` is used three files over.
  - `src/sync.rs:429` (w1, altitude, 19 sites) — The "is this compiled Validation still current for its Journey?" predicate — the same four/five body keys `type` / `journey_hash` / `surface_hash` / `profile` / `compiler_version` compared against the same right-hand sides — is written out longhand in seven files, each with its own spelling of the `.body.get(K).and_then(as_str)` chain.
  - `src/store/facts.rs:1666` (w1, reuse, 14 sites) — The 7-value finding-verdict vocabulary has no enum at all: it is hand-spelled as raw &str lists or match arms in 11 places across 6 modules, while model.rs:45 str_enum! generates exactly that surface for 16 sibling vocabularies.
  - `src/risk.rs:28` (w1, reuse, 12 sites) — Thirteen enums pair `#[serde(rename_all = "...")]` with a hand-written `as_str` that re-derives exactly the same strings — two independent mechanisms producing one vocabulary, in twelve modules.
  - `src/risk.rs:28` (w1, reuse, 12 sites) — EXTENDS the existing truth.rs/registry.rs `str_enum!` finding: eleven more enums across nine modules carry BOTH `#[serde(rename_all = "snake_case")]` and a hand-written `as_str` that reproduces exactly the same strings, while `model.rs:45 str_enum!` generates as_str/ALL/FromStr/Display/serde from one variant list for seventeen enums.
  - `src/cli/subcommands/ops.rs:212` (w1, reuse, 11 sites) — Extends the known cli.rs:445 lane/role cluster: `ClaimRoleArg` is a seventh independent spelling of the role vocabulary, with its own hand-written variant→OwnerRole bridge.
  - `src/grammar.rs:19` (w1, reuse, 7 sites) — `ACTIVE_LIFECYCLES` and `ALL_LIFECYCLES` restate the five variants of the `IntentLifecycle` str_enum as raw &str consts, and `loom schema` publishes both spellings side by side in one JSON document.
  - `src/ratification.rs:27` (w1, simplification, 6 sites) — `ratification::ASSERTED_STATES` is dead — zero references anywhere in the crate — and is a byte-identical copy of `grammar::RATIFICATION_STATES`, doc comment included.
  - `src/thresholds.rs:87` (w1, simplification, 4 sites) — The five threshold gate names are spelled four times in production code — a `GATES` const, `pairs()`, `set_gate`'s match, and `reset_gate`'s match — with no enum, and a test exists solely to guard the agreement.

## C-limits-registry  —  5 findings, 44 corroborating sites

- [x] STATUS: APPLIED 2026-08-23 — registry 17 -> 29 entries. Registered: journey_timeout_secs, max_argv_tokens, max_argv_bytes, max_operation_nesting, max_adversarial_review_frontier, validation_excerpt_bytes, journey_stream_excerpt_bytes, journey_failure_diagnostic_bytes, release_diagnostic_bytes, git_output_cap, finding_title_chars, max_packet_notes. Seven constants widened to pub(crate); workitem NOTE_CAP lifted from function-local to module scope. SKIPPED with reason: CO_CHANGE_MAX_TRACKED / _RAW_PATHS / _PAIR_MEMBERS — advisory debt-signal tuning, never worker-facing, and two of that family are already registered. clippy 0, tests 1304/0.   (3 behavior-changing, default SKIPPED)

  - `src/limits.rs:5` (w1, altitude, 20 sites) **[behavior-change]** — limits.rs claims to be the single list that "cannot drift from enforcement", but at least fourteen enforced caps, timeouts and budgets in src/ are absent from `all()` — including a 2700s step killer, three byte budgets, five co-change caps and four candidate-surface caps.
  - `src/release/section_06.rs:223` (w1, reuse, 10 sites) — "Cap captured output, keep both ends, disclose the omission" is implemented six independent times across the crate — two of them byte-for-byte identical — with four different omission markers and two separately-declared 8192-byte constants.
  - `src/checkpoint.rs:883` (w1, reuse, 7 sites) — The byte-capped, timeout-controlled git invocation harness exists twice — checkpoint.rs and signal/debt/git.rs — with two identical 16 MiB caps under two names, two identical 10 s timeouts, two identical stdout-filter closures, and two identical post-run cap re-checks; neither goes through `src/subprocess.rs`, the crate's process chokepoint.
  - `src/candidate_surface_policy.rs:553` (w1, altitude, 4 sites) **[behavior-change]** — Three enforced caps in candidate_surface_policy (and checkpoint's git output cap) bail on violation but are absent from `limits::all()`, and their refusals do not name the limit or its value — the exact contract limits.rs claims to hold.
  - `src/cli/subcommands/intent.rs:11` (w1, altitude, 3 sites) **[behavior-change]** — `loom intent dependents` is documented as the intent-graph twin of `loom impact`, but its walk depth kept a bare literal 5 with no named default, no maximum, no clamp and no limits entry — the impact-depth hardening was applied only to the code-graph half.

## D-json-envelope  —  9 findings, 87 corroborating sites

- [ ] STATUS: PENDING   (8 behavior-changing, default SKIPPED)

  - `src/commands/status_cmd.rs:378` (w1, simplification, 43 sites) — `println!("{}", serde_json::to_string_pretty(&x)?)` is written 56 times across 40 command handlers with no shared emitter, even though the sibling envelope helper `pagination_envelope` was centralized.
  - `src/commands/wiki.rs:282` (w2, simplification, 21 sites) **[behavior-change]** — The `removed` key is emitted by 22 delete handlers in three different JSON types — boolean `true` at 14 sites, a bare id/name string at 6, and an array of strings at 1 — with no shared removal envelope.
  - `src/commands/challenge_cmd.rs:168` (w2, reuse, 7 sites) **[behavior-change]** — Seven `list`-family `--json` handlers still print a bare top-level JSON array instead of routing through `commands.rs:565 pagination_envelope`, the shared list envelope the crate documents as a breaking contract change; two of them (`challenge list`, `pattern lookup`) even accept `--limit`/`--offset` and then discard the paging facts.
  - `src/commands/journey/support.rs:92` (w1, simplification, 5 sites) **[behavior-change]** — Two different `pub(crate) fn node_json(&Node) -> Value` exist with different output shapes, and the journey module's glob import silently shadows the crate-wide one — so the same JSON key carries a 6-field node in journey handlers and a 9-field node everywhere else.
  - `src/mcp.rs:887` (w2, reuse, 3 sites) **[behavior-change]** — An MCP tool failure builds its own `{"error": …}` document instead of the crate's shared failure envelope `commands.rs:315 json_error_envelope`, so the same command failure reaches a CLI driver and an MCP driver under different keys — and the MCP path silently discards both the anyhow context chain and any command-attached `JsonErrorEnvelope`.
  - `src/commands/status_cmd.rs:344` (w2, altitude, 3 sites) **[behavior-change]** — The `compass` sub-envelope is hand-projected out of `maturity::Ladder` at four sites in three modules under three different key sets — one with `rung`, two without, and one that flattens the same fields to the document's top level under different names.
  - `src/pattern.rs:16` (w2, altitude, 2 sites) **[behavior-change]** — `PatternGuidance` is a second, independently-named pagination envelope: it restates the same six paging facts under six different key names (`offset`/`matched`/`included`/`omitted`/`items`/`lookup_command`) instead of using the crate's published `pagination` block.
  - `src/commands/diagnostics_cmd/impact.rs:310` (w2, simplification, 2 sites) **[behavior-change]** — `loom absorb --json` emits three different envelopes for one command — a 3-key document on the empty path, a 5-key document on the populated path, and a third 3-key set from its MCP twin — and the empty path is the only JSON print in the crate that skips `to_string_pretty`.
  - `src/commands/graph_cmd.rs:380` (w2, simplification, 1 sites) **[behavior-change]** — Two commands change the *type* of their top-level `--json` document — bare array on the clean path, object envelope on the dirty path — so a consumer must type-sniff the response before it can read it.

## E-efficiency  —  20 findings, 121 corroborating sites

- [x] STATUS: APPLIED 2026-08-23 — DONE: the 7 sites that loaded an entire table just to call `.len()` now use the store's existing `count_nodes`/`count_edges` COUNT queries (status_cmd.rs x4, orient_cmd.rs x3; zero `usize::MAX)?.len()` sites remain). VERIFIED: clippy 0, tests 1304/0. CORRECTION TO THAT FINDING: it proposed ADDING count_nodes/count_edges; the verifier caught that they already exist (store/nodes/lookup.rs:281, store/edges.rs:605) and codefile_cmd.rs already used them. REMAINING (deliberately not started): the snapshot-reuse refactor. `loom status` builds the whole-graph snapshot ~5x via maturity::ladder_and_depths -> smells, audit::backlog -> doctor+smells, plus doctor and layer_detector_state directly. CORRECTION TO THAT FINDING: it claims signal::doctor 'does all its work off snap and nothing else' — doctor uses `store` 7 further times, so the fix is threading a (&Store, &Snapshot) pair through maturity.rs and audit.rs, not changing signatures to &Snapshot. APPLIED 2026-08-23: added `_with(&Store, &Snapshot)` variants for smells, doctor, layer_detector_state, audit::backlog, LadderInputs::gather and ladder_and_depths; each existing public fn is now a one-line wrapper that builds the snapshot, so no standalone caller changed. gather_status_facts builds ONE snapshot and threads it: status_cmd.rs:311 is now the only snapshot() reachable from `loom status` (was 5). Verified: cargo check 0, clippy 0, tests 1304/0. Cost of the route: the body split left 23 clippy needless-borrow warnings, the first repair regex was too broad and broke 10 field borrows, and one over-correction broke an iterator chain — three passes over two files to land one edit.   (0 behavior-changing, default SKIPPED)

  - `src/store/edges.rs:271` (w2, efficiency, 23 sites) — The "is this a live realizing grounding?" predicate is two-to-three separate single-key facet SELECTs on the SAME edge, hand-inlined in loops at 20 sites, where the store's existing `facets_of` would answer all of them in one query.
  - `src/commands/status_cmd.rs:306` (w1, efficiency, 12 sites) — `loom status` builds the FULL graph snapshot five times — `snapshot()` is the whole-graph read (nodes + edges + facts + one `evidence_for` query per fact + all facets + tags + meta), and five independent callers each build their own private copy on one command path.
  - `src/coverage.rs:98` (w2, efficiency, 9 sites) — `unowned_codefiles` issues one `edges_with` query PER registered CodeFile and then two-to-five facet/node SELECTs per incident edge — and its own caller `coverage_scope_summary` re-reads the whole CodeFile roster a second time immediately afterwards.
  - `src/commands/status_cmd.rs:310` (w2, efficiency, 9 sites) — Four whole-table materializations — every Journey, Intent, CodeFile node and every edge, each row JSON-parsed — are performed solely to call `.len()` on them, and the store already demonstrates the SQL COUNT idiom it needs.
  - `src/callgraph.rs:94` (w1, efficiency, 8 sites) — `callgraph::build` issues three separate facet point-queries per CodeFile (`symbols_of`, `test_symbols_of`, `calls_of`) plus three `serde_json` parses, in a loop over the whole roster — an N+1 with no bulk facet read available in the store API.
  - `src/commands/diagnostics_cmd/findings.rs:123` (w2, efficiency, 8 sites) — `loom findings` walks the entire Finding roster THREE times in three consecutive lines — `untriaged_findings`, `stale_findings`, and `findings_view` are the same whole-graph walk, the first two being literally `findings_view(store)?` plus a filter.
  - `src/store/facts.rs:1083` (w1, efficiency, 7 sites) — `reverify_all` re-reads and re-hashes the cited file once per EVIDENCE ROW, so a file cited by many facts is opened dozens of times in one sync pass — even though the same function already carries a `SuccessorCache` proving the caching idea was understood here.
  - `src/locator.rs:285` (w1, efficiency, 7 sites) — `scan_markers` reads EVERY registered CodeFile off disk and line-splits it, and `resolve_anchor` calls it fresh on every single anchor resolution — so resolving one anchor costs a full read of the whole registered corpus, and `plan_anchor_staleness` does it once per anchor-form locator edge.
  - `src/store/edges.rs:555` (w2, efficiency, 7 sites) — `edge_retired(edge_id)` re-SELECTs the edge row its two callers are already holding in hand, adding one wholly redundant `get_edge` per edge to every live-edge scan in the crate.
  - `src/store/nodes/mutate.rs:313` (w2, efficiency, 4 sites) — `notes_for` loads every Note node in the graph and filters in memory on a JSON body field, on every call — and `delete_node` re-implements the same full Note scan rather than using it, despite `notes_for`'s doc claiming to be the single owner of that lookup.
  - `src/commands/orient_cmd.rs:213` (w2, efficiency, 4 sites) — `loom orient session` pays the whole-graph Journey readiness walk TWICE — once inside `all_scorecards` and again inside `ladder_and_depths` — even though the `_with` seam that exists precisely to prevent this is right there.
  - `src/packs.rs:85` (w1, efficiency, 3 sites) — `recommended_packs` performs a full recursive directory walk (depth 6, every file's extension classified into a counting map) and the ONLY thing it reads back out of that map is one `contains_key("sql")`.
  - `src/coverage.rs:68` (w1, efficiency, 3 sites) — `matching_ignore_rules` re-queries the `ignores` meta row, re-parses its JSON, and compiles a fresh single-glob `GlobSet` PER RULE on every call — and it is called inside a per-codefile loop that has already fetched the same rule list two lines earlier.
  - `src/commands/domain_cmd/vocab.rs:56` (w1, efficiency, 3 sites) — Builds the entire graph snapshot — nodes, edges, facts, one `evidence_for` query per fact, all facets — and immediately discards everything but `.tags`, of which this graph has exactly one.
  - `src/commands/diagnostics_cmd/impact.rs:34` (w1, efficiency, 3 sites) — `impact` reads the full CodeFile roster twice in one function — once to linear-search for the target by name, once to build the name→node map it then uses — on top of the `callgraph::build` at :22 that already walked the same roster.
  - `src/completeness/scorecard.rs:128` (w2, efficiency, 3 sites) — `BoundaryIndex` was built to stop per-intent store round trips, but it only covers the boundary axis: `apply_waiver`, `scenarios_axis`, `prerequisites_axis`, `proof_axis` and `questions_axis` still issue point queries per intent (and per family member), so `all_scorecards_with` remains an N+1 over every feature intent.
  - `src/absorb.rs:185` (w2, efficiency, 3 sites) — `absorb::observe` re-runs FNV-1a over the entire file body once per newly-seen symbol and once per stale locator, inside loops where the file's fingerprint is a loop invariant already available one scope up.
  - `src/evidence.rs:644` (w1, efficiency, 2 sites) — The citation loop deliberately caches the extraction per file (`symbol_cache`, with a comment saying so) but leaves the `read_to_string` and the `content.lines()` Vec inside the loop, so a file cited five times is still read from disk and re-split five times.
  - `src/workitem/queues/prescreen.rs:70` (w2, efficiency, 2 sites) — `prescreen_for` calls `store.is_hit_suppressed` once per pre-screen hit — up to 200 separate `SELECT COUNT(*)` round trips per work packet — when the store already exposes a bulk reader for the same rule's adjudications.
  - `src/completeness/scorecard.rs:525` (w2, efficiency, 1 sites) — `proof_axis` re-queries the `Validates` edges of the intent that `proofstrength::assess` already fetched one line earlier and threw away.

## F-text-capping  —  1 findings, 2 corroborating sites

- [x] STATUS: APPLIED 2026-08-23 — `text::bounded_head_tail` added (+3 tests); `release/section_06.rs:223` and `journey_runtime/process.rs:174`, which were byte-identical 19-line bodies, both route through it. clippy 0, tests 1304/0. The remaining markers (runner.rs, subprocess.rs, pattern.rs, proof.rs) are a deliberate OUTPUT choice and stay in F as behavior-changing.

  - `src/journey_runtime/types.rs:12` (w1, simplification, 2 sites) — A fifteen-line doc block explaining the Journey step timeout — including the literal values 2700s, 900s and 300s — is attached to `STREAM_EXCERPT_BYTES`, because the constant it describes was moved to journey/spec.rs and the comment stayed behind.

## G-altitude  —  13 findings, 97 corroborating sites

- [ ] STATUS: PARTIALLY APPLIED 2026-08-23, verified clippy 0 / tests 1304-0. DONE: `fn layer` moved out of commands.rs into commands/domain_cmd/layers.rs beside `layer_detector_state`, which it already reached into for half its body — every other dispatch arm routes to a handler module, this one held its whole body in the dispatcher. STILL PENDING: the other 12 altitude findings.   (2 behavior-changing, default SKIPPED)

  - `src/workitem/queues/roster.rs:211` (w1, altitude, 29 sites) — roster.rs and packets.rs independently re-derive the reason string and Target for the SAME queue candidate from the SAME predicates — 20 literals are duplicated verbatim between the two files.
  - `src/commands/capture_cmd.rs:273` (w1, altitude, 15 sites) — Every `list` command hand-writes the same status/name/short-id row — `println!("{:<10} {} [{}]", n.status, n.name, model::short(&n.id))` — eleven times across nine files, while the FOOTER of those exact same loops was already extracted into `commands::page_footer`.
  - `src/completeness/mod.rs:58` (w1, altitude, 8 sites) — "Is this Validation a compiled-Journey proof?" — the gate that decides whether a proof is compiler-owned — is hand-inlined at eight sites in eight modules, spelled four different ways, with no shared predicate.
  - `src/maturity.rs:53` (w1, altitude, 7 sites) — `Ladder::phase` stores a typed `Lane` as a `String`, and three downstream sites recover the lane by string comparison instead of carrying the enum.
  - `src/workitem/mod.rs:639` (w1, altitude, 7 sites) — The role/lane → TruthAxis mapping is written three times — once typed on `Lane`, once as a raw-&str match in workitem, once inline in orient_cmd's guide table — despite truth.rs's module doc claiming the mapping lives in one place.
  - `src/commands/status_cmd.rs:782` (w1, altitude, 7 sites) — `require_lane` re-implements the store's own lane gate at the command plane — a byte-for-byte copy of `Store::check_lane`, including its bail text, even though `check_lane` is already `pub(crate)` and reachable.
  - `src/store/open.rs:479` (w1, altitude, 5 sites) — The `Implements`-edge lane exception is bolted onto three separate call sites as `if kind == EdgeKind::Implements { check_grounding_write() } else { check_lane(spec(kind).owner) }`, because `EdgeKindSpec` can only express a single `owner` and has no room for a second permitted lane.
  - `src/mcp.rs:247` (w1, altitude, 4 sites) — Two MCP tools — `loom_absorb` and `loom_journal` — implement command logic inside the transport module, against the file's own stated contract; `loom_absorb` even re-derives the `ready`/`needs_you` split that `diagnostics_cmd::absorb_cmd` already computes.
  - `src/journey_runtime/types.rs:32` (w1, altitude, 4 sites) **[behavior-change]** — `EXECUTOR_PLATFORM_ENVIRONMENT` is a named public const for the process-launch env allowlist, and three other modules re-type the same list inline with three different memberships.
  - `src/commands/proof_cmd/support.rs:24` (w1, altitude, 4 sites) **[behavior-change]** — Two different functions named `covered_hashes` both build the `RunRecord.covered` file→fingerprint map, with opposite policies for an unreadable file — one stores the empty hash, the other aborts.
  - `src/seed.rs:392` (w2, altitude, 4 sites) — `derive_findings` writes the four per-callable detectors as four copy-pasted `if metric > threshold { out.push(FindingDesc { … }) }` blocks that differ only in five values, with the rule key spelled a second time as the finding kind (`complex_symbol` vs `complex-symbol`).
  - `src/proofstrength/command.rs:604` (w2, altitude, 2 sites) — `loom_cli_handler` is a hand-maintained shadow copy of the `commands::run` dispatch table: 23 `cli::Command` variants mapped to hardcoded source-file paths and function-name strings, with a comment admitting it restates `commands::run`'s branches.
  - `src/commands.rs:613` (w1, altitude, 1 sites) — `query_terms` — a pure text helper with exactly one consumer, `discover_cmd::search`— lives in the dispatcher module alongside the routing table, together with its stopword list and its unit test.

## H-misc-dedupe  —  37 findings, 161 corroborating sites

- [ ] STATUS: PARTIALLY APPLIED 2026-08-23, verified clippy 0 / tests 1307-0.
  APPLIED:
  - FNV-1a schedule: 5 sites in 3 modules -> one streaming `artifact::Fnv1a`. Constant sites 5 -> 1.
    Streaming kept because section_08 documents that buffering ~/.rustup SIGKILLed the release rehearsal.
  - JSON canonicalizer: 5 copies / 4 names / 5 modules -> `src/canonical.rs` (+3 tests).
  - `.loom` and `loom.graph.json` routed through crate::LOOM_DIR / crate::GRAPH_EXPORT at 11 sites.
  - dead `completeness::journey_readiness_with_journal` re-export deleted (its comment claimed a consumer;
    the only callers are inside readiness.rs calling it directly).
  - `signal.rs` `#[allow(unused_imports)] use graph::node_name` shim removed; the two children that relied
    on it now spell `graph::node_name` like their siblings.
  SKIPPED WITH REASON:
  - `store/nodes/ratify.rs:163` four byte-identical ratify methods: the two honest fixes are enforcing the
    intent/pattern distinction (a BEHAVIOR change) or collapsing the names (~50 test call sites). Neither is
    a structure-only change, and the duplication is four one-line bodies.
  DEFERRED WITH REASON:
  - `PromptContract` Default across 28 constructions: the five-field blocks are NOT uniform (varying
    indentation, some inline, `human_gate` separated from the rest by required fields), so it is 28 hand
    edits for a formatting win with no drift risk. Low value against this session's mechanical-edit error rate.
  STILL PENDING: ~28 other findings in this cluster.

  - `src/commands/proposal_cmd.rs:67` (w2, reuse, 21 sites) **[behavior-change]** — The node-summary row is hand-written at 19 more sites in five mutually incompatible key subsets, on top of the two `node_json` helpers that already exist — three of those copies are byte-identical inside a single file.
  - `src/workitem/queues/roster.rs:124` (w1, reuse, 15 sites) — `node_entry` takes the lane as a raw `&str` and re-matches it to derive `owner_role` and `routing_hint`, re-spelling both typed vocabularies — while its sibling module packets.rs uses the shared `hint_judgment()`/`hint_mechanical()` accessors for the identical values 20 times.
  - `src/store/mod.rs:157` (w1, simplification, 10 sites) — A self-deleting temp-directory test guard is written eight times across eight modules under four names — four of the copies are byte-identical, and one carries a doc comment admitting the duplication.
  - `src/store/open.rs:309` (w1, reuse, 9 sites) — `lib.rs` exports `LOOM_DIR = ".loom"` and `GRAPH_EXPORT = "loom.graph.json"`, used through the const by 11 and 9 files respectively — yet nine sites in eight other files write the literal instead.
  - `src/checkpoint.rs:1120` (w1, reuse, 6 sites) **[behavior-change]** — The "this path enters a reserved root" rule is implemented four times in four files with four different literal sets — `(".loom",".git")` twice verbatim, `(".git",".loom","target")` once, and a ten-entry `INVENTORY_RESERVED_COMPONENTS` once.
  - `src/release/section_02.rs:112` (w1, reuse, 6 sites) — The manifest-hash recipe `fingerprint(to_string(canonical_json(to_value(x))))` is hand-inlined at five release sites even though `commands/journey/derive.rs:117 derivation_manifest_hash` is exactly that function, and the release sites compare their result against the hash that function minted.
  - `src/checkpoint.rs:1100` (w1, reuse, 6 sites) **[behavior-change]** — "Is this a normalized, confined relative path?" is hand-written six times across six modules with five different reserved-component lists — and checkpoint.rs's copy is a near byte-identical twin of journey/surface_setup.rs's.
  - `src/signal/debt/cochange.rs:593` (w1, simplification, 6 sites) — The `codefile` and `empty_snap` test fixture builders are byte-identical in signal/debt.rs and signal/debt/cochange.rs, and travel.rs writes the same `Snapshot { .. }` literal inline four more times.
  - `src/completeness/mod.rs:433` (w1, simplification, 6 sites) — Three module facades keep re-exports alive with `#[allow(unused_imports)]` plus a comment naming a consumer; the `completeness` one has no consumer at all, and the `signal` one exists only so two children can spell a path the other two children spell properly.
  - `src/commands.rs:50` (w1, simplification, 6 sites) — Three items in the `commands` public facade have no production caller — they exist only so integration tests can reach them, and one of them flattens a named struct back into an anonymous 4-tuple to do it.
  - `src/workitem/contracts/inspect.rs:39` (w2, simplification, 6 sites) — `PromptContract` has five fields that are empty in almost every contract, and because the struct does not derive `Default` all 26 constructions spell them out: `evidence_template: None, examples: None, pre_screen: None, pre_screened_hits: Vec::new(), human_gate: None`.
  - `src/rolelease.rs:383` (w1, simplification, 5 sites) — `rolelease.rs` renders "seconds since last seen" with the same `now.saturating_sub(x.last_seen_ms) / 1000` expression at four sites, and `describe` and `roster_value` each carry a full private copy of the per-role debt computation.
  - `src/proofstrength/mod.rs:136` (w1, simplification, 5 sites) — `ProofAssessment` stores `any_passing` and `meaningful_passing`, both derived from the sibling `best_passing_strength`; the derivation that the struct refuses to own is then re-spelled as an identical five-line prelude at three call sites.
  - `src/journey_runtime/compile.rs:127` (w2, simplification, 5 sites) — The compiled-setup rule set (before_steps well-formedness, plus the four setup-operation rules) is written out twice inside compile.rs itself — once in `compile_surface`'s setup closure against the authored `SurfaceSetup`, once in `CompiledJourneyProof::validate` against the `CompiledSetup`.
  - `src/journey/spec.rs:620` (w1, reuse, 4 sites) — The recursive "sort every object's keys, recurse into arrays" JSON canonicalizer is written five times under four names, in five different modules, all feeding hashes that are compared against each other.
  - `src/checkpoint.rs:884` (w1, reuse, 4 sites) — The byte-capping stdout filter for a `controlled_with_output` git run — Arc<AtomicBool> flag, running `accepted` counter, cap check, and the identical post-wait re-check — is duplicated between `checkpoint.rs` and `signal/debt/git.rs`, along with two separately-declared 16 MiB constants.
  - `src/release/section_08.rs:301` (w1, reuse, 4 sites) — The recursive "sort every JSON object key" canonicalizer is written five times under five names in five modules, and every copy feeds a hash that other modules compare against.
  - `src/store/codec.rs:13` (w1, reuse, 4 sites) — The FNV-1a 64-bit schedule is re-typed five times across three modules — extending the known artifact.rs pair with a TreeHasher and a second fingerprint_bytes in release/section_08.rs and fnv_hex_digest in store/codec.rs.
  - `src/store/nodes/ratify.rs:163` (w1, simplification, 4 sites) — Four `Store` methods differ only in name — `ratify_intent`/`ratify_pattern` and `ratify_intent_from_human`/`ratify_pattern_from_human` have byte-identical bodies, and the intent/pattern distinction the names promise is enforced nowhere.
  - `src/journey_runtime/types.rs:32` (w1, reuse, 3 sites) **[behavior-change]** — `EXECUTOR_PLATFORM_ENVIRONMENT` is the crate's named list of process-launch environment essentials, and three other spawn sites hand-write their own overlapping copy of it instead of using it.
  - `src/proofstrength/command.rs:176` (w2, simplification, 3 sites) — The cargo option vocabulary is spelled four different ways inside one file — a reject list, a `strip_prefix` chain, a `takes_value` `matches!`, and a second reject list in `cargo_run_entries` — with the `word == "--x" || word.starts_with("--x=")` idiom hand-expanded 11 times and the `--color` value check written 4 times.
  - `src/commands/proof_cmd/observe.rs:131` (w2, reuse, 3 sites) — POSIX single-quote shell escaping is implemented four times in four modules under four names, with two different escape sequences for an embedded quote.
  - `src/commands/status_cmd.rs:418` (w1, reuse, 2 sites) — The excluded-codefile percentage — divide-by-zero guard and all — is written out verbatim in two renderers instead of living beside `CoverageScope::excluded()`, the derived accessor it is built from.
  - `src/completeness/mod.rs:214` (w1, reuse, 2 sites) — `projection_current` — the one-line rule for whether a Journey projection edge counts as current — is defined three times in three modules, two of them byte-identical.
  - `src/store/facts.rs:1122` (w1, simplification, 2 sites) — The "recompute the content-addressed evidence id, probe for a colliding twin, delete-or-update" block is written out twice in the same `match`, arms `AnchorFate::Moved` and `AnchorFate::Refreshed`.
  - `src/research.rs:53` (w2, reuse, 2 sites) **[behavior-change]** — `research::substantive` inlines its own five-token placeholder list instead of using `grammar::is_placeholder` / `grammar::PLACEHOLDER_TOKENS`, the crate's named and documented owner of that judgment — and `capture_cmd.rs:877` inlines a byte-identical third copy that re-checks a value `substantive` already checked.
  - `src/journey_runtime/artifacts.rs:77` (w2, reuse, 2 sites) — "Write to a temp sibling, then rename over the target" is implemented three times — twice in sibling modules of the same `journey_runtime` package (`artifacts::atomic_write`, `temporal::atomic_replace_temporal_file`) and once in `rolelease::write_lease`.
  - `src/extract/metrics.rs:140` (w2, simplification, 2 sites) — `TS_METRICS` restates `JS_METRICS` in full — the seven-entry `branch_kinds` list and `flat_branch_kinds` are byte-identical — and `TS_SPEC` likewise restates `JS_SPEC`'s `imports`, `callables` and `declarators` while its `symbols` list is a superset.
  - `src/workitem/contracts/quality.rs:71` (w1, reuse, 1 sites) — The `loom rule verdict` write-back template is spelled twice, byte-identically, once inside `verdict_write_back` (the function that exists to own write-back strings) and once inline in `quality_contract_body`.
  - `src/commands/journey/derive.rs:384` (w1, reuse, 1 sites) — `command_relationship_path_exists` is a byte-identical copy of `journey/derivation.rs:270 relationship_path_exists`, re-typed under a `command_` prefix in a different module.
  - `src/risk.rs:205` (w1, reuse, 1 sites) **[behavior-change]** — `days_between` — including its private nested `ord` helper and its fake `y*365 + m*31 + day` calendar — is written twice in two modules, identical except for a `.max(0)`.
  - `src/subprocess.rs:202` (w1, simplification, 1 sites) — `set_fd_flag` and `set_status_flag` are the same 15-line fcntl read-modify-write body, differing only in the F_GETFD/F_SETFD vs F_GETFL/F_SETFL constant pair.
  - `src/extract/calls.rs:391` (w1, simplification, 1 sites) — The `extract` test shim — including its four-line doc comment — is duplicated verbatim in two sibling `#[cfg(test)]` modules inside the same file, 100 lines apart.
  - `src/store/nodes/mutate.rs:37` (w2, simplification, 1 sites) — The "a quality rule changed, so stale every governs edge it owns" block is written twice in one file — in `update_node` and in `set_node_body` — differing only in the word inside the cause string.
  - `src/store/nodes/mutate.rs:221` (w2, reuse, 1 sites) — `delete_edge_records` exists as the edge-side "delete every row keyed to this id" helper, but the node-side equivalent has no helper and is hand-inlined twice inside `delete_node` — once for the target node, once per dependent note.
  - `src/mcp.rs:847` (w2, reuse, 1 sites) — Two JSON/serialized version fields call `env!("CARGO_PKG_VERSION")` inline instead of `lib.rs:86 CRATE_VERSION`, the named constant whose doc comment exists precisely to keep this value distinguishable from `SCHEMA_VERSION`.
  - `src/research.rs:183` (w2, simplification, 0 sites) — `stamp_conclusion_freshness` re-evaluates the "is this source still usable?" predicate that `validate_close` — which it calls on its own first line — has just evaluated over the same list, with the closure written out verbatim a second time.

## I-test-fixtures  —  13 findings, 105 corroborating sites

- [ ] STATUS: PARTIALLY APPLIED 2026-08-23 — new `src/testutil.rs` (cfg(test) only) holds ONE `TmpRoot`: unique `<prefix>-<pid>-<nanos>-<seq>` naming, create-on-new, remove-on-drop, plus `write()`. 6 of the 8 copies migrated to it: store/mod.rs, store/open.rs, store/schema.rs, fsglob.rs, thresholds.rs, batch_auth.rs — 5 dead COUNTER statics and 9 dead imports removed with them. CORRECTION TO THE FINDING: it called all 8 the same harness. They are not. scan.rs:816 returns Result AND seeds src/lib.rs fixture content; prescan.rs:174 exposes `path` as a FIELD, not a method. Forcing those two onto the shared form would change test contracts, not just structure, so both were reverted and left alone. Verified: check 0, clippy 0, tests 1304/0. STILL PENDING: the tests/ directory sweep (78 files) findings in this cluster.   (0 behavior-changing, default SKIPPED)

  - `tests/ring1.rs:92` (w2, simplification, 20 sites) — The two-line opener `let tmp = Tmp::new(); let store = Store::init(tmp.path(), Some("t"), false).unwrap();` appears verbatim 321 times across tests/, and the `Tmp::new()` + `Store::init(...)` pair 403 times — while `tests/common/mod.rs` exports a `Tmp` and a `codefile` helper but no `fresh_store()`.
  - `tests/ring12_federation.rs:46` (w2, reuse, 17 sites) — "Run `loom --graph <root> <args> --json`, assert exit zero, parse stdout as JSON, panic with stdout/stderr on failure" is written out 18 times across 16 test files, under six different names (`loom_json`, `loom_json_global`, `loom_json_out`, `run_cli_json`, `cli_json`, `builder_json`, `builder`, and one confusingly named `loom_command`).
  - `tests/ring18_journey_baseline.rs:16` (w2, simplification, 16 sites) — The minimal Journey-spec JSON envelope — `"schema": JOURNEY_SCHEMA` / `"inputs": {}` / `"preconditions": []` / `"profiles":{"proof":{"inputs":{},"workspace":{}}}` — is hand-written 33 times across 11 test files (plus once inside `tests/common/mod.rs` itself), including four as single-line raw string literals.
  - `tests/common/mod.rs:20` (w2, reuse, 14 sites) — The "unique temp dir named pid+nanos+counter, rmdir on Drop" harness is written 14 separate times — 10 private copies inside src/ cfg(test) mods, 3 inside tests/ files that already have the canonical one in scope, and the canonical one itself.
  - `tests/ring5.rs:1678` (w2, reuse, 11 sites) — `fn loom_bin() -> PathBuf` is re-declared in 12 test files, in two mutually incompatible implementations: 10 return `env!("CARGO_BIN_EXE_loom")`, while `tests/ring27_observe.rs:96` and `tests/ring30_runner.rs:29` walk `current_exe()` up out of `deps/` to guess the path.
  - `tests/ring12_federation.rs:19` (w2, reuse, 7 sites) — `fn loom_init(root, name)` is re-declared in 8 test files, every one of which already writes `mod common;` — and `tests/common/mod.rs:65` exports exactly that function.
  - `src/divergence.rs:563` (w1, simplification, 5 sites) — Six `pub fn` items across six modules have zero callers anywhere in src/ or tests/, and four of them carry doc comments naming a consumer that does not exist.
  - `src/ratification.rs:27` (w1, reuse, 4 sites) — `ratification::ASSERTED_STATES` is a byte-identical copy of `grammar::RATIFICATION_STATES`, with a paraphrase of the same doc comment, and has zero references anywhere.
  - `src/commands/proof_cmd/support.rs:51` (w1, simplification, 4 sites) — Two of `ProofCommandCollision`'s four fields — `detected` and `prior_behavior_count` — are stored copies of `!prior_behaviors.is_empty()` and `prior_behaviors.len()`.
  - `tests/ring23_chokepoint.rs:31` (w2, reuse, 4 sites) — `fn source_files() -> Vec<(String, String)>` — a recursive `read_dir` walk of `src/` collecting `(relative path, file body)` — is copied verbatim (one `.to_string()` vs `.replace('\\', "/")` apart) into two structural tests, and two more files hand-roll the same walk with different collection types.
  - `src/policy.rs:26` (w1, simplification, 2 sites) — `policy::INTENT_ORIGINS` is a dead `pub const` whose four-value vocabulary has already drifted from the two values the code actually writes and publishes.
  - `tests/ring43_validation_specific_strength.rs:899` (w2, simplification, 1 sites) — Inside a single test function, the identical 16-line recursive walk of `src/` is written twice back to back — once to copy the files into a temp root, once to register them as CodeFile nodes — differing only in the two lines at the bottom of the loop body.
  - `tests/common/mod.rs:88` (w2, simplification, 0 sites) — `common::prove` — 32 lines of helper plus a 9-line doc comment explaining the evidence-spine rationale — has zero callers; it compiles only because of the `#[allow(dead_code)]` on the line above it.

## J-error-vocab  —  12 findings, 142 corroborating sites

- [ ] STATUS: PENDING   (8 behavior-changing, default SKIPPED)

  - `src/store/nodes/mutate.rs:14` (w2, reuse, 26 sites) **[behavior-change]** — `get_node(id)? .ok_or_else(|| anyhow!("no node '{id}'"))` and its edge twin are inlined 20 times in the store, the edge copy split across two format strings (`{edge_id}` vs `{id}`), and five further modules phrase the identical "subject id is not in the graph" failure five other ways.
  - `src/journey/compile.rs:132` (w2, altitude, 22 sites) **[behavior-change]** — The "load a registered Journey's artifact and verify it" block is copy-pasted across journey/compile.rs, commands/journey/support.rs, commands/journey/lint.rs, commands/journey/runtime.rs and journey_exercises.rs, carrying eight byte-identical error strings — and three of the pairs have already drifted.
  - `src/store/nodes/lookup.rs:154` (w2, altitude, 21 sites) — The 0/1/many "resolve a subject by key" refusal is written seven times with seven different message pairs, and two production sites steer control flow by `.to_string().starts_with(...)` on that prose.
  - `src/store/edges.rs:52` (w2, simplification, 19 sites) **[behavior-change]** — The read-after-write invariant violation ("we just inserted it and it is not there") is spelled twenty different ways across eleven modules, and not one of the twenty names the id that vanished.
  - `src/store/judgments.rs:19` (w1, reuse, 13 sites) — JudgmentKind and JudgmentState hand-write the entire str_enum! surface twice in one file — per-variant `#[serde(rename)]`, `as_str`, `Display`, `FromStr` — so each variant's canonical string is typed four times (five counting the bail! prose).
  - `src/journey/spec.rs:427` (w2, reuse, 13 sites) **[behavior-change]** — `nonempty(label, value)` exists and is used 25 times, but is `pub(super)` to the journey module, so eleven "must not be empty" refusals outside that module are hand-written — one of them with the negation flipped.
  - `src/commands/capture_cmd.rs:305` (w1, altitude, 11 sites) — The "declare a local `const FOO: &[&str]`, `contains` it, bail with `FOO.join("|")`" idiom is copy-pasted eight times across four modules as the crate's ad-hoc substitute for parsing into a typed enum.
  - `src/store/open.rs:113` (w2, reuse, 5 sites) **[behavior-change]** — The crate's entry error — "there is no graph here, run `loom init`" — is written four times in production with two different wordings and two different subjects (the db path vs the search cwd).
  - `src/store/facts.rs:505` (w2, simplification, 5 sites) **[behavior-change]** — "This fact is derived — sync owns it, you may not write it" is the store's core authority refusal and it is written six times in six different sentences, three of which name the remediation command and three of which do not.
  - `src/candidate_surface_policy.rs:926` (w1, altitude, 3 sites) **[behavior-change]** — The candidate-surface policy enforces four caps — three named consts plus a bare literal 128 — and every one of its violation messages names neither the limit nor its value, which is the single contract limits.rs exists to hold.
  - `src/journey/sources.rs:77` (w2, altitude, 3 sites) **[behavior-change]** — The entire Journey runtime-source validation policy is implemented twice — once in `journey/sources.rs` (authoring side) and once in `journey_runtime/compile.rs` (compiler side) — as four pairs of near-identical functions with divergent error texts.
  - `src/journey_runtime/compile.rs:110` (w2, altitude, 1 sites) — An 88-line fallible validator with nine `bail!` sites is written as a closure passed to `Option::map`, then unwrapped with `.transpose()?` — `let compiled_setup = setup.map(|setup| { ... }).transpose()?` — inside the already-283-line `compile_surface`.

## K-function-size  —  7 findings, 27 corroborating sites

- [ ] STATUS: PENDING   (0 behavior-changing, default SKIPPED)

  - `src/commands/codefile_cmd.rs:12` (w2, altitude, 13 sites) — `codefile_cmd::dispatch` is 314 lines because three of its six match arms carry their whole command body inline (`Add` 105 lines, `Remove` 149 lines, `List` 46 lines) while the other three are already one-line delegations to `codefile_anchor` / `codefile_rescan` / `codefile_show` in the same file — the convention is established and then broken in the same match.
  - `src/commands/journey/derive.rs:703` (w2, altitude, 4 sites) — `journey_derive_accept` is 371 lines (703-1073) and holds six sequential responsibilities in one body — manifest load, pre-mutation intent resolution, Proposal record selection, an idempotent-return path, the whole write transaction, and emission — even though its own file has already factored out fourteen sibling helpers including `reconcile_derivation_relationships` for the *other* half of the same reconcile step.
  - `src/store/facets.rs:261` (w2, altitude, 3 sites) — `restore_inner` is 312 lines and labels its own decomposition in comments — `// Phase 1:` (:294), `// Phase 2:` (:427), `// Phase 3, mandatory:` (:566) — so three functions already exist in the author's head and only the comments name them; worse, the orphan-partition rule is split across the Phase 1/Phase 2 boundary, with facets/tags partitioned at :384-425 and the structurally identical fact partition at :481-522, after the transaction has opened.
  - `src/journey/surface_ops.rs:422` (w2, altitude, 3 sites) — `InterfaceSurfaceDefinition::validate` is 269 lines in which 250 are one `for operation in &self.operations` body that validates four nested levels — operation, argument, output assertion, exercise — inline, while the same file's convention is a small named validator per leaf (`validate_stable_id`, `nonempty`, `insert_unique`, `validate_json_pointer`, `validate_runtime_source`, `validate_process_environment_name`) and each nested level is already its own public type.
  - `src/signal/doctor.rs:249` (w2, altitude, 2 sites) — `journey_integrity_issues` is 349 lines holding five mutually independent integrity checks — retired metadata, registration/artifact agreement, Derives bindings, Surfaces bindings, Proves chain — each of which already announces itself with its own `let X: Vec<&Edge> = snap.edges.iter().filter(...)` header and its own `kind:` string, in a module whose every other check IS a named function.
  - `src/store/facts.rs:1063` (w2, altitude, 2 sites) — `reverify_all` is 246 lines carrying four distinct per-fact responsibilities in one loop body: applying the four `AnchorFate` outcomes to each evidence row (:1087-1186, at 4-5 levels of nesting), computing the fact's new strength, re-opening the subject edge with a `stale_cause` facet and proof reset, and writing the fact row — none of which is named.
  - `src/journey/spec.rs:306` (w2, simplification, 0 sites) — Inside the 180-line `JourneySpec::validate`, the profile loop walks `profile.inputs` twice, and the second walk (:306-313) re-checks a condition the first walk's match arm at :238 already bails on — `input.secret && binding.template.is_some()` is exactly the `(Some(_), None) if input.secret` arm — so the second loop and its `expect("validated above")` are unreachable.
